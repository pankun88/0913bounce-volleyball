# 바운스발리볼 개발·검사·배포 가이드

이 문서는 설치·로컬 실행·검사·배포 절차를 정리합니다. 경기 규칙은 [`AGENTS.md`](AGENTS.md), 현재 운영 상태와 복구 절차는 [`인수인계_노트.md`](인수인계_노트.md), 과거 버그의 배경은 [`문제해결기록.md`](문제해결기록.md)를 참고합니다.

## 1. 저장소와 고정 대상

- Firebase 프로젝트는 `bounce-0913-edu-cup-2026`, 대회 문서 루트는 `tournaments/main`입니다. `js/firebase-config.js`에는 프로젝트 ID와 `TOURNAMENT_ID = "main"`이, `.firebaserc`에는 기본 Firebase 프로젝트가 정의되어 있습니다.
- 화면은 `admin.html`(관리자), `recorder.html`(기록관), `dashboard.html`(관객)입니다.
- 브라우저 코드는 `js/`, 서버 함수는 `functions/`에 있습니다.
- `firestore.rules`는 데이터 권한, `firestore.indexes.json`은 Firestore 인덱스, `firebase.json`은 Hosting·Functions·에뮬레이터 구성을 담습니다.
- 자동 검사는 `js/`, `functions/test/`, `test/emulator/`에 있습니다. 배포된 상태와 실제 운영 리허설의 최신 기록은 인수인계 노트에서 확인합니다.

프로젝트 ID나 대회 ID를 새로 만들거나 클라이언트 설정만 바꿔 다른 프로젝트를 가리키지 않습니다. 현재 로컬·공유 미리보기도 운영 Firebase를 사용하므로, 개발·회귀검사 목적으로 실제 대회 데이터를 생성·수정·삭제·복원하지 않습니다.

## 2. 설치

검사와 로컬 서버에 다음 런타임이 필요합니다.

- Node.js 22
- Java 21 (Firebase 에뮬레이터)
- Python 3 (정적 로컬 서버)

저장소 루트와 `functions/`의 lockfile을 사용해 설치합니다.

```bash
npm ci
npm --prefix functions ci
```

전역 패키지를 추가로 설치하거나 lockfile을 갱신하는 방식으로 설치하지 않습니다. `firebase-tools`는 루트 개발 의존성에 있으므로 설치 후 `npx firebase ...`로 실행합니다.

## 3. 로컬 화면 실행

저장소의 [`로컬서버_시작.command`](로컬서버_시작.command)를 실행하면 현재 스크립트가 포트 `8765`에서 캐시 방지 정적 서버를 시작하고 `admin.html`을 엽니다. 브라우저에서 직접 열 주소는 다음과 같습니다.

```text
http://localhost:8765/admin.html
```

위 스크립트는 모든 네트워크 인터페이스에 바인딩합니다. 본인 컴퓨터에서만 확인할 때는 다음처럼 루프백에 한정한 서버를 사용할 수 있습니다.

```bash
python3 -m http.server 8765 --bind 127.0.0.1
```

기본 Python 서버에는 launcher의 캐시 방지 헤더가 없으므로 변경이 보이지 않으면 개발자 도구의 캐시 사용 중지를 확인합니다. 이 정적 서버에는 Firebase Hosting의 파일 제외 규칙도 적용되지 않으므로 저장소를 외부에 공유하지 않습니다.

`file://`로 HTML을 직접 열지 않습니다. ES module 로딩과 Firebase 인증 흐름을 확인하려면 HTTP 로컬 서버를 사용합니다. 로컬 화면과 공유 미리보기는 현재 `js/firebase-config.js`의 운영 프로젝트에 연결되므로, 데이터 변경이 필요한 검사는 반드시 에뮬레이터에서 수행합니다.

## 4. 클라이언트·서버 경기 로직 동기화

경기 판정과 진출 계산의 작성 원본은 `js/match-logic.js` 하나입니다. `scripts/prepare-shared.mjs`가 이 파일을 `functions/match-logic.generated.js`로 바이트 단위 복사합니다.

```bash
npm run prepare:shared
node scripts/prepare-shared.mjs --check
```

- `npm run prepare:shared`는 Functions 검사·배포 전에 필요한 생성 파일을 갱신합니다.
- `node scripts/prepare-shared.mjs --check`는 두 파일이 일치하는지만 확인하고, 불일치하면 실패합니다.
- `functions/match-logic.generated.js`는 직접 편집하거나 커밋하지 않습니다. 판정 변경은 `js/match-logic.js`를 수정한 뒤 생성 명령으로 반영합니다.
- 예선·본선 규칙을 임의로 문서나 코드에 덧붙이지 말고 `AGENTS.md`와 기존 테스트가 정한 해석을 유지합니다. 다자 동률의 승자승 해석도 바꾸지 않습니다.

## 5. 검사와 에뮬레이터

루트 `package.json`의 검사를 사용합니다.

```bash
npm run test:unit
npm run test:emulator
npm run test:release
```

`test:unit`과 `test:emulator` 실행 전에는 `prepare:shared`가 자동으로 실행됩니다. `test:release`는 두 검사를 순서대로 실행하므로 최종 검사에는 이 명령 하나를 사용하면 됩니다.

`test:emulator`는 `demo-bounce-volleyball` 프로젝트로 Auth·Firestore·Functions 에뮬레이터를 실행하고 `test/emulator/run-all.mjs`를 구동합니다. 실행기는 프로젝트 ID와 Auth·Firestore·Hub의 루프백 주소를 확인합니다. Functions 주소는 기본 `127.0.0.1:5001`이며 `FUNCTIONS_EMULATOR_HOST`를 외부 주소로 덮어쓰지 않습니다.

macOS에서 설치된 Homebrew Java 21을 찾지 못할 때는 현재 터미널의 경로를 지정합니다.

```bash
export JAVA_HOME="$(brew --prefix openjdk@21)/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
java -version
```

실제 프로젝트(`bounce-0913-edu-cup-2026`)나 운영 데이터로 쓰기·수정·삭제를 검증하지 않습니다. 운영과 연결된 localhost/공유 미리보기는 읽기·화면 확인 용도로만 사용하고, 데이터 변이 시나리오는 에뮬레이터 fixture와 통합 검사로 재현합니다.

## 6. Firebase 인증 최초 부트스트랩

현재 운영 프로젝트에는 이미 구성되어 있습니다. 다음은 최초 구성 절차를 보존한 것으로, 개발할 때마다 문서를 재생성하거나 기존 권한을 덮어쓰지 않습니다.

1. Firebase Console에서 `bounce-0913-edu-cup-2026` 프로젝트의 Firestore Database를 활성화합니다.
2. Authentication의 Sign-in method에서 관리자용 **Email/Password**와 기록관용 **Google** 공급자를 활성화합니다.
3. Authentication의 Authorized domains에 `bounce-0913-edu-cup-2026.web.app`, `bounce-0913-edu-cup-2026.firebaseapp.com`, `localhost`, 실제 사용하는 사용자 정의 도메인을 등록합니다.
4. Authentication에서 관리자 Email/Password 계정을 만들고 해당 사용자의 UID를 확인합니다.
5. 신뢰할 수 있는 Firebase Admin SDK 또는 Firebase Console의 Firestore 데이터 편집기로 다음 두 문서를 만듭니다.
   - `tournaments/main`
   - `tournaments/main/admins/{관리자 UID}` (빈 객체여도 됨)
6. 관리자 계정으로 `admin.html`에 로그인한 뒤 대회 설정을 입력합니다.

Firestore Rules는 이 두 문서의 클라이언트 생성을 허용하지 않습니다. 따라서 일반 브라우저 코드나 임의의 권한 완화로 부트스트랩하지 않습니다. 관리자는 Email/Password와 `admins/{UID}` 멤버십이 모두 필요합니다. 기록관은 Google 로그인 후 관리자가 발급한 현재 대회 접근 코드를 교환해야 하며, 공용 코드·grant의 발급과 폐기는 관리자 화면의 기존 흐름을 사용합니다.

## 7. 승인된 배포

배포는 변경 내용을 검토한 뒤 사용자 또는 릴리스 담당자의 **명시적 승인**을 받은 경우에만 실행합니다. 승인 전에는 운영 Firebase를 대상으로 `firebase deploy`를 실행하지 않습니다.

먼저 필요한 경우 Firebase CLI에 로그인합니다.

```bash
npx firebase login
```

`--only` 대상은 변경 범위에 따라 고릅니다. `firestore:indexes`를 모든 배포에 습관적으로 포함하지 않습니다.

| 변경 범위 | 배포 대상 |
| --- | --- |
| 브라우저 HTML·CSS·일반 클라이언트 코드 | `hosting` |
| `functions/` 코드 또는 공유 경기 로직의 서버 반영 | `functions` |
| `firestore.rules` | `firestore:rules` |
| `firestore.indexes.json`의 실제 변경 | `firestore:indexes` |

예시는 다음과 같습니다.

```bash
# 화면만 변경
npx firebase deploy --project bounce-0913-edu-cup-2026 --only hosting

# Functions만 변경
npx firebase deploy --project bounce-0913-edu-cup-2026 --only functions

# Rules만 변경
npx firebase deploy --project bounce-0913-edu-cup-2026 --only firestore:rules

# 인덱스 파일을 실제로 변경한 경우에만
npx firebase deploy --project bounce-0913-edu-cup-2026 --only firestore:indexes
```

예선 진출 계산·검증, 본선 진출 메타데이터, 본선 공개 경계를 변경하는 작업은 클라이언트·서버·Rules의 결합 계약입니다. 위의 개별 배포 예시로 분리하지 말고 필요한 대상을 같은 릴리스에서 반영합니다. 공유 경기 로직을 바꿨다면 먼저 `npm run prepare:shared`를 실행하고, 인덱스를 바꾼 경우에만 `firestore:indexes`를 추가합니다.

새 서버 API를 먼저 준비해야 하는 이번 유형의 배포는 입력 작업이 없는 시간에 조율하고, 서버 배포 성공을 확인한 뒤 Rules와 화면을 반영합니다. 여러 서비스의 배포가 원자적으로 이루어지는 것은 아닙니다.

```bash
npx firebase deploy --project bounce-0913-edu-cup-2026 --only functions
npx firebase deploy --project bounce-0913-edu-cup-2026 --only hosting,firestore:rules
```

서버·Rules가 준비된 뒤 승인된 미리보기를 갱신합니다. 미리보기는 기간이 있는 Hosting 채널이지 격리된 Firebase 환경이 아닙니다.

```bash
npx firebase hosting:channel:deploy correction-review --project bounce-0913-edu-cup-2026 --expires 7d
```

배포 후에는 실제 서비스 상태·화면 연결과 배포 파일 일치를 확인합니다. Hosting 제외 목록의 `**/.*`, `**/.*/**`, `functions/**`, `test/**`, `**/*.log`, `**/*.mjs` 등을 유지하고 내부 경로가 공개되지 않는지도 확인합니다. 컴파일 경고는 기록하며 검사·권한을 완화해 감추지 않습니다.

`firebase.json`의 Functions predeploy가 `npm run prepare:shared`를 다시 실행하지만, 배포 전에 생성 상태를 직접 확인해 두는 원칙은 유지합니다. 현재 배포 버전·영수증·운영 검증 결과는 날짜가 있는 사실이므로 [인수인계 노트](인수인계_노트.md)에서 확인하며, 이를 영구적인 현장 준비 보증으로 해석하지 않습니다.
