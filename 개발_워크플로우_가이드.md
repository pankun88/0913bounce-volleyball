# 바운스발리볼 웹앱 — 개발 워크플로우 가이드 (ver1 회고)

이 문서는 **ver1까지 만들면서 좌충우돌했던 내용을 정리**한 가이드입니다. 비슷한 "관리자 입력 + 실시간 공개 대시보드" 형태의 웹앱을 처음부터 다시 만들거나, 이 앱을 계속 수정할 때 참고용으로 씁니다.

---

## 1. ver1 스냅샷 — 무엇을 만들었나

- **관리자 페이지** (`admin.html` + `js/admin.js`): 대회설정/예선/본선 3개 탭. 조·팀 등록, 예선 라운드로빈·링크제 일정 생성 및 점수 입력(승점·세트득실·승자승 자동 계산), 본선 진출팀 자동 추천(+진출 이유 표시), 대진표 자동 생성(부전승·3·4위전 포함), 결과 CSV 내보내기, 관리자 로그인/비밀번호 변경.
- **공개 대시보드** (`dashboard.html` + `js/dashboard.js`): 위 데이터를 Firebase Firestore로 실시간 구독해 그대로 보여주는 읽기 전용 화면. 모바일에서도 보도록 반응형 처리(본선 대진표 가로 스크롤 포함).
- **핵심 로직** (`js/match-logic.js`): 세트/경기 판정, 조 순위 계산. `js/test.mjs`로 단위테스트 검증.
- **로컬 개발 환경**: `로컬서버_시작.command` 더블클릭 → 캐시 방지 헤더가 적용된 로컬 서버로 `admin.html` 자동 오픈.
- **배포**: Firebase Hosting + Firestore (쓰기는 관리자 인증 필요, 읽기는 공개).
- **문서화**: `CLAUDE.md`(도메인 규칙), `README.md`(사용법), `문제해결기록.md`(버그 기록), `인수인계_노트.md`(세션 인수인계).

여기까지를 **ver1**으로 본다. 아래는 이걸 처음부터 다시 만든다면 어떤 순서·프롬프트가 가장 효율적이었을지 정리한 것이다.

---

## 2. 처음부터 만든다면: 추천 단계 순서

실제로는 "관리자 페이지부터 만들고 → 버튼이 먹통이라서 → 로컬서버 도입 → 캐시 문제 발생 → 또 수정"처럼 **문제가 터진 다음에야 대응하는 순서**로 진행됐다. 아래는 그 경험을 반영해 거꾸로 재배열한, 같은 결과를 더 적은 시행착오로 얻는 순서다.

### 0단계 — 개발 환경 먼저 (기능 코드 작성 전에)
가장 먼저 캐시 방지 로컬 서버용 `.command` 파일을 만들어둔다. 이걸 0단계에 두는 이유: 이게 없으면 이후 모든 기능 개발 단계에서 "분명히 고쳤는데 화면엔 안 보여요"라는 혼란이 반복적으로 발생한다(§3-1 참고). 처음부터 있으면 이 문제 자체가 발생하지 않는다.

> 프롬프트 예: *"프로젝트 폴더에 더블클릭으로 실행되는 로컬 서버 시작 .command 파일을 만들어줘. file://로 직접 여는 건 모듈 스크립트가 차단돼서 쓰지 말고, 캐시 방지 헤더(Cache-Control: no-store)를 붙인 파이썬 서버로 만들어줘."*

### 1단계 — 도메인 규칙을 문서로 먼저 박아두기
경기 규칙처럼 복잡하고 세부적인 로직은 코드를 짜기 전에 **자연어로 먼저 `CLAUDE.md`에 정리**한다. 이 프로젝트는 처음부터 이렇게 했고, 이후 모든 세션에서 규칙을 다시 설명할 필요가 없었다 — 효과가 좋았던 부분이니 다음에도 그대로 유지.

### 2단계 — 데이터 모델 + Firebase 골격
Firestore 컬렉션 구조를 먼저 설계하고, 보안 규칙은 **"쓰기는 인증 필요, 읽기는 공개"** 형태를 기본값으로 시작한다(관리자 입력 + 공개 대시보드 구조에 항상 맞는 패턴). 연결 자체만 검증하는 단독 테스트 페이지(`connection-test.html`류)를 같이 만들어두면 이후 "동작 안 함" 보고가 들어왔을 때 코드 버그인지 Firebase 설정 문제인지 바로 구분할 수 있다.

### 3단계 — UI보다 핵심 로직 먼저, 단위테스트와 함께
점수 판정·순위 계산처럼 정답이 명확한 로직은 화면 없이 순수 함수로 먼저 만들고 `node` 기반 단위테스트로 검증한다. 이 프로젝트의 `match-logic.js` + `test.mjs`가 그 예다. 화면(UI)은 이 위에 얹는 것이므로, 로직이 흔들리지 않게 먼저 굳혀두면 이후 UI를 갈아엎어도 핵심 판정이 깨지지 않는다.

### 4단계 — 관리자 페이지 골격 (탭 구조 + CRUD)
탭(대회설정/예선/본선)을 나누고 조·팀 등록 같은 기본 CRUD를 만든다. 이때 **삭제 기능을 만들 때마다 "이걸 지우면 이걸 참조하던 다른 데이터는 어떻게 되는가"를 같이 질문**한다(§3-6 참고) — 나중에 따로 고치는 것보다 한 번에 같이 처리하는 게 훨씬 싸다.

### 5단계 — 예선 기능
라운드로빈/링크제 등 예선 진행 로직을 만든다. 화면에 보여줄 목록은 **항상 명시적 정렬 기준(예: round 필드)으로 정렬**하고, 이 정렬 로직은 관리자 화면·대시보드·CSV 내보내기 등 쓰이는 모든 곳에서 동일하게 적용한다(§3-7 참고, 가능하면 공용 함수로 통일).

### 6단계 — 본선 대진표
대진표 "생성" 로직과 "렌더링(연결선 그리기)" 로직을 분리해서 만든다. **이 단계부터는 매번 좁은 화면(모바일 뷰포트)으로도 한 번씩 확인**한다 — 가로로 넓은 트리 구조는 데스크탑에서만 보면 절대 안 보이는 문제(§3-8)가 있다.

### 7단계 — 실시간 공개 대시보드
관리자 페이지에서 만든 데이터를 읽기 전용으로 구독해 보여준다. **연결 실패/타임아웃 시 사용자에게 보이는 에러 배너를 기능과 같이 1세트로 만든다**(§3-9 참고) — 나중에 따로 추가하지 말고 처음부터.

### 8단계 — 인증/보안
관리자 로그인, 비밀번호 변경 등을 배포 전에 마무리한다.

### 9단계 — 모바일 최적화 점검 + 마무리
전체 화면을 모바일 뷰포트로 다시 한 번 점검하고(zoom 같은 비표준 CSS 안 쓰는지, 가로 스크롤이 끝까지 닿는지), CSV 내보내기 등 마무리 기능을 붙인다.

---

## 3. 시행착오에서 건진 핵심 교훈

### 3-1. ⭐ 캐시 문제 — `.command` 파일로 영구 해결 (가장 인상적이었던 해결)

**증상**: `js/admin.js` 등을 분명히 수정했는데(디스크 확인까지 했는데도), 서버를 껐다 켜고 새로고침까지 해도 브라우저가 계속 옛 버전을 보여줬다.

**원인**: 기본 `python3 -m http.server`는 캐시 관련 헤더를 보내지 않는다. 크롬은 모듈 스크립트(`js/admin.js`)를 자체 판단으로 디스크 캐시에 계속 보관·재사용한다. 서버 재시작, 일반 새로고침(Cmd+R)으로도 이 캐시는 안 풀린다.

**해결**: `로컬서버_시작.command`를 모든 응답에 `Cache-Control: no-store, no-cache, must-revalidate` 등 캐시 금지 헤더를 강제로 붙이는 커스텀 파이썬 서버로 교체. 첫 자동 오픈 URL에도 `?t=타임스탬프`를 붙여 첫 로드부터 캐시를 우회. 더블클릭 한 번이면 이 모든 게 자동으로 적용된다.

실제 코드 (그대로 복붙해서 재사용 가능):

```bash
#!/bin/bash
cd "$(dirname "$0")"
PORT=8765
URL="http://localhost:$PORT/admin.html?t=$(date +%s)"

( sleep 1 && open "$URL" ) &

python3 -c "
import http.server
import socketserver

class NoCacheHandler(http.server.SimpleHTTPRequestHandler):
    def end_headers(self):
        self.send_header('Cache-Control', 'no-store, no-cache, must-revalidate, max-age=0')
        self.send_header('Pragma', 'no-cache')
        self.send_header('Expires', '0')
        super().end_headers()

class ReusableTCPServer(socketserver.TCPServer):
    allow_reuse_address = True

with ReusableTCPServer(('', $PORT), NoCacheHandler) as httpd:
    httpd.serve_forever()
"
```

**다음에 적용할 일반 원칙**: 정적 파일을 로컬 서버로 띄워 개발하는 프로젝트라면, 어떤 종류든 **0단계에 이런 캐시 방지 `.command`/`.sh` launcher를 먼저 만들어둔다.** "수정했는데 안 보여요" 류 혼란을 통째로 예방한다.

### 3-2. `file://`로 직접 열면 안 됨

크롬 계열 브라우저는 `file://`에서 `<script type="module">`을 보안 정책상 차단한다. 버튼이 전부 먹통이 되고 에러도 화면에 안 보여서 진짜 코드 버그처럼 보이지만, 실제로는 자바스크립트가 단 한 줄도 실행되지 않은 것. 반드시 `http://localhost`로 열 것 (§3-1의 `.command` 파일 사용).

### 3-3. 한글 경로 + 터미널은 항상 의심

macOS는 한글 폴더명을 NFD(자모분해형)로 저장하는데, 터미널에 한글 경로를 직접 타이핑하면 NFC(완성형)로 입력돼 "겉보기엔 똑같은 이름"인 별도 폴더가 생길 수 있다. **해결**: 경로를 직접 타이핑하지 말고 글로빙(`ls -d */`)으로 실제 폴더명을 그대로 얻어 변수에 저장해 재사용한다.

```bash
cd <부모폴더> && DIR=$(ls -d */ | grep -v outputs | head -1) && BASE="<부모폴더>/${DIR%/}"
```

### 3-4. Claude 자신의 검증(grep/Read/bash)을 100% 믿지 말 것

세션이 보는 "연결된 폴더"가 사용자의 실제 데스크탑 폴더와 다른 마운트를 가리킬 수 있다. 이 경우 Claude가 스스로 grep/Read로 "확인"해도 같은 어긋난 마운트를 다시 보는 것이라 거짓양성이 난다. **해결**: 의심되면 고유 문자열이 담긴 마커 파일을 만들고, **사용자가 직접** 자기 Finder/터미널에서 그 파일이 보이는지 확인하게 한다. 안전한 기본값은 Claude의 임시 작업폴더에서 완성 → `present_files`로 전달 → 사용자가 직접 옮기기.

### 3-5. Edit/Write 도구가 "protected location" 에러로 막히면

bash로 직접 파일을 읽고(`open()`) 문자열을 치환(`.replace()`)해서 다시 쓰는 방식으로 우회한다. 이때 `assert content.count(old) == 1` 같은 가드를 넣어 치환 대상이 의도한 곳에만, 정확히 원하는 횟수만큼 일치하는지 먼저 확인하고 나서 쓴다 — 의도치 않은 곳까지 바뀌는 사고를 막는다.

### 3-6. 부모 삭제 → 자식 데이터 "고아화" 항상 먼저 생각

조를 삭제했을 때 그 조 소속 팀들의 `groupId`가 삭제된 조 id를 계속 가리킨 채 남아, 화면 어디에도 안 보이고 지울 수도 없는 상태가 된 적이 있었다. **원칙**: 부모를 지우는 기능을 만들 때는 항상 "이 부모를 참조하던 자식들은 어떻게 처리할 것인가"(null로 일괄 변경 등)를 같은 트랜잭션/배치로 같이 처리한다.

### 3-7. 실시간 DB 구독 순서는 절대 암묵적으로 신뢰하지 않기

Firestore `onSnapshot`은 기본적으로 문서 ID 순서로 데이터를 준다. 의도한 순서(예: 경기 진행 순서)로 보여주려면 항상 명시적으로 정렬해야 한다. 이 프로젝트에서는 라운드로빈 방식에선 우연히 안 드러났던 정렬 문제가, 링크제 도형에 번호를 매기면서 비로소 드러났다. **원칙**: 같은 정렬 로직을 관리자 화면·대시보드·CSV 내보내기처럼 여러 곳에서 각각 따로 구현하면 한 곳만 빠뜨리기 쉽다 — 가능하면 공용 정렬/포맷 함수로 통일.

### 3-8. 모바일은 "마지막에 한 번"이 아니라 화면 단위로 바로 점검

대진표(가로로 넓은 트리)를 데스크탑 기준으로만 만들고 나중에 모바일을 점검하니, 그제서야 두 가지 문제를 한꺼번에 만났다:
- CSS `zoom` 속성이 iOS Safari에서 전혀 동작하지 않음(자동 축소 기능이 무용지물).
- `justify-content: center` + 콘텐츠가 넘칠 때, WebKit에서는 양쪽 끝이 스크롤로도 닿지 않게 잘리는 현상.

**원칙**: 가로 스크롤이 필요한 와이드 콘텐츠(표, 트리, 타임라인, 칸반 등)는
- 자동 축소에 `zoom`을 쓰지 말고 실제 크기를 줄이는 반응형 값(미디어쿼리/JS 변수)을 쓴다.
- 부모 컨테이너는 `justify-content: center` 대신 `justify-content: flex-start` + 내부 콘텐츠에 `margin: 0 auto`를 쓴다(화면보다 좁으면 가운데, 넘치면 처음부터 끝까지 스크롤 가능).
- 화면을 만들 때마다 좁은 뷰포트로 한 번씩 확인하는 습관이 "한꺼번에 두 가지 버그를 동시에 만나는" 상황 자체를 줄여준다.

### 3-9. 실시간 연동 기능엔 "안 되고 있을 때" 보이는 신호를 같이

Firestore 연결 실패/타임아웃이 콘솔에만 찍히고 화면엔 아무 표시가 없으면, 사용자는 그냥 "고장났다"고만 느끼고 원인을 알 수 없다. **해결**: 구독 시작 시 8초 타임아웃 감시를 걸고, 실패/타임아웃 시 화면 상단에 원인과 대처법(확장 프로그램 끄기, 다른 네트워크 시도 등)을 보여주는 에러 배너를 **기능 본체와 함께 1세트로** 만든다.

### 3-10. 영구 삭제는 Claude가 대신 할 수 없음

`rm`, 휴지통 비우기 등 영구 삭제는 어떤 도구로도 차단되어 있다. 오래된 파일 정리가 필요하면 정확한 경로를 안내하고 사용자가 Finder에서 직접 지우게 한다.

### 3-11. 버그 리포트는 스크린샷 + 증상 한 줄이 가장 빠름

이번 모바일 클리핑 버그도 "왼쪽·오른쪽이 둘 다 잘린 스크린샷" 하나로 원인(가운데 정렬 오버플로우 클리핑)을 바로 좁힐 수 있었다. 코드를 길게 설명하기보다 실제로 보이는 화면을 그대로 보여주는 게 진단 속도를 크게 높인다.

### 3-12. "기억 파일"을 프로젝트에 같이 두기

`문제해결기록.md`(증상/원인/해결 형식의 버그 기록), `인수인계_노트.md`(세션 인수인계용 요약), 그리고 이 가이드 자체가 그 역할이다. 세션이 끊기거나 새 프로젝트로 옮겨도, 이 파일들을 먼저 읽게 하면 처음부터 다시 설명할 필요 없이 바로 이어서 작업할 수 있다.

---

## 4. 재사용 프롬프트 템플릿

### A. 프로젝트 시작
> "OO를 운영하는 웹앱을 만들어줘. (1) 관리자가 입력하는 페이지, (2) 그 결과를 실시간으로 보여주는 공개 대시보드, 이렇게 두 개로 나눠줘. 데이터는 Firebase Firestore로 실시간 동기화하고 Firebase Hosting으로 배포할 거야. 시작하기 전에 먼저 [도메인 규칙]을 CLAUDE.md로 정리해서 루트에 만들어두고, 캐시 방지 헤더가 적용된 로컬 서버 실행용 .command 파일도 같이 만들어줘."

### B. 도메인 규칙 정의
> "다음 규칙을 CLAUDE.md에 정리해줘: [규칙 나열]. 이후 모든 로직 구현은 이 문서를 기준으로 하고, 규칙이 바뀌면 이 문서도 같이 갱신해줘."

### C. 핵심 로직 우선
> "화면보다 먼저 OO 판정/계산 로직을 순수 함수로 만들고, node 기반 단위테스트로 검증해줘. 로직이 안정화된 다음에 화면을 붙이자."

### D. 삭제 기능 요청 시
> "OO를 삭제하는 기능을 만들 때, OO를 참조하던 다른 데이터(예: 소속 팀)는 어떻게 처리할지도 같이 설계해줘. 고아 데이터가 남지 않게 해줘."

### E. 버그 리포트
> "[스크린샷 첨부] 이 화면에서 OO 현상이 발생해. (기기/브라우저), (발생 시점)도 같이 알려줄게. 원인 진단하고 수정해줘. 기존에 고쳤던 관련 버그가 다시 생기지 않는지도 같이 확인해줘."

### F. 검증 요청
> "수정한 다음엔 항상 기존 테스트(node js/test.mjs) 통과 확인하고, 회귀 없는지 같이 확인해줘."

### G. 모바일 점검
> "이 화면을 모바일 뷰포트 기준으로도 확인해줘. 가로로 넓은 콘텐츠(표/트리/타임라인)가 있으면 iOS Safari에서도 깨지지 않는지 확인해줘(zoom 속성은 Safari 미지원이니 쓰지 말 것)."

---

## 5. 빠른 체크리스트

| 단계 | 확인 항목 |
|---|---|
| 0 | 캐시 방지 `.command` 로컬 서버 launcher가 있는가 |
| 0 | `file://`로 직접 열지 않고 `http://localhost`로 여는가 |
| 1 | 도메인 규칙이 `CLAUDE.md`에 문서로 정리돼 있는가 |
| 2 | Firestore 보안 규칙: 쓰기=인증 필요 / 읽기=공개로 시작했는가 |
| 3 | 핵심 판정/계산 로직에 단위테스트가 있는가 |
| 4 | 삭제 기능마다 "참조하던 자식 데이터" 처리를 같이 설계했는가 |
| 5 | 여러 화면에서 쓰이는 정렬/포맷 로직이 한 곳에 통일돼 있는가 |
| 6~7 | 실시간 구독 기능마다 연결 실패/타임아웃 에러 배너가 있는가 |
| 9 | 가로로 넓은 콘텐츠를 모바일(특히 iOS Safari)에서 직접 확인했는가 |
| 상시 | `문제해결기록.md`에 새 버그를 증상/원인/해결 형식으로 계속 쌓고 있는가 |

---

*이 문서는 ver1 완료 시점(2026-06-24)을 기준으로 작성됨. 이후 새로운 시행착오가 생기면 §3에 같은 형식으로 이어서 추가할 것.*
