# 바운스발리볼 인수인계 노트

## 현재 운영 상태

- Firebase 프로젝트: **`bounce-0913-edu-cup-2026`**
- Hosting: `https://bounce-0913-edu-cup-2026.web.app`
- 고정 대회 문서: `tournaments/main` (단일 대회 배포)
- 화면: `admin.html`(관리자), `recorder.html`(기록관), `dashboard.html`(관객)
- 인증: 관리자는 Firebase Authentication Email/Password와 `tournaments/main/admins/{uid}` 멤버십이 모두 필요하다. 기록관은 Google 로그인과 관리자가 발급한 현재 접근 코드 grant가 모두 필요하다.
- 권한 부트스트랩: 신뢰할 수 있는 Firebase Admin SDK 또는 Firebase Console로만 `tournaments/main`과 `tournaments/main/admins/{관리자 UID}`를 만든다. 일반 클라이언트로 관리자 멤버십을 만들거나 수정하지 않는다.

운영 변경은 현재 Firebase 프로젝트와 `main` 경로를 유지해야 한다. 관리자·기록관 권한 데이터와 접근 코드 설정은 보호 데이터이며 백업/복원 대상이 아니다.

## 개발·검사·배포

필수 런타임은 Node.js 22, Java 21, Python 3이다. lockfile을 사용한다.

```bash
npm ci
npm --prefix functions ci
npm run test:unit
npm run test:emulator
npm run test:release
```

macOS에서 Homebrew의 `openjdk@21`이 설치되어 있는데 Java를 찾지 못하면, 설치를 반복하지 말고 현재 터미널의 런타임 경로를 지정한다.

```bash
export JAVA_HOME="$(brew --prefix openjdk@21)/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
java -version
```

에뮬레이터 검사는 `demo-bounce-volleyball` 프로젝트와 로컬 Auth·Firestore·Functions만 사용한다. 실제 대회 데이터로 회귀 테스트나 복원 리허설을 진행하지 않는다.

로컬 정적 서버:

```bash
python3 -m http.server 8765
```

`file://` 직접 열기는 ES module과 인증 흐름에 맞지 않는다. 배포는 다음의 전체 명령으로 Hosting, Functions, Rules, Indexes를 함께 반영한다.

```bash
npx firebase login
npx firebase deploy --project bounce-0913-edu-cup-2026 --only hosting,functions,firestore:rules,firestore:indexes
```

Authentication Console에서는 Email/Password와 Google 공급자를 활성화하고, `bounce-0913-edu-cup-2026.web.app`, `bounce-0913-edu-cup-2026.firebaseapp.com`, `localhost`, 실제 사용자 정의 도메인을 Authorized domains에 등록한다.

## 현재 소스 인벤토리

- `js/firebase-config.js`: 고정 Firebase 웹 설정과 `TOURNAMENT_ID = "main"`.
- `js/auth-service.js`, `js/recorder-auth-service.js`: 관리자 Email/Password 및 기록관 Google+접근 코드 인증.
- `js/firestore-service.js`, `js/workflow-service.js`: 클라이언트 데이터·워크플로 호출 계층.
- `js/backup-format.js`: v3 휴대용 백업 정규화와 민감 정보 제거.
- `functions/restore.js`: 유지보수 lease, manifest, chunk checksum, 검증, 승격으로 구성된 정확 복원.
- `functions/admin-workflow.js`, `functions/workflow-core.js`: 관리자 승인과 코트/점수 워크플로.
- `firestore.rules`, `firestore.indexes.json`, `firebase.json`: 권한·인덱스·배포 구성.
- `test/emulator/`: Auth/Firestore/Functions 통합 경계 검사.

## 백업·복원 운영

관리자 화면에서 백업을 만들고 별도 안전 저장소에 보관한다. 복원은 v3 파일만 허용한다. export는 Firestore Timestamp를 결정적인 JSON 태그로 인코딩하고, restore는 allowlist 태그만 디코딩해 Firestore Timestamp로 쓴다. 체크섬은 인코딩된 표현을 대상으로 한다. 알 수 없는 태그, 비정상 프로토타입, 지원하지 않는 Firestore 값은 거부한다.

복원은 manifest를 단계적으로 생성·적용·오래된 문서 정리·검증·승격한다. 유지보수 상태가 손상되었거나 동시 복원이 감지되면 실패해야 하며, 수동으로 manifest나 보호된 인증 경로를 수정하지 않는다. `admins`, recorder grant, 접근 코드 및 기타 인증 자료는 백업에도 복원에도 포함되지 않는다.

v3 백업의 조·팀·예선에는 `men` 또는 `women` 부문이 명시되어야 한다. 복원 전과 최종 검증에서 팀·조 참조, 예선의 같은 조·부문 소속, 본선의 확정된 팀 참조를 검사한다. 미확정 본선 자리와 부전승의 빈 자리는 허용하지만, 존재하지 않는 팀을 참조하는 백업은 거부한다.

오래된 문서를 삭제하는 정리 단계 직전에도 적용된 복원 대상의 체크섬과 관계 무결성을 재검사한다. 이 검사가 실패하면 삭제를 진행하지 않으며, 유지보수 상태를 임의로 해제하지 않는다.

## 경기·워크플로 요약

예선은 조별 경기와 순위 계산을 거쳐 본선 진출팀을 정한다. 본선 구조는 관리자 초안 후 공개된다. 기록관 점수는 비공개 워크플로에서 작성·제출되며 관리자 승인 결과만 공식 경기와 대시보드에 반영된다. 코트 큐와 점수 워크플로의 소유권·순서는 서버 검증 대상이다.

공식 예선 이력이 생긴 뒤에는 해당 조·팀·예선 초기화나 대진 재생성으로 결과를 지울 수 없다. 오입력은 관리자 `기록·검수` 탭의 `승인 결과 정정`으로 처리한다. 전체 대회 초기화는 점수 정정의 대체 수단이 아니다.

기록관의 임시 저장 응답이 끊기면 서버 초안을 다시 확인한다. 내용이 같으면 저장 완료로 처리하고, 다르면 로컬 초안을 유지하며 `로컬 초안 유지 후 다시 저장` 또는 `서버 초안 사용`으로 선택하게 한다. 기기 저장 실패 경고가 보이면 화면을 닫지 말고 점수를 별도로 기록한다. 데이터 연결 오류는 `대회 데이터 다시 불러오기`로 재시도하며 서버 확인 전에는 저장·제출을 진행하지 않는다.

제출·승인된 워크플로는 현재 기록관 권한이 있는 실제 제출자와 관리자만 조회할 수 있다. 공개 대시보드의 공식 결과 조회에는 영향을 주지 않는다.

## 역사적 참고

이전 문서에 있던 과거 프로젝트 ID, `bbounce-cdb31.web.app`, 익명 운영, 단독 Hosting 배포, 세션 폴더 동기화 문제, 예전 Task 번호와 당시 줄 수·테스트 수는 현재 운영 지침이 아니다. 과거 UX·경기 규칙 변경의 세부 기록은 `문제해결기록.md`와 Git history에서만 역사 자료로 참조한다.
