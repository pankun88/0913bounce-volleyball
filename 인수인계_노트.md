# 바운스발리볼 인수인계 노트

## 현재 운영 상태

- Firebase 프로젝트: **`bounce-0913-edu-cup-2026`**
- Hosting: `https://bounce-0913-edu-cup-2026.web.app`
- 고정 대회 문서: `tournaments/main` (단일 대회 배포)
- 화면: `admin.html`(관리자), `recorder.html`(기록관), `dashboard.html`(관객)
- 2026-09-09 사용자 승인으로 `c20217d`의 코트 전체 교환·기록관 입력 보호·드래그 삽입 위치 안내를 운영 반영했다. 배포 대상은 Hosting과 `replaceCourtWorkflows` 함수이며 Rules·Indexes는 변경하지 않았다. 최종 단위 검사 389개 및 에뮬레이터 통합 검사를 통과했고, 운영 화면·스크립트·스타일 6개가 로컬 소스와 일치함을 SHA-256으로 확인했다. `.git/config`, `.gjc/settings.json`, Functions 소스와 신규 통합 테스트 경로의 운영 응답은 404다. 운영 경기 데이터로 변경 테스트는 실행하지 않았다.
- 2026-09-09 사용자 승인으로 후속 신뢰성 보강 `89465fa`(진출팀 검증·기록관 초안 식별·송출 재연결)를 커밋·푸시하고 운영 Functions 전체, Firestore Rules, Hosting에 반영했다. `correction-review` 미리보기도 같은 코드로 갱신했다. 기본 단위 검사 405개, 별도 회귀 검사, Auth/Firestore/Functions 전체 에뮬레이터 통합 검사와 `git diff --check`를 통과했다. 검증 없는 기존 본선, 본선 시작 후 예선 정정, 부전승만 있는 본선 교체, 조·팀 구성 변경도 검증에 포함한다. 운영 파일 11개의 SHA-256 일치와 내부 경로 5개의 404 응답, 주요 신규·수정 함수의 ACTIVE 상태 및 운영 대시보드의 모든 데이터 연결 정상을 확인했다. 운영 경기 데이터는 변경하지 않았으며 실제 기기·현장 Wi-Fi·운영자 리허설은 별도로 필요하다.
- 이번 Rules 배포에서 미사용 식별자 및 `request`·`get`·`getAfter` 관련 컴파일 경고가 출력됐다. 컴파일과 Rules 게시 자체는 성공했으며 경고를 숨기거나 접근 규칙을 완화하지 않았다.
- 인증: 관리자는 Firebase Authentication Email/Password와 `tournaments/main/admins/{uid}` 멤버십이 모두 필요하다. 기록관은 Google 로그인과 관리자가 발급한 현재 접근 코드 grant가 모두 필요하다.
- 권한 부트스트랩: 신뢰할 수 있는 Firebase Admin SDK 또는 Firebase Console로만 `tournaments/main`과 `tournaments/main/admins/{관리자 UID}`를 만든다. 일반 클라이언트로 관리자 멤버십을 만들거나 수정하지 않는다.

운영 변경은 현재 Firebase 프로젝트와 `main` 경로를 유지해야 한다. 관리자·기록관 권한 데이터와 접근 코드 설정은 보호 데이터이며 백업/복원 대상이 아니다.

## 개발·검사·배포

필수 런타임은 Node.js 22, Java 21, Python 3이다. lockfile을 사용한다.

```bash
npm ci
npm --prefix functions ci
npm run test:unit
npm run test:emulator
npm run test:release
```

macOS에서 Homebrew의 `openjdk@21`이 설치되어 있는데 Java를 찾지 못하면, 설치를 반복하지 말고 현재 터미널의 런타임 경로를 지정한다.

```bash
export JAVA_HOME="$(brew --prefix openjdk@21)/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
java -version
```

에뮬레이터 검사는 `demo-bounce-volleyball` 프로젝트와 로컬 Auth·Firestore·Functions만 사용한다. 실제 대회 데이터로 회귀 테스트나 복원 리허설을 진행하지 않는다.

경기 판정·진출 검증의 작성 원본은 `js/match-logic.js` 하나다. `npm run prepare:shared`가 이를 Functions의 `match-logic.generated.js`로 그대로 복사하며, 단위·에뮬레이터 검사 전과 Functions 배포 전에 자동 실행된다. 생성 파일은 직접 수정하거나 커밋하지 않는다. `node scripts/prepare-shared.mjs --check`로 두 파일의 바이트 일치를 확인한다.

로컬 정적 서버:

```bash
python3 -m http.server 8765
```

`file://` 직접 열기는 ES module과 인증 흐름에 맞지 않는다. 화면 수정은 로컬에서 확인하고, 사용자 검토가 필요할 때 같은 미리보기 채널을 갱신한다. 최종 확인 후 변경사항을 모아 커밋·푸시·운영 배포하며 작은 수정마다 운영 사이트를 갱신하지 않는다. 현재 미리보기는 운영 Firebase에 연결되므로 데이터 변경 테스트는 반드시 에뮬레이터에서 한다.

Hosting 제외 목록에는 `**/.*`와 `**/.*/**`가 모두 필요하다. 전자만으로는 `.gjc` 같은 숨김 디렉터리의 하위 작업 파일이 제외되지 않는다. 배포 파일 목록에 숨김 디렉터리 경로가 없는지 확인한다. 2026-09-07 사용자의 승인으로 운영 반영을 완료했으며, 기존에 노출된 `.gjc/state/sdk/` 내부 파일과 `.git/config`, `.gjc/settings.json`의 운영 응답이 404임을 확인했다. 이후 배포에서도 내부 작업 파일 경로 차단을 유지한다.

운영 반영 승인을 받은 뒤 변경 대상에 맞춰 배포한다. 아래는 Hosting, Functions, Rules, Indexes 전체를 함께 반영하는 명령이다.

```bash
npx firebase login
npx firebase deploy --project bounce-0913-edu-cup-2026 --only hosting,functions,firestore:rules,firestore:indexes
```

Authentication Console에서는 Email/Password와 Google 공급자를 활성화하고, `bounce-0913-edu-cup-2026.web.app`, `bounce-0913-edu-cup-2026.firebaseapp.com`, `localhost`, 실제 사용자 정의 도메인을 Authorized domains에 등록한다.

## 현재 소스 인벤토리

- `js/firebase-config.js`: 고정 Firebase 웹 설정과 `TOURNAMENT_ID = "main"`.
- `js/auth-service.js`, `js/recorder-auth-service.js`: 관리자 Email/Password 및 기록관 Google+접근 코드 인증.
- `js/firestore-service.js`, `js/workflow-service.js`: 클라이언트 데이터·워크플로 호출 계층.
- `js/backup-format.js`: v3 휴대용 백업 정규화와 민감 정보 제거.
- `functions/restore.js`: 유지보수 lease, manifest, chunk checksum, 검증, 승격으로 구성된 정확 복원.
- `functions/admin-workflow.js`, `functions/workflow-core.js`: 관리자 승인과 코트/점수 워크플로.
- `functions/public-schedule.js`: 관객용 공개 일정 생성, 초기 게시와 코트·배정·대회 상태 변경의 실시간 반영.
- `firestore.rules`, `firestore.indexes.json`, `firebase.json`: 권한·인덱스·배포 구성.
- `test/emulator/`: Auth/Firestore/Functions 통합 경계 검사.

## 백업·복원 운영

관리자 화면에서 백업을 만들고 별도 안전 저장소에 보관한다. 복원은 v3 파일만 허용한다. export는 Firestore Timestamp를 결정적인 JSON 태그로 인코딩하고, restore는 allowlist 태그만 디코딩해 Firestore Timestamp로 쓴다. 체크섬은 인코딩된 표현을 대상으로 한다. 알 수 없는 태그, 비정상 프로토타입, 지원하지 않는 Firestore 값은 거부한다.

복원은 manifest를 단계적으로 생성·적용·오래된 문서 정리·검증·승격한다. 유지보수 상태가 손상되었거나 동시 복원이 감지되면 실패해야 하며, 수동으로 manifest나 보호된 인증 경로를 수정하지 않는다. `admins`, recorder grant, 접근 코드 및 기타 인증 자료는 백업에도 복원에도 포함되지 않는다.

v3 백업의 조·팀·예선에는 `men` 또는 `women` 부문이 명시되어야 한다. 복원 전과 최종 검증에서 팀·조 참조, 예선의 같은 조·부문 소속, 본선의 확정된 팀 참조를 검사한다. 미확정 본선 자리와 부전승의 빈 자리는 허용하지만, 존재하지 않는 팀을 참조하는 백업은 거부한다.

오래된 문서를 삭제하는 정리 단계 직전에도 적용된 복원 대상의 체크섬과 관계 무결성을 재검사한다. 이 검사가 실패하면 삭제를 진행하지 않으며, 유지보수 상태를 임의로 해제하지 않는다.

송출 설정 저장과 대시보드 자동 전환에서는 서버 시계 호출을 사용하지 않는다. `getServerClock` 공개 함수는 `connection-test.html`의 Functions 연결 진단에 사용하므로 유지한다.

## 경기·워크플로 요약

예선은 조별 경기와 순위 계산을 거쳐 본선 진출팀을 정한다. 본선 구조는 관리자 초안 후 공개된다. 기록관 점수는 비공개 워크플로에서 작성·제출되며 관리자 승인 결과만 공식 경기와 대시보드에 반영된다. 코트 큐와 점수 워크플로의 소유권·순서는 서버 검증 대상이다.

본선 진출은 해당 부문의 모든 참가 조에서 예정 대진과 공식 결과가 완비된 뒤 확정한다. 자동 진출자는 임의로 제외할 수 없고, 진출선에 걸친 동률 후보의 남은 자리만 추첨 결과에 따라 선택한다. 다자 동률의 승자승은 기존 동률팀 간 승수 기준을 유지하며 새 규칙으로 바꾸지 않는다. 본선 공개 시 서버가 예선 입력 지문과 실제 참가자 전체(미배치 부전승 후보 포함)를 다시 검증하므로, 화면을 연 뒤 예선이 바뀌면 다시 확인해야 한다. 조별 진출 인원 변경도 서버 명령을 사용한다.

조·팀 생성과 소속·대진 방식·링 배치 변경도 서버에서 실제 경기 문서를 확인한다. 예전 공개 대진이나 복원 대진에 검증 표시가 없다는 이유로 구성을 자유롭게 바꿀 수는 없다. 이름과 표시 순서 수정은 별도로 허용하며, 구성 변경 때문에 공식 점수나 진행 중인 기록을 지우지 않는다.

예선 정정으로 오래된 본선 초안을 자동 삭제하거나 재생성하지 않는다. 실제 본선 기록이 없는 공개 대진은 재확인 대상으로 표시하며, 명시적인 확인·사유를 거쳐 다시 공개하거나 미시작 대진을 교체한다. 교체 전 대진은 감사 이력에 남는다. 실제 본선 입력·경기 이력이 있으면 진출자가 달라지거나 예선이 미완료가 되는 정정은 먼저 차단한다. 기록을 유지한 채 유효한 명단을 재검증하는 것과 대진 재생성은 별도 동작이다. 이때 전체 초기화를 해결책으로 사용하지 않고 대회본부 판단과 백업을 우선한다.

`finalQualification`은 서버 검증 결과이므로 백업 파일의 신뢰 근거로 사용하지 않는다. 복원 시 이전 대회의 검증 표시는 지우고 경기·점수·감사 이력은 기존 복원 방식대로 보존한다. 복원된 본선은 명단 재확인 후 입력을 이어간다. 진출 보호 변경은 새 Functions와 Rules 및 관리자·기록관·관객 화면을 함께 반영해야 하므로, 서버 미배포 상태에서 새 화면만 운영 또는 공유 미리보기에 배포하지 않는다.

관객용 경기장 화면은 관리자 `경기장 화면 열기` 또는 `dashboard.html?display=venue`로 연다. 자동 모드는 남자부·여자부를 한 부문씩 크게 표시하며 기본 15초 간격으로 교대한다(10·15·20·30초 설정 가능). 현재 부문·다음 부문·전환까지 남은 시간을 표시하며, 선택한 예선/본선 탭은 유지한다. 부문별 전용 화면에는 남자부 또는 여자부 고정 송출을 사용한다. 송출 설정은 모든 경기장 화면에 공통 적용되며, 자동 전환 타이머는 각 화면에서 독립적으로 동작하므로 여러 화면의 전환 순간은 일치하지 않을 수 있다. 자동 전환은 서버 시간 조회에 의존하지 않지만 공식 결과 갱신에는 Firebase 연결이 필요하다. 일반 대시보드는 관객이 부문을 직접 선택한다.

관리자의 `통합 경기 배정·순서`는 `전체 / 예선 / 본선`으로 표시를 구분한다. 승인 완료된 경기는 각 코트 아래의 접힌 `완료 경기`에서 조회한다. 재입력·검수·입력 중인 경기는 과거 승인 이력이 있어도 완료 목록에 숨기지 않는다. 표시 필터와 접기는 실제 배정·순서나 저장할 전체 경기 목록을 바꾸지 않는다. 숨겨진 경기가 있으면 드래그 정렬을 제한하며, 화살표는 실제 순서에서 바로 이웃하고 화면에 표시된 미완료 경기 사이에서만 사용할 수 있다.

경기 카드를 드래그할 때 대상 카드의 위·아래 절반에 따라 앞·뒤 삽입 위치와 코트 내 순서를 안내한다. 안내한 위치와 실제 드롭 위치는 같은 기준을 사용하며, 마우스를 움직이는 동안 배정 초안은 바꾸지 않는다. 드롭 후 이동한 카드를 잠시 강조하고, 취소·영역 이탈·화면 갱신 시 남은 위치 표시는 제거한다. 빈 코트·미배정 이동도 같은 방식으로 안내한다. 동작 줄이기 설정에서는 움직임 효과를 제한한다. 드롭은 미저장 초안 변경이며 기존 저장 버튼으로 확정한다.

코트 전체 교환은 두 코트에 배정된 예선·본선의 모든 경기를 진행 중·검수 중·완료 여부나 화면 필터와 관계없이 순서 그대로 맞바꾼다. 코트 이름과 담당 기록관은 바꾸지 않으며, 공식 점수·제출 기록·서버 저장 초안을 유지한다. 교환은 관리자 화면에서 확인한 뒤 기존 배정·순서 저장으로 확정한다. 저장 시 이동한 경기의 기존 입력권은 해제되므로 기록관은 새 코트에서 입력권을 다시 확보해야 한다. 교환 전에 기록관에게 초안 저장을 안내한다. 기기에만 남은 미저장 입력은 다른 기록관에게 자동 전달되지 않는다. 서버는 전체 교환 여부와 최신 배정·대기열 버전을 검증하며, 일반 개별 배정 변경으로 진행 중 경기의 입력권 보호를 우회할 수 없다. 이 기능은 관리자 화면과 Functions 변경을 함께 배포해야 하며, 운영 배포는 별도 승인 후 진행한다.

예선 생성 목록과 관리자 예선 탭도 통합 배정의 코트별 진행 순서를 표시한다. `대진 N`은 경기 식별 번호이며 `A코트 · 전체 5번째`는 다른 조·부문·본선을 포함한 해당 코트의 전체 배정 위치다. 도형의 `A·5`도 같은 위치를 뜻한다. 코트가 다른 경기 사이에는 전체 공통 순서를 부여하지 않고, 미배정 경기는 별도로 표시한다. 저장 전에는 두 예선 화면에도 미저장 코트 계획이 표시되며, 기존 코트 배정·순서 저장으로 확정한다. 진행 순서 변경은 통합 배정 화면으로 일원화하고 예선 목록의 드래그 정렬은 사용하지 않는다. 도형의 팀 배치는 대진 상대를 바꾸는 기능이므로 진행 순서를 바꾸려고 팀을 재배치하지 않는다.

관객 대시보드는 저장된 일정만 `tournaments/main/publicSchedule/current` 문서로 구독한다. 공개 문서에는 코트 ID·이름과 경기 키·종류·배정 코트·코트 내 순서만 포함한다. 기록관 이름·권한·잠금·초안·점수는 복사하지 않으며 원본 코트·배정 문서의 접근 제한은 유지한다. 관리자 화면의 미저장 배정은 관객에게 전달되지 않고, 경기 결과·순위는 기존 공개 공식 경기 문서의 승인된 점수로 계산한다.

관객용 대시보드는 예선에서 큰 조별 순위표와 경기별 승·무·패 및 두 세트 점수를 우선 표시하며, 링 도형은 관리자 화면에서만 사용한다. 본선은 기존 대진표를 유지한다. 예선·본선 모두 스크롤 없는 전체 개요 화면으로 현재 부문의 모든 조·경기·대진표를 헤더와 송출 상태 표시를 제외한 실제 화면 공간에 비율을 유지해 맞춘다. `overflow: hidden`만 적용해 결과를 잘라내거나, 특정 모니터 해상도에서만 맞추는 방식은 허용하지 않는다. 창 크기·부문·탭·경기 데이터·전체화면 상태가 바뀌면 다시 맞춘다. 브라우저 전체화면 진입은 사용자가 `전체화면` 버튼으로 실행하며, 자동 진입은 브라우저의 사용자 동작 제한 때문에 보장할 수 없다.

실시간 구독이 오류로 종료되면 제한된 간격으로 다시 구독한다. 이전 구독의 늦은 응답은 무시하며 화면 종료 시 재시도도 해제한다. 일반 오프라인 상태에는 마지막 서버 확인 결과와 연결 경고를 유지하지만, 대회 상태 구독 자체가 종료되면 점검 여부를 확인할 수 없어 결과 영역을 가린다. 서버에서 새 대회 상태를 확인해야 다시 표시한다. 본선 검증이 없거나 오래되면 대진 위에 `본선 진출팀 재확인 필요 · 이전 공개 대진`을 표시한다.

공개 일정은 서버가 저장된 원본을 다시 읽어 생성하는 파생 데이터다. 코트·배정 변경 및 대회 유지보수 상태 변경 트리거로 갱신되므로 저장 직후 짧은 반영 지연은 있을 수 있다. 최초 공개 문서가 없으면 `ensurePublicSchedule` 호출로 기존 저장 일정을 초기 게시하며 재저장할 필요는 없다. 유지보수 중에는 빈 일정과 점검 상태를 게시하고, 복원 완료 후 현재 원본으로 다시 생성한다. 공개 일정은 백업·복원 대상에 추가하지 않는다. 이 기능을 미리보기에서 사용하려면 공개 일정용 Functions와 해당 단일 문서의 읽기 규칙도 함께 배포되어야 한다. 미리보기는 운영 Firebase를 공유하므로 서버 배포는 별도로 승인을 받은 뒤 진행한다.

기록관의 코트 목록은 관리자 설정 순서로 정렬된다. 코트와 해당 코트에 배정된 이름을 선택하고 `경기 목록 보기`를 누르면 별도 운영 화면으로 이동한다. 이 화면은 해당 코트의 전체 배정 경기를 순서대로 보여주며, 완료·검토 중·대기 상태와 서버가 지정한 현재·다음 경기를 구분한다. 순서 목록의 인접한 행이 아니라 서버의 현재 경기만 점수를 입력할 수 있다.

기록관끼리의 신뢰를 전제로 공용 접근 코드와 코트·이름 선택 방식을 유지한다. 계정별 코트 강제 배정이나 추가 인증 단계는 도입하지 않는다. 코트 교대 전 현재 입력을 마치고, 제출 확인 화면의 코트·양 팀·세트 점수를 확인한다. 공식 반영은 기존 관리자 승인 절차를 따른다.

`점수 입력`은 입력권 확보에 성공한 뒤 점수 입력 영역으로 자동 스크롤한다. 이미 입력 중인 경기로 이동할 때도 동일하게 작동하며, 제목에 포커스를 두어 모바일 키보드를 자동으로 열지 않는다. 입력 시작 실패나 경기 변경 시에는 이동하지 않고, 동작 줄이기 설정을 존중한다.

입력권 상태는 서버의 제출·입력 종료·초안 폐기 결과와 대조한다. 정상적인 입력권 해제를 실제 권한 상실로 경고하지 않으며, 캐시나 종료된 이전 입력권의 늦은 갱신 응답으로 현재 입력권을 취소하지 않는다. 실제 권한 상실 시에는 로컬 초안을 보관하고, 기기 저장에 실패하면 화면을 닫지 말라는 경고를 표시한다.

기기 초안은 경기 키·부문·양 팀 ID·공식 수정 버전·전이 식별자로 원래 경기를 확인한다. 코트와 표시 이름은 동일 경기 판단에서 제외하므로 코트 교환이나 팀 이름 수정만으로 복구가 깨지지 않는다. 같은 경기·버전의 초안은 그대로 이어받고, 다른 경기 또는 식별 정보 없는 옛 초안은 새 경기에 자동 적용하지 않는다. 보존된 초안과 현재 팀·점수를 비교할 수 있으며, 저장 공간이 막히면 화면을 닫지 않고 별도 기록한다. 늦게 도착한 저장·제출 응답이 새 경기의 초안을 지우지 않도록 원래 요청의 식별 정보를 유지한다.

제출 전에는 좌우 팀 이름과 세트별 점수를 표로 확인한다. 제출이 완료되면 같은 코트의 목록과 다음 경기가 자동 갱신된다. 다른 코트·이름은 입력을 마친 뒤 `코트 변경`으로 선택 화면에 돌아가 변경한다. 작성 중이거나 제출 확인이 남아 있으면 코트를 바꿀 수 없으며, 입력 종료와 초안 폐기는 접힌 `입력 관리`에 있다.

공식 예선 이력이 생긴 뒤에는 해당 조·팀·예선 초기화나 대진 재생성으로 결과를 지울 수 없다. 일반적인 점수 오입력은 해당 경기의 `점수 수정`에서 처리한다. 기록관에게 다시 입력하도록 요청할 때만 관리자 `기록·검수` 탭의 접힌 `추가 관리 기능`을 열어 `승인 취소 · 점수 재입력`을 사용한다. 같은 코트의 경기 카드를 선택하고 `변경 내용 확인`으로 취소 대상 점수와 처리 전후 경기 순서를 확인한 뒤, 사유와 확인 체크를 입력해 요청한다.

이 기능은 기존 공식 점수를 철회하고 점수 재입력 대기로 돌리는 기능이며, 이 화면에서 새 점수를 수정하거나 실제 경기를 다시 치르도록 지시하는 기능이 아니다. 관객 화면의 기존 결과도 철회되고, 본선은 연결된 다음 대진에 영향을 줄 수 있다. 선택 또는 경기 상태가 바뀌면 변경 내용을 다시 확인해야 한다. 전체 대회 초기화는 점수 정정의 대체 수단이 아니다.

기록관의 임시 저장 응답이 끊기면 서버 초안을 다시 확인한다. 내용이 같으면 저장 완료로 처리하고, 다르면 로컬 초안을 유지하며 `로컬 초안 유지 후 다시 저장` 또는 `서버 초안 사용`으로 선택하게 한다. 기기 저장 실패 경고가 보이면 화면을 닫지 말고 점수를 별도로 기록한다. 데이터 연결 오류는 `대회 데이터 다시 불러오기`로 재시도하며 서버 확인 전에는 저장·제출을 진행하지 않는다.

제출·승인된 워크플로는 현재 기록관 권한이 있는 실제 제출자와 관리자만 조회할 수 있다. 공개 대시보드의 공식 결과 조회에는 영향을 주지 않는다.

## 역사적 참고

이전 문서에 있던 과거 프로젝트 ID, `bbounce-cdb31.web.app`, 익명 운영, 단독 Hosting 배포, 세션 폴더 동기화 문제, 예전 Task 번호와 당시 줄 수·테스트 수는 현재 운영 지침이 아니다. 과거 UX·경기 규칙 변경의 세부 기록은 `문제해결기록.md`와 Git history에서만 역사 자료로 참조한다.
