# 바운스발리볼 운영·인수인계 노트

이 문서는 **현재 운영 절차와 주의사항**을 정리합니다. 접속 주소와 문서 목록은 [README](README.md), 경기 규칙은 [AGENTS.md](AGENTS.md), 설치·검사·배포 명령은 [개발 가이드](개발_워크플로우_가이드.md), 과거 해결 과정은 [문제해결 기록](문제해결기록.md)을 참고합니다.

## 1. 운영 기준과 최근 배포

| 항목 | 기준 |
|---|---|
| Firebase 프로젝트 | `bounce-0913-edu-cup-2026` |
| 대회 문서 | `tournaments/main` — 고정 단일 대회 |
| 관리자 | Email/Password 로그인 + `tournaments/main/admins/{uid}` 멤버십 |
| 기록관 | Google 로그인 + 현재 대회 접근 코드로 발급받은 유효한 grant |
| 관객 | 공개 공식 결과와 공개 일정 조회 |
| 미리보기 | `correction-review` 채널. 운영 Firebase를 공유하며 별도 테스트 DB가 아님 |

### 2026-09-09 배포 확인 기록

- 구현 커밋 `89465fa`, 배포 기록 커밋 `8c39f17`이 `origin/main`에 반영되었습니다.
- 진출팀 검증·기록관 초안 식별·송출 재연결을 Functions 전체, Firestore Rules, Hosting에 배포했고 미리보기도 갱신했습니다. 코트 전체 교환·드래그 삽입 안내도 현재 배포에 포함됩니다.
- 당시 기본 단위 검사 405개, 별도 회귀 검사, 전체 Auth/Firestore/Functions 에뮬레이터 통합 검사, 공유 판정 모듈의 바이트 일치 및 `git diff --check`를 통과했습니다.
- 운영 파일 11개의 SHA-256 일치, 내부 경로 5개의 404 응답, 주요 함수 ACTIVE 상태, 대시보드의 모든 데이터 연결 정상을 확인했습니다. 운영 경기 데이터로 변경 테스트를 하지 않았습니다.
- Rules 컴파일에서 미사용 식별자 및 `request`·`get`·`getAfter` 관련 경고가 남았습니다. 컴파일과 게시 자체는 성공했으며 경고를 숨기거나 규칙을 완화하지 않았습니다.

위 내용은 해당 배포 당시의 검증 기록입니다. **실제 기기·현장 Wi-Fi·운영자 리허설까지 완료했다는 뜻은 아닙니다.** 후속 변경은 별도로 검증해야 합니다.

## 2. 대회 시작 전 확인

1. 관리자 로그인과 관리자 멤버십을 확인합니다. 일반 클라이언트에서 관리자 권한 문서를 만들거나 수정하지 않습니다.
2. 남자부·여자부의 팀, 조, 경기 방식, 예선 대진을 확인합니다.
3. 코트 이름·표시 순서·담당 기록관 이름과 저장된 경기 배정을 확인합니다.
4. 기록관에게 현재 접근 코드를 전달하고 실제 기기에서 로그인·코트 선택을 확인합니다. 기록관끼리의 신뢰를 전제로 하며 계정별 코트 강제 배정은 하지 않습니다.
5. 관객 화면의 부문·예선/본선 탭·연결 상태·전체화면을 확인합니다.
6. 현재 JSON 백업을 내려받아 별도 안전 저장소에 보관합니다. 데이터 변경을 포함한 연습은 운영·미리보기가 아닌 에뮬레이터에서 진행합니다.

배포 전에 열어 둔 탭은 작성 중인 점수를 먼저 보존한 뒤 새로고침합니다. 새 파일이 배포되어도 이미 열린 화면의 코드가 자동으로 교체되지는 않습니다.

## 3. 관리자 경기 운영

### 기본 흐름

대회 설정 → 팀·조 등록 → 예선 대진 생성 → 코트 배정·순서 저장 → 점수 입력·검수 → 예선 완료 → 본선 진출팀 확인 → 대진 생성·공개 순서로 진행합니다.

기록관의 작성·제출 점수는 비공개 워크플로에 저장됩니다. 관객에게는 관리자가 공식 반영한 결과만 표시합니다. 예선 점수 수정과 본선 점수 공개는 각 화면의 절차를 사용하며 Firestore 문서를 직접 수정하지 않습니다.

### 계정별 기록관 접근 권한

- `기록·검수`의 계정별 접근 권한은 로그인 계정의 이메일·이름으로 식별합니다. 코트에 배정한 기록관 이름과는 별개이며, 내부 식별번호인 UID는 계정 상세에서 확인·복사합니다.
- 활성 권한을 기본 목록에 표시하고 취소·만료·이전 코드·전체 비활성 권한은 접힌 이력으로 분리합니다. 이력은 20개씩 더 볼 수 있습니다. 서버의 100개 단위 페이지를 끝까지 조회한 뒤 목록을 표시하며, 조회 실패를 전체 목록이나 빈 목록으로 표시하지 않습니다.
- 이메일·이름은 관리자 전용 서버 조회에서 Firebase Authentication의 현재 계정 정보를 확인합니다. 예전 권한도 다시 발급할 필요가 없으며, 로그인 계정이 삭제되었거나 이메일이 없는 경우 이를 표시합니다. 관객 공개 데이터에는 계정 정보를 추가하지 않습니다.
- `접근 권한 취소`는 해당 계정의 기록관 접근만 취소합니다. Google 로그인 계정 자체, 다른 기록관의 권한, 입력 중인 초안·잠금을 삭제하지 않습니다.
- 취소된 권한은 같은 버전의 접근 코드로 재발급받지 못하도록 보존합니다. 목록 정리를 위해 권한 문서를 직접 삭제하지 않습니다. 새 접근 코드 발급 후에는 해당 계정도 새 코드로 권한을 받을 수 있으므로, 코드 재발급을 영구적인 계정 차단으로 해석하지 않습니다.
- 계정별 권한은 현재 상태를 보여주며 같은 계정의 재발급은 기존 항목을 갱신합니다. 접힌 이력은 모든 과거 발급을 누적한 감사 로그가 아닙니다.

### 조·팀 구성 변경

- 조·팀 생성, 소속 변경, 경기 방식과 링 배치는 서버 명령으로 검증합니다. 이름·표시 순서 변경은 별도로 허용합니다.
- 해당 부문에 공개 본선이 있으면 구성 변경이 제한됩니다. 예전 대진이나 복원 대진에 검증 표시가 없더라도 보호를 우회할 수 없습니다.
- 공식 예선 이력이나 진행 중 입력이 있는 조·팀은 구성 변경·초기화·대진 재생성으로 기록을 지우지 않습니다. 점수 오입력은 점수 정정 절차로 처리합니다.

### 통합 경기 배정·순서

- `전체 / 예선 / 본선` 필터와 코트별 `완료 경기` 접기는 표시만 바꿉니다. 저장 대상인 전체 배정·순서는 바뀌지 않습니다.
- 검수·재입력·입력 중인 경기는 과거 승인 이력이 있어도 완료 목록에 숨기지 않습니다. 숨겨진 경기가 있는 경우 드래그를 제한하고, 화살표는 실제로 이웃하며 화면에 표시된 미완료 경기 사이에서만 사용할 수 있습니다.
- 드래그는 대상 카드의 위·아래 절반에 따라 앞·뒤 삽입 위치를 안내합니다. 드롭은 **미저장 초안 변경**이며 저장 버튼으로 확정합니다.
- 운영상 한 라운드에 여러 코트를 동시에 사용합니다. 화면에 보이는 경기 순서는 `1코트 · 5라운드`처럼 해당 코트에 배정된 위치를 기준으로 표시합니다. 링크제 도형에서는 `1코트 - 5라운드`로 표시하며, 코트 이름이 숫자여도 `1·5`처럼 단위를 생략하지 않습니다. 배정되지 않은 도형의 경기는 `미배정`으로 표시합니다. 코트 간 경기의 시작·종료 시각 동기화를 앱에서 강제하지 않으며 서로 다른 코트 사이에는 공통 진행 순서가 없습니다. 구조상 생성 순서인 내부 `round`는 이 표시용 코트 순번과 별개이며 전역 고유 경기 식별자로 쓰이지 않고, 진출 검증 fingerprint에는 포함됩니다.
- 예선 도형에서 팀 위치를 바꾸는 것은 대진 상대를 바꾸는 동작입니다. 경기 진행 순서 변경은 통합 배정 화면을 사용합니다.

### 코트 전체 교환

두 코트에 배정된 **모든 예선·본선 경기**를 코트 내부 순서 그대로 맞바꿉니다. 입력 중·검수 중·완료 경기와 화면에서 숨긴 경기도 포함하며, 코트 이름과 담당 기록관 이름은 바꾸지 않습니다.

공식 점수·제출 기록·서버 초안은 보존합니다. 저장 시 이동한 경기의 기존 입력권이 해제되므로 기록관은 새 코트에서 입력권을 다시 확보해야 합니다. 교환 전에 초안을 저장하도록 안내하세요. 기기에만 남은 미저장 입력은 다른 기기로 자동 전달되지 않습니다.

### 본선 진출·공개

- 해당 부문의 모든 참가 조에서 예정 대진과 현재 공식 예선 결과가 완비되어야 합니다.
- 자동 진출팀은 임의로 제외할 수 없습니다. 추첨 선택은 **진출선 동률 후보의 남은 자리**에만 적용하고 기록을 남깁니다.
- 생성 시 확인한 예선 상태와 공개 시 서버 상태가 다르면 재확인이 필요합니다. 조별 진출 인원 변경도 서버가 검증합니다.
- 경기 규칙과 다자 동률의 승자승 기준은 [AGENTS.md](AGENTS.md)를 따릅니다.

| 상황 | 처리 |
|---|---|
| 예선이 미완료이거나 추첨 선택이 미완료·초과 | 본선 생성·공개 전 해당 원인을 해결 |
| 예선 정정으로 기존 초안이 오래됨 | 대진·점수 초안을 자동 삭제하지 않고 보존한 채 재확인 |
| 실제 본선 입력·경기 이력이 없는 공개 대진 | 명시적으로 재확인·재공개. 참가자를 바꿔야 하면 사유를 입력해 미시작 대진 교체 |
| 부전승 확정만 있는 대진 | 부전승만으로 실제 경기가 진행된 것으로 보지 않음. 미시작 교체 경로 사용 가능 |
| 실제 본선 진행 이력이 있음 | 진출팀이 바뀌거나 예선이 미완료가 되는 정정은 차단. 유효한 동일 명단의 정정·재검증은 기록을 보존 |
| 기존·복원 본선에 검증 정보가 없음 | 현재 예선 자격과 실제 본선 명단을 재검증한 뒤 입력 재개. 검증 정보 부재를 새 대회로 취급하지 않음 |

미시작 교체 전 대진과 관련 상태는 감사 이력에 보존합니다. 명단 재검증과 대진 재생성은 다른 동작이며, 전체 초기화를 정정의 대체 수단으로 사용하지 않습니다.

### 점수 오입력과 승인 취소

일반적인 예선 오입력은 해당 경기의 `점수 수정`으로 처리합니다. 기록관에게 다시 입력하도록 요청할 때만 `기록·검수`의 `추가 관리 기능`에서 `승인 취소 · 점수 재입력`을 사용합니다.

1. 같은 코트의 대상 경기를 선택합니다.
2. `변경 내용 확인`에서 철회할 점수와 처리 전후 경기 순서를 확인합니다.
3. 사유와 확인 체크를 입력한 뒤 적용합니다. 선택이나 경기 상태가 바뀌면 다시 확인합니다.

이 기능은 기존 공식 결과를 철회해 재입력 대기로 돌리는 기능입니다. 그 자리에서 새 점수를 수정하거나 실제 경기를 다시 치르라는 뜻이 아닙니다. 관객 화면에서도 결과가 철회되고 본선 후속 대진에 영향을 줄 수 있습니다. 실제 본선 진행 후의 예선 철회는 진출 보호에 의해 차단됩니다.

## 4. 기록관 입력과 복구

### 정상 입력

1. Google 로그인 후 현재 접근 코드를 입력합니다.
2. 코트와 배정된 이름을 선택하고 `경기 목록 보기`로 들어갑니다.
3. 서버가 지정한 현재 경기에서 입력권을 확보합니다. 목록에서 바로 다음에 보이는 경기가 반드시 서버의 다음 경기는 아닙니다.
4. 점수를 입력하고 제출 전 좌우 팀·세트 점수를 확인합니다. 제출 후 같은 코트의 목록과 다음 경기가 갱신됩니다.
5. 코트·이름 변경은 입력을 마친 뒤 `코트 변경`을 사용합니다. 입력 종료·초안 폐기는 접힌 `입력 관리`에 있습니다.

기록관 Google 로그인은 같은 브라우저·사이트에서 탭이나 브라우저를 닫았다가 열어도 유지합니다. 변경 적용 후 한 번 로그인하면 로컬 저장 방식이 적용됩니다. 로그아웃하거나 브라우저의 사이트 데이터를 삭제한 경우, 시크릿 모드를 종료한 경우에는 다시 로그인해야 합니다. 공용 기기는 사용 후 반드시 로그아웃합니다. 관리자 로그인은 기존 세션 방식을 유지합니다.

로그인 유지와 대회 접근 권한·경기 입력권은 별개입니다. 대회 접근 권한은 발급 후 12시간이 지나거나 관리자가 코드를 재발급·폐기하면 다시 확인해야 하며, 취소된 계정은 같은 코드로 재진입할 수 없습니다. 경기 입력권은 3분 동안 유효하고 화면 활성 상태에서 갱신합니다. 입력권이 만료되면 해당 경기의 입력을 재개해야 하지만 Google 로그인을 다시 할 필요는 없습니다.

입력권 확보 후 점수 영역으로 이동하지만 모바일 키보드를 강제로 열지는 않습니다. 정상적인 제출·입력 종료를 권한 상실로 처리하지 않으며, 이전 요청의 늦은 응답으로 새 입력을 초기화하지 않습니다.

### 초안·연결 문제

| 상황 | 처리 |
|---|---|
| 같은 경기·공식 버전의 기기 초안 | 자동으로 이어받음 |
| 같은 팀·경기이지만 공식 버전이 바뀜 | 이전 초안과 현재 상태를 확인한 뒤 복구 여부 판단 |
| 팀·부문·경기 종류가 다르거나 식별 정보가 없는 옛 초안 | 새 경기에 자동 적용하지 않음. 이전 초안을 별도로 보존하고 팀·점수를 비교 |
| 저장 응답이 끊김 | 서버 초안을 조회. 같으면 저장 완료, 다르면 로컬 유지·다시 저장 또는 서버 초안 사용 선택 |
| 실제 입력권 상실 | 로컬 초안 보존 후 현재 입력권·경기 상태 재확인 |
| 기기 저장 실패 | 화면을 닫지 말고 점수를 별도 기록 |
| 데이터 연결 오류 | `대회 데이터 다시 불러오기`로 재시도. 서버 확인 전 저장·제출 중단 |

경기 식별에는 경기 키·종류·부문·양 팀 ID·공식 버전·전이 식별자를 사용합니다. 코트와 표시 이름은 비교에서 제외하므로 코트 교환·팀 이름 수정만으로 다른 경기로 취급하지 않습니다.

제출·승인된 비공개 워크플로는 현재 기록관 권한을 가진 실제 제출자와 관리자만 조회합니다. 공개 공식 결과의 조회 권한과는 별개입니다.

## 5. 관객 화면과 송출

- 예선은 조별 순위표와 경기별 승·무·패, 두 세트 점수를 표시합니다. 링 도형은 관리자 화면용입니다. 본선은 대진표로 표시합니다.
- 현재 부문의 결과를 화면 영역에 비율을 유지해 맞춥니다. 과거의 가로 스크롤 중심 대시보드 설명은 현재 지침이 아닙니다.
- 일반 대시보드는 관객이 부문을 선택합니다. 첫 화면(`index.html`)과 관리자 상단·송출 설정의 `관객 대시보드 열기`는 같은 `dashboard.html?display=venue&tab=prelim` 경로에서 저장된 자동 교대·고정 설정을 사용합니다. 저장된 설정과 무관하게 부문을 직접 고르려면 `수동 관람 화면 열기`(`dashboard.html`)를 사용합니다.
- 경기장 자동 모드는 남자부·여자부를 교대합니다. 기본 15초이며 10·15·20·30초를 선택할 수 있습니다. 이 15초는 부문 교대 간격이며 송출 지연이 아닙니다. 부문 고정 송출도 가능합니다. 선택한 예선/본선 탭은 유지합니다.
- 송출 설정은 공통이지만 각 화면의 타이머는 독립적이므로 여러 화면의 전환 순간이 완전히 일치한다고 보장하지 않습니다. 전체화면은 사용자가 버튼으로 실행합니다.

### 연결 상태와 재확인 경고

- 일반 오프라인·개별 피드 오류에는 마지막 서버 확인 결과와 연결 경고를 유지합니다. 종료된 공용 구독은 제한된 간격으로 재연결하며 오래된 응답과 취소 후 재시도를 무시합니다.
- **대회 상태 구독 자체가 종료되면** 점검 여부를 확인할 수 없어 결과 영역을 가립니다. 캐시만으로 다시 열지 않고 서버 확인 후 복구합니다.
- 공개 본선의 진출 검증이 없거나 오래되면 `본선 진출팀 재확인 필요 · 이전 공개 대진`을 표시합니다. 연결 오류와 다른 안내이며 관리자 명단 재검증이 필요합니다.

### 공개 일정의 범위

`tournaments/main/publicSchedule/current`는 저장된 코트·경기 배정만 담는 파생 문서입니다. 기록관 이름·권한·잠금·초안·점수는 복사하지 않으며, 경기 결과는 별도의 공개 공식 경기 문서에서 표시합니다. 관리자 화면의 미저장 배정은 관객에게 전달되지 않습니다.

트리거 반영에 짧은 지연이 있을 수 있습니다. 최초 공개 일정이 없으면 `ensurePublicSchedule`로 초기 게시합니다. 유지보수 중에는 빈 일정·점검 상태를 게시하고 복원 완료 후 현재 원본으로 재생성합니다. 송출 자동 전환은 서버 시계 호출에 의존하지 않습니다.

## 6. 백업·복원

### 보관과 파일 형식

- JSON 백업은 복원용이며, CSV는 결과 자료용입니다. CSV를 JSON 백업의 대체물로 취급하지 않습니다.
- 일반 내보내기와 서버 복원 형식은 **v3**입니다. Firestore Timestamp는 허용된 JSON 태그로 인코딩하며 알 수 없는 태그·비정상 값은 거부합니다.
- 관리자 화면은 **초기 v1 백업을 v3으로 변환하는 경로**도 제공합니다. 파일명·대회명에서 부문을 판별하거나 `초기 버전(v1) 백업의 경기 부문`에서 직접 선택합니다. 확정할 수 없으면 진행하지 않습니다.
- **v1 변환도 선택 부문에 추가하는 작업이 아닙니다. 복원은 현재 대회 데이터를 백업 내용으로 대체합니다.** 대상 부문과 확인 문구를 읽고 현재 백업부터 별도로 보관합니다.

### 복원 경계

- 관리자 권한과 유지보수 잠금 아래에서 manifest·청크 적용 → 오래된 문서 정리 → 검증 → 승격 순서로 진행합니다.
- 조·팀·경기·공식 수정 이력·코트·배정·큐·점수 워크플로·감사 이력 등 백업 대상 데이터를 복원합니다. 기기에만 남은 미저장 입력은 서버 백업으로 보호되지 않습니다.
- 관리자 멤버십, 기록관 grant·접근 코드와 인증 정보는 백업·복원 대상이 아닙니다.
- 부문, 팀·조 참조, 예선 같은 조·부문 소속, 본선 팀 참조와 체크섬을 검사합니다. 미확정 자리·부전승 빈자리는 허용하지만 존재하지 않는 팀 참조는 거부합니다.
- 기존 문서를 지우기 직전에도 적용 데이터의 무결성을 다시 검사합니다. 실패 시 정리를 진행하지 않으며 유지보수 잠금이나 manifest를 수동으로 고쳐 넘기지 않습니다.
- `finalQualification`은 복원 파일의 신뢰 근거로 사용하지 않고 기존 검증 표시를 제거합니다. 복원된 본선은 명단을 다시 확인해야 합니다. 공개 일정도 백업에서 가져오지 않고 원본으로 재생성합니다.

## 7. 운영 판단이 필요한 경우

- 실제 본선 진행 뒤 예선 정정으로 진출팀이 달라져야 하는 경우: 기록을 지우지 말고 대회본부 판단과 백업을 우선합니다.
- 다자 동률 승자승의 해석을 바꾸려는 경우: 주최 측 규칙 확인 없이 계산식을 변경하지 않습니다.
- 기기 저장 장애, 현장 네트워크 불안정, 담당 기록관 혼선: 실제 기기·운영자 리허설에서 확인하고 점수의 별도 기록 수단을 준비합니다.
- 새 화면이 서버·Rules 변경에 의존하는 경우: 화면만 먼저 배포하지 않습니다. 배포 대상·승인·검증 절차는 [개발 가이드](개발_워크플로우_가이드.md)를 따릅니다.
