# 바운스발리볼 프로젝트 인수인계 노트

이 문서는 새 Cowork 프로젝트/세션에서 이 작업을 이어받을 Claude를 위한 인수인계 문서입니다. 사용자(bong, pankun88@gmail.com)는 한국어로 소통하며, 간결하고 직접적인 답변을 선호합니다.

---

## 0. 가장 먼저 확인할 것 (TL;DR)

1. **현재 상태: 핵심 기능 전부 완성·배포 완료. 막힌 곳 없음.** 최근(2026-06 기준) 작업은 본선 대진표 UX 다듬기(전체화면 송출, 공개 게이트, 모바일 최적화)와 예선 점수 검증 강화였고 모두 배포됨. 진행 중인 새 요청이 없으면 사용자에게 다음 할 일을 물어볼 것.
2. **이 환경에서는 파일 직접 수정·배포가 정상 동작함.** Edit/Write 도구로 `/Users/bongchulkim/Desktop/project/바운스발리볼/` 아래 파일을 직접 고치고, bash에서 `firebase deploy --only hosting`으로 바로 배포됨(이번 작업 내내 검증됨). §8의 "폴더 동기화 안 됨" 이슈는 과거 다른 Cowork 세션 환경의 일이고 **지금 환경에는 해당 없음**(기록 보존용으로만 남겨둠).
3. **배포는 코드 수정만으로 끝나지 않음 — 반드시 `firebase deploy --only hosting` 실행.** 라이브 사이트(`bbounce-cdb31.web.app`)에 영향을 주므로, 배포 전 사용자에게 확인받는 흐름을 유지할 것(사용자는 "배포해줘"로 승인해 왔음).
4. **상시 규칙: 버그를 진단·해결할 때마다 `문제해결기록.md`에 증상/원인/해결 형식으로 자동 기록**(사용자 지시, 메모리에도 저장됨). 기능 추가가 아니라 "버그/이슈 해결"만 기록.

---

## 1. 프로젝트 개요

배구 대회 운영용 웹앱. 두 개의 화면으로 구성:
- **관리자 페이지** (`admin.html` + `js/admin.js`): 대회 설정, 팀/조 등록, 예선 경기결과 입력, 본선 대진표 생성·진행을 담당. 관리자만 사용, URL 외부 공유 금지.
- **관객 공유 대시보드** (`dashboard.html` + `js/dashboard.js`): 관리자가 입력한 결과를 Firebase Firestore 실시간 동기화로 그대로 보여줌. 관객에게 공개.

로컬 실행은 `로컬서버_시작.command` 더블클릭 → `python3 -m http.server 8765` → `http://localhost:8765/admin.html` 자동 오픈 (file:// 직접 열기는 크롬 계열에서 module script가 막혀서 전부 먹통이 되므로 금지 — 문제해결기록.md #2 참고).

배포 시에는 Firebase Hosting (`firebase deploy --only hosting`)으로 `https://bbounce-cdb31.web.app/...`에 올림. firebase-tools CLI가 Claude의 bash 샌드박스에는 없으므로 배포는 항상 사용자가 직접 해야 함.

프로젝트 규칙 원문은 `CLAUDE.md`에 있음(이 인수인계 문서와 별개로 항상 자동 로드됨). 핵심만 아래 §2에 요약.

---

## 2. 경기 규칙 요약

- **예선**: 2세트제. 1~2세트 모두 10점. 1세트씩 나눠 가지면(1:1) 무승부.
- **본선(토너먼트)**: 3세트제. 1~2세트 10점, 3세트(필요시) 7점. 2세트 먼저 따면 즉시 종료.
- **듀스**: 2점차 승리. 15점 도달 시 그 시점 점수로 즉시 종료(상한).
- **예선 승점**: 승 3점, 무 1점, 패 0점.
- **예선 순위 결정 순서**: 승점 → 세트득실 → 득실차 → 승자승(동률 그룹 내 미니리그 승수) → 그래도 동률이면 "추첨 필요"로 표시(실제 추첨은 관리자가 오프라인으로 진행).
- **본선 진출 인원**: 조별 상위 N팀 (N = `qualifyPerGroup`, 대회설정 탭에서 관리자가 직접 설정 가능, 기본값 2).

이 로직은 전부 `js/match-logic.js`에 구현되어 있고, Node 단위테스트(`js/test.mjs`)로 검증됨. **이 파일은 안정적으로 검증된 핵심 로직이므로, 수정이 필요하면 반드시 `js/test.mjs`를 함께 갱신하고 `node js/test.mjs`로 통과를 확인할 것.**

---

## 3. 파일 구조

```
바운스발리볼/                    ← 프로젝트 루트 (사용자 데스크탑의 "프로젝트 폴더" 안)
├── admin.html                  관리자 페이지 마크업
├── dashboard.html               관객 대시보드 마크업
├── index.html                   (랜딩/리다이렉트용, 거의 안 건드림)
├── connection-test.html         Firebase 연결 단독 테스트 페이지
├── firebase.json / .firebaserc / firestore.rules   Firebase 배포 설정
├── 로컬서버_시작.command        더블클릭하면 localhost:8765로 admin.html 오픈
├── README.md                    설치/배포/사용법 안내 (사용자용 문서, 최신 상태 유지 중요)
├── 문제해결기록.md              증상/원인/해결 형식의 버그 기록 — 새 버그 해결 시 계속 추가
├── css/
│   ├── style.css                전체 UI 스타일 (407줄)
│   └── bracket.css              본선 대진표 전용 스타일 (152줄)
└── js/
    ├── admin.js                 관리자 페이지 메인 로직 (981줄) — §5 함수 목록 참고
    ├── dashboard.js              대시보드 로직 (190줄)
    ├── match-logic.js            세트/경기 판정 + 조 순위 계산 (핵심 로직, 272줄) — §2 규칙 구현체
    ├── schedule.js               예선 라운드로빈 일정 생성 (27줄)
    ├── bracket.js                본선 대진표 생성/진행 로직, 부전승·3·4위전 포함 (193줄)
    ├── bracket-render.js         대진표 화면 렌더링 (관리자/대시보드 공용, 147줄)
    ├── ring-bracket.js           예선 "링크제" (꼭짓점 배치) 관련 헬퍼 (171줄)
    ├── csv-export.js             전체 결과 CSV 내보내기 (86줄, BOM 포함해 엑셀 한글 호환)
    ├── firestore-service.js      Firestore 연동 함수 모음 (240줄) — §4 데이터 구조 참고
    ├── firebase-config.js        ⚠️ 실제 Firebase 프로젝트 키값 (직접 입력됨, 민감정보 아님 — 공개 web API key)
    ├── firebase-init.js          firebase-config.js를 읽어 Firestore 인스턴스 초기화
    └── test.mjs                  match-logic.js 단위테스트 (`node js/test.mjs`로 실행)
```

**주의**: Claude의 임시 작업 폴더(outputs)에는 이 외에도 `bounce_volleyball_app_v2/`, `바운스발리볼_업데이트/`, `바운스발리볼_최종수정본/`, 그리고 여러 `.zip` 파일들이 남아있는데, 이건 예전에 파일을 zip으로 전달하던 방식(Task #11)의 잔재이며 **더 이상 사용하지 않는 죽은 코드/구버전**. 루트의 `admin.html`, `js/`, `css/`가 유일한 정본(source of truth)이며, 위 잔재 폴더들은 새 프로젝트로 넘어가면 자동으로 사라지므로 신경 쓸 필요 없음.

---

## 4. Firestore 데이터 구조

`firestore-service.js` 기준, `tournaments/{TOURNAMENT_ID}` 문서 아래:

- **`tournaments/{id}`** (문서 자체): `{ name, qualifyPerGroup, updatedAt }` — `saveTournamentInfo()`로 저장, `merge:true`라 부분 저장 안전.
- **`groups`** (서브컬렉션): `{ name, order, matchMode?, ringOrder? }` — `matchMode`는 `"roundrobin"`(기본) 또는 `"ring"`(링크제), `ringOrder`는 링크제일 때 꼭짓점 배치(팀 id 배열, 빈 자리는 null).
- **`teams`** (서브컬렉션): `{ name, groupId, order, advanced }`.
- **`prelimMatches`** (서브컬렉션): `{ groupId, teamA, teamB, round, sets, status, result, setsWonA, setsWonB, pointsForA, pointsForB, createdAt }`. `round`는 화면 표시 순서(드래그로 재배열 가능, `reorderPrelimMatches()`).
- **`finalMatches`** (서브컬렉션): `bracket.js`의 `generateBracket()` 결과 형태 그대로 저장.

모든 구독 함수(`subscribeXxx`)는 8초 타임아웃 감시(`watchForTimeout`)와 에러 이벤트(`firestore-error`/`firestore-timeout`)를 가지고 있어 admin.js/dashboard.js의 `initConnectionWatch()`가 화면 상단 에러 배너로 보여줌 — 동기화가 안 될 때 Firebase 자체 문제인지 먼저 이걸로 구분 가능.

---

## 5. `js/admin.js` 함수 목록 (981줄)

상태 변수 (17~25줄):
```js
let tournamentInfo = {};
let groups = [];
let teams = [];
let prelimMatches = [];
let finalMatches = [];
let seedSelection = [];        // 본선 진출팀 id 순서 (시드순)
let seedAutoMode = true;       // true면 예선 순위 기준 추천을 매 렌더마다 자동 채움
let pendingAutoSelectGroupName = null;
let ringSelection = null;      // 링크제 클릭배치 선택 상태
```

| 영역 | 주요 함수 | 비고 |
|---|---|---|
| 연결상태 | `initConnectionWatch`, `setConnStatus`, `showErrorBanner`, `hideErrorBanner` | Firestore 타임아웃/에러 배너 |
| 탭 | `initTabs` | 대회설정/예선/본선 탭 전환 |
| 대회설정 | `bindStaticHandlers`(저장 버튼 핸들러 포함), `addGroupFromForm`, `addTeamFromForm`, `renderGroupList`, `renderTeamGroupSelect`, `renderGroupTeamLists` | `qualifyPerGroupInput` 저장 로직도 여기 |
| 예선(공통) | `groupHasPrelimMatches`, `groupHasScoredMatches`, `confirmIfResultsWillReset`, `handleSetMatchMode` | 모드 전환 시 기존 결과 있으면 확인창 (비파괴 처리, Task #21) |
| 예선(링크제) | `applyRingOrderChange`, `handleRingShuffle`, `handleRingVertexDrop`, `handleRingPoolDrop`, `handleVertexClick`, `handlePoolChipClick`, `buildRingControls` | 도형 꼭짓점 클릭/드래그 배치 |
| 예선(라운드로빈) | `buildRoundRobinControls`, `handleMatchReorderDrop` | 드래그로 경기 순서 변경 (Task #21) |
| 예선 렌더링 | `renderPrelimGroups`, `teamName`, `openPrelimScoreModal` | "대진" 탭(구 "일정") 본문 |
| **본선 진출팀 선택 (최신, Task #22)** | `fmtSigned`, `computeRecommendedSeeds`, `buildQualifyReasonText`, `renderFinalTeamPicker` | §7 상세 설명 |
| 본선 | `onGenerateBracket`, `renderFinalBracket`, `openFinalScoreModal` | 대진표 생성/진행 |
| 공용 UI | `openScoreModal`, `showToast`, `reportError`, `escapeHtml`, `safeParseJson`, `dateStamp` | |

---

## 6. 작업 이력 (Task #1~23)

1~14: 초기 구축(프로젝트 구조, 관리자/대시보드 페이지, CSV, Firebase 연동/배포, 버튼 먹통 버그 진단·수정 등) — 전부 완료, 자세한 내용은 `문제해결기록.md` 참고.

15: `문제해결기록.md` 유지관리 — **상시 진행 중(pending)**. 새로운 *버그*가 진짜로 해결될 때마다 증상/원인/해결 형식으로 항목 추가. (기능 추가는 여기 기록 안 함, 버그만.)

16~21: 참가팀 등록 UX 개선, 링크제(꼭짓점 배치) 예선 대진 기능 신설, 도형에 경기 순서 라벨 추가, 정렬 순서 버그 수정, 모드 토글 비파괴화 + 링크제 우선배치 + "일정"→"대진" 명칭 변경 + 드래그 순서변경 — 전부 완료.

**22: 본선 진출팀 자동 선택 + 진출원칙/동률사유 표시 — 완료.** 사용자 요청 원문: "1. 본선 진출팀은 진출팀 원칙에 맞게 계산해서 이미 선택되어 있도록 해줘. 2. 선택되어 있되, 진출원칙(승점, 득실세트수, 점수 수 등 md에 있는 원칙을 기준으로)을 보여줘. 만약 무승부에 세트수까지 동률일때에는 원칙을 보여줘야 납득을 시킬 수 있어." 구현 상세는 §7.

**23: "폴더 동기화" 버그 진단 — (과거 Cowork 세션 한정 이슈) §8 참고. 현재 작업 환경에서는 해당 없음.**

**24~34: 본선 대진표 UX 다듬기 + 예선 점수 검증 강화 + 우승 세레모니 (2026-06, 전부 완료·배포됨).** 상세는 아래 §10. 버그성 수정은 `문제해결기록.md` #7-1·#8·#9·#10에도 기록됨.

---

## 7. Task #22 상세: 본선 진출팀 자동 선택

요구사항 두 가지: (1) 본선 진출팀이 예선 순위 기준으로 미리 체크되어 있어야 함 (수동 체크 전제 X). (2) 체크된 이유(승점/세트득실/득실차)와, 동률일 때 어떤 기준(승자승 또는 추첨)으로 갈렸는지 화면에 보여줘야 함.

**`qualifyPerGroup`** (신규 설정값, `tournamentInfo` 문서 필드, 기본 2): 대회설정 탭의 `qualifyPerGroupInput`(숫자 입력)에서 설정. "조별 상위 N팀"을 의미. `saveTournamentNameBtn` 클릭 시 대회명과 함께 저장됨(`saveTournamentInfo({ name, qualifyPerGroup })`).

**`seedAutoMode`** (admin.js 모듈 상태, 기본 `true`): true면 `renderFinalTeamPicker()`가 호출될 때마다 `computeRecommendedSeeds()`로 `seedSelection`을 다시 계산해서 덮어씀(즉, 예선 결과가 바뀌면 실시간으로 추천 목록도 갱신됨). 체크박스를 직접 클릭하거나 시드 순서 화살표(↑↓)를 누르면 `false`로 바뀌어 "관리자가 수동으로 관리하는 상태"가 되고, 이후엔 자동 갱신을 멈춤. "추천대로 다시 채우기" 버튼 또는 대회설정 저장(qualifyPerGroup 변경)으로 다시 `true`로 복귀.

**`computeRecommendedSeeds()`**: 각 조마다 `computeGroupStandings()`로 순위를 매기고, `rank <= qualifyPerGroup`인 팀들의 id를 모아 반환.

**`buildQualifyReasonText(standings, s)`**: "승점 X · 세트 ±Y · 득실 ±Z" 기본 문구를 만들고, 같은 (승점, 세트득실, 득실차) 조합을 가진 다른 팀이 있으면 그 팀 이름과 함께 "승자승으로 N위 결정" 또는(`needsLottery`가 true면) "추첨으로 결정 필요"를 덧붙임.

**`renderFinalTeamPicker()`**: 좌측엔 조별로 팀 체크리스트(예선 N위 배지 + 위 reason 텍스트 + 동률·추첨필요 빨간 플래그), 우측엔 체크된 팀들의 시드 순서(↑↓로 순서 변경 가능) 표시. 코드 전문은 `js/admin.js` 723~873줄.

검증: jsdom 기반 기능 테스트(`/tmp/rrtest/taskb.mjs`, Claude의 bash 샌드박스 전용 임시 스크립트로 새 세션에는 안 남아있음) 27개 + 기존 108개 = 135개 assertion 전부 통과. 클린 랭크, 3자 순환 동률(추첨 필요) 시나리오, 수동 오버라이드 유지, 추천 리셋 버튼, qualifyPerGroup 실시간 변경까지 커버.

---

## 8. (과거 이슈·기록 보존용) 세션 폴더 연결과 실제 데스크탑 폴더 불일치

> ⚠️ 이 섹션은 **과거 다른 Cowork 세션 환경**에서 있었던 일이며, **현재 작업 환경에는 해당하지 않습니다**(지금은 Edit/Write로 실제 폴더를 직접 수정하고 `firebase deploy`로 바로 배포됨 — 이번 작업 내내 검증됨). 비슷한 증상이 다시 나타날 때를 대비한 참고용으로만 남겨둡니다.

### 증상
사용자가 "업데이트가 안됐어" 라고 보고. 실제 admin.html/본선 탭 스크린샷을 보내왔는데 Task #22의 새 UI(조별 본선 진출 인원 입력칸, 자동 체크, 진출원칙 텍스트)가 전혀 안 보임 — 예전 버전 그대로.

### 진단 과정 (배제한 가설들)
1. **Firebase Hosting 캐시 문제** — 배제됨. 사용자는 `localhost:8765`(로컬서버)를 쓰고 있었고, 배포된 `https://bbounce-cdb31.web.app`는 아예 쓰지 않았음.
2. **포트 8765에 옛 서버 프로세스가 남아있어서 새 서버를 못 띄움** — 배제됨. `lsof -nP -iTCP:8765 -sTCP:LISTEN` 결과 아무 프로세스도 없었음.
3. **한글 폴더명 NFC/NFD 중복 폴더** (`문제해결기록.md` #3과 같은 종류의 문제로 처음 의심함) — **부분적으로 배제됨**. `ls -la ~/Desktop/프로젝트\ 폴더/`로 확인한 결과 폴더는 정확히 2개: `바운스발리볼`(메인)과 `바운스발리볼 ver1`(이름이 다른 별도 백업 폴더, **admin.html 자체가 없음** — 무관). 이름이 같은 "가짜 중복 폴더"는 없었음.

### 확인된 실제 원인
**이 Cowork 세션의 "연결된 폴더"가 사용자의 실제 데스크탑 `바운스발리볼` 폴더와 같은 곳을 가리키지 않음(또는 실시간으로 동기화되지 않음).** 두 가지 독립적인 테스트로 증명:

1. **bash 마운트 테스트**: bash 도구로 `/sessions/.../mnt/바운스발리볼/`에 마커 파일(`_sync_test_마커.txt`)을 직접 생성. 사용자가 자기 터미널에서 같은 경로를 열어봤지만 `cat: No such file or directory` — 파일이 없었음.
2. **Write 도구 테스트**: Write 도구로 `/Users/bongchulkim/Desktop/프로젝트 폴더/바운스발리볼/_sync_test2_마커.txt`를 만들려고 시도 → **명시적 에러**: `"Write on ... is blocked in this session — it resolves to a protected location or a path outside the connected folder."` 즉, 화면에 표시되는 경로 문자열은 "연결된 폴더"와 똑같이 생겼지만, Write 도구 내부적으로는 그 경로를 연결된 폴더 "바깥"으로 판단하고 거부함.

이 두 결과는 서로 다른 메커니즘(bash 마운트 vs Write 도구의 경로 검증)에서 나왔지만 같은 결론을 가리킴: **이 세션에서 admin.html/admin.js를 실제 폴더 경로에 직접 쓰거나 cp하는 방식은 신뢰할 수 없었음.** 이전 세그먼트에서 "delivered to real folder" + grep으로 검증했다고 기록된 모든 내용은, 사실 같은(어긋난) bash 마운트 안에서 쓰고 같은 마운트 안에서 grep으로 확인한 것이었기 때문에 **거짓 양성**이었던 것으로 보임 — 검증 자체가 문제가 있던 경로를 다시 확인한 셀프 체크였던 것.

### 적용한 임시 해결책
실제 폴더 경로에 직접 쓰는 모든 방법이 막혀 있었으므로, **마운트와 무관한 별도 경로(Claude의 임시 작업 폴더 `outputs/`)에서 작업하고, `mcp__cowork__present_files` 도구로 사용자에게 파일을 직접 전달**하는 방식으로 전환:
- `admin.html` (4758바이트, md5 `de4a62de5f052c1a7dd5c125cafc834e`)
- `js/admin.js` (40688바이트)

사용자에게 이 두 파일을 다운로드해서 실제 `바운스발리볼` 폴더의 같은 파일에 직접 덮어쓰라고 안내함. **이 교체를 사용자가 실제로 했는지, 했다면 새 기능이 정상적으로 보이는지는 아직 확인 안 됨 — 다음 세션 시작 시 가장 먼저 확인할 것.**

### 추가로 확인된 사실
- Cowork 앱에는 현재 "연결된 폴더 끊기/다시 연결하기" 전용 UI 버튼이 없음 (anthropics/claude-code GitHub 이슈 #40043, 아직 미구현 기능 요청 상태). 실질적 대안은 **새 작업/새 프로젝트를 시작해서 거기서 폴더를 새로 선택**하는 것.
- 새 프로젝트에서도 같은 동기화 문제가 재발할 수 있음. 재발 여부와 무관하게, **앞으로는 다음 검증 원칙을 따를 것**:
  - Claude 자신의 bash/grep/Read 결과만으로 "실제 폴더에 반영됐다"고 단정하지 말 것. 그 검증도 같은(잘못된) 마운트를 보고 있을 수 있음.
  - 의심스러우면 마커 파일 테스트(고유한 임의 문자열을 담은 파일을 만들고, **사용자가 자기 터미널/Finder에서 직접** 그 파일을 찾아보게 하기)로 교차 검증할 것.
  - 안전한 기본값: 모든 결과물은 Claude의 임시 `outputs/` 폴더에서 완성 → `mcp__cowork__present_files`로 사용자에게 전달 → 사용자가 직접 실제 폴더에 옮기게 안내. 이게 가장 느리지만 가장 확실한 경로.

### 다음 세션에서 할 일 (체크리스트)
- [ ] 사용자에게 실제 `바운스발리볼` 폴더의 admin.html에 "조별 본선 진출 인원" 입력칸이 보이는지 확인 요청
- [ ] 보이면: 동기화/연결 문제가 해결된 것으로 보고, `문제해결기록.md`에 새 항목(증상/원인/해결, 위 §8 내용 요약) 추가 — Task #15 작업
- [ ] 안 보이면: 새 프로젝트의 폴더 연결이 여전히 문제인지, §8의 마커 파일 테스트부터 다시 진행
- [ ] Task #23을 완료 처리 (현재는 "원인 진단 완료"까지만 completed 표시되어 있고, 사용자 확인 후 진짜로 종료 가능)

---

## 9. 작업 시 일반 주의사항

- 변수/UI 텍스트/주석 전부 한국어 — 새 코드 작성 시 기존 톤(반말 아닌 정중체, "~해주세요"/"~됩니다" 스타일)을 맞출 것.
- `match-logic.js`를 수정하면 반드시 `js/test.mjs` 갱신 + `node js/test.mjs` 통과 확인.
- 새 기능을 admin.js에 추가할 때 `renderXxx()` 패턴(전체 innerHTML 재생성 후 이벤트 리스너 재바인딩)을 따르고 있음 — 기존 스타일 유지.
- CSS 클래스 `.badge`, `.empty-hint`, `.lottery-flag`는 이미 `css/style.css`에 정의되어 있으니 재사용.
- 사용자는 간결한 답변을 선호함 — 작업 완료 보고 시 과정을 길게 재설명하지 말고 결과 위주로 짧게.
- 코드 수정 후에는 `node --check <파일>`로 문법 확인, CSS는 중괄호 짝 확인 정도로 가볍게 검증한 뒤, 사용자 승인받아 `firebase deploy --only hosting`으로 배포하는 흐름을 유지.

---

## 10. 최근 작업 상세 (2026-06, Task #24~34) — 본선 대진표 UX, 점수 검증, 우승 세레모니

이 시기에 사용자가 연속으로 요청한 다듬기 작업들. 모두 완료·배포됨.

### 10-1. 대시보드 본선 대진표 "전체화면" 송출 기능 (체육관 모니터용)
- `dashboard.html` 본선 탭에 "⛶ 전체화면" 버튼(`#dashBracketFullscreenBtn`), 카드(`#dashBracketCard`) 추가. `js/dashboard.js`의 `initBracketFullscreen()`이 토글 담당.
- PC는 표준 Fullscreen API(`requestFullscreen`/webkit) 사용. iOS Safari·인앱 브라우저는 일반 요소 전체화면을 막아서, API가 없거나 0.4초 안에 전환 안 되면 `.is-fullscreen-fallback`(position:fixed 오버레이)로 **의사 전체화면** 대체.
- 전체화면 시 `container.dataset.fullscreenZoom = "1"`로 표시 → `js/bracket-render.js`의 redraw가 이 값을 보고 배율 정책을 바꿈.

### 10-2. 대진표 화면맞춤(스케일) 방식 — `zoom` → `transform: scale()` (★중요)
- `js/bracket-render.js`의 핵심 변경. 기존 CSS `zoom`은 iOS Safari에서 동작 안 함 → 모바일에서 트리가 안 줄고 넘쳐 흘렀음. `transform: scale()`로 전환(모든 브라우저 동작).
- transform은 레이아웃 박스를 안 줄이므로, 트리(`.bracket-track`)를 `position:absolute`로 띄우고 부모(`.bracket-root`)의 width/height를 "스케일된 실제 크기"로 직접 지정해 스크롤/여백 없이 맞춤. **연결선(SVG)은 반드시 배율 1(transform none) 상태에서 그린 뒤 트리와 함께 스케일**되게 해야 어긋나지 않음(redraw가 매번 transform을 잠깐 none으로 → 측정·drawConnectors → 다시 scale). 이 순서를 깨면 연결선이 틀어지므로 주의.
- **배율 3갈래 정책** (이 부분 건드릴 땐 `문제해결기록.md` #9 꼭 읽을 것):
  1. 전체화면(PC 송출): 가로·세로 모두 맞춰 전체를 한 화면에(1배 초과 확대 허용).
  2. 모바일 일반(`window.innerWidth <= 720`): 가로 폭에 맞춰 전체가 한눈에 보이게 축소 + 사용자가 핀치 줌으로 상세 확인.
  3. PC 일반: 자연 크기(1배), 넘치면 가로 스크롤.
- `css/bracket.css`: 모바일에서는 전체화면 버튼을 `display:none`으로 숨김(사용자 결정 — 모바일은 QR 개인 확인용이라 전체화면 불필요, 제목 겹침도 해결). 모바일 안내문구 "👉 두 손가락으로 확대하거나 스크롤해서 자세히 보세요".
- **설계 의도(목적이 다름)**: PC = 화면 고정 후 큰 모니터 송출(전체 한눈에). 모바일 = 관객이 QR로 각자 휴대폰에서 실시간 확인(전체 구조 먼저 → 핀치 줌으로 상세).

### 10-3. 본선 대진표 변경 "공개 게이트" (관객 오해 방지) — ★10-8에서 점수 입력까지 확장됨, 최신 동작은 10-8 참고
- 요구: 대진표 생성 → 부전승 배치 → 자리 수동 조정까지 다 끝낸 뒤에야 관객 대시보드에 공유되게.
- `js/admin.js`: `bracketPublishPending` 상태 도입. 대진표 생성(`onGenerateBracket`은 이제 `generateBracket()`으로 **로컬에서만** 생성), 부전승 배치/확정, 자리 드래그 스왑은 전부 Firestore에 즉시 저장하지 않고 화면(`finalMatches`)에만 반영 + 경고 막대(`#bracketPublishBar`) 표시.
- "공개" 버튼(`#publishBracketBtn` → `handlePublishBracket`)을 눌러야 `publishFinalBracket(finalMatches)`로 한 번에 반영. 공개 전 새로고침/이탈 시 `beforeunload` 경고.
- `js/firestore-service.js`: 기존 `generateFinalBracket`(즉시 저장) 제거, **`publishFinalBracket`** 신설 — 새 대진표는 경기 수/ID 구성이 달라질 수 있어 단순 merge가 아니라 "없어진 경기는 삭제 + 새 배열 전부 교체"하는 완전 교체 방식.
- (당시엔) 점수 입력(`openFinalScoreModal`)과 대진표 초기화는 게이트 대상 아님 — 즉시 반영. **→ 이후 10-8에서 점수 입력도 게이트에 포함되도록 바뀜.**

### 10-4. 예선 점수 검증 강화 — "끝난 점수만 입력 가능"
- 운영 방식이 "경기 종료 후 최종 점수만 한 번에 입력"이라, 진행중 점수(미완료) 자체를 막기로 함.
- `js/match-logic.js`의 `validateSetScore`: 목표점수 미달·듀스 중 2점차 미달 등 "아직 승부 안 난 점수"는 0:0(미입력) 빼고 전부 막고 "아직 끝나지 않은 세트입니다…" 경고. (15점 상한·듀스 2점차 규칙은 기존대로.)
- `js/admin.js` 예선 경기 목록 배지: `evald.status === "in_progress"`면 "경기중" 표시(기존엔 무조건 "경기전"이던 버그 수정).

### 10-5. Firebase Hosting 캐시 헤더
- `firebase.json` hosting에 `headers` 추가: `**/*.@(js|css|html)`에 `Cache-Control: no-cache, max-age=0, must-revalidate`. 배포해도 브라우저가 옛 버전 캐시를 계속 쓰던 문제 해결(`문제해결기록.md` #7-1).

### 10-6. PC 전체화면 한정 시각 효과 (승리 아이템 🔥, 결승 트로피 회전)
- 전부 `css/bracket.css`에 `#dashBracketCard.is-fullscreen` 스코프로만 추가 — 일반 화면·관리자 화면엔 영향 없음.
- 이긴 팀 이름 위에 🔥(불꽃) 아이콘이 통통 튀며 반복(`@keyframes winner-char-bounce`). **처음엔 👑(왕관)로 했다가 사용자가 "왕관은 우승팀 전용 느낌이라 어색함" 피드백 → ✌️(승리사인) → 최종 🔥로 변경.** 이름(`.match-vs-name`)은 overflow:hidden(말줄임)이라 아이콘이 잘리므로, `:has()`로 안 잘리는 바깥 컬럼(`.match-vs-side`)에 붙임 — Safari 구버전은 `:has()` 미지원이라 그 경우 아이콘만 안 보이고 나머진 정상 동작(우아한 성능 저하).
- 결승 트로피(`.bracket-final-title-icon`)는 3D `rotateY` 회전 + 금빛 광채 맥동. 우승 확정되면(`.is-champion-title` 하위) 더 크고 빠르게.
- `prefers-reduced-motion: reduce`면 전부 꺼짐.

### 10-7. 본선 대진표 팀명에서 시드 코드 `(A1)`/`(C2)` 표시 숨김
- `js/bracket-render.js`에 `displayTeamName(name)` 헬퍼 추가 — 끝에 붙은 영문/숫자 괄호만 정규식으로 제거(`/\s*\([A-Za-z0-9]+\)\s*$/`). 실제 이름의 괄호(예: "교회배(주말반)")는 영문/숫자만 있는 패턴이 아니면 안 지워짐 → 한글 포함 괄호는 보존됨에 주의(현재 정규식은 영문/숫자만 매치하므로 한글 들어간 괄호는 그대로 남음, 의도된 동작).
- 저장된 데이터(팀 이름 자체)는 안 건드림 — **화면 표시(카드 이름·세트 표·우승 발표 문구)에서만** 적용. 조/순위 라벨("A조 1위")은 별도 로직(`appendGroupLabel`)이라 그대로 유지됨.

### 10-8. 본선 점수 입력도 "공개 게이트"에 포함 (라운드별 공개 가능하게)
- **버그**: 10-3에서 만든 공개 버튼/막대가 `bracketPublishPending`이 true일 때만 보였는데, 점수 입력(`openFinalScoreModal`)은 게이트 없이 즉시 Firestore에 저장됐음. 그래서 한 번이라도 공개하고 나면 막대가 사라져서, **6강→준결승→결승처럼 라운드마다 결과를 입력해 공개하는 흐름 자체가 막혀 있었음.**
- **해결**: 점수 입력도 로컬에만 반영(`recordMatchResult` 후 `bracketPublishPending = true`)하도록 통일. 공개 막대는 **대진표가 있는 한 항상 표시**되도록 변경 — 공개 안 한 게 있으면 🟠 주황 + "관객 화면에 공개"(활성), 다 공개됐으면 🟢 초록 + "공개 완료"(비활성). 더는 쓰지 않는 `saveFinalMatches` import 제거(`publishFinalBracket`만 사용).
- 결과적으로 지금은 **본선 점수가 실시간 자동공개가 아니라, 관리자가 매 라운드 입력 후 버튼을 눌러야 공개되는 방식**임. (사용자가 의도적으로 원한 동작 — 혹시 "점수만큼은 즉시 자동 공개"로 되돌리고 싶다고 하면 `openFinalScoreModal`의 onSave에서 `bracketPublishPending=true` 대신 다시 `publishFinalBracket`을 즉시 호출하도록만 바꾸면 됨.)
- `문제해결기록.md` #10에 상세 기록됨.

### 10-9. 우승 세레모니 (불꽃 연결선 + 우승 배너)
- 요구: 우승팀의 본선 첫 라운드 팀명을 클릭하면, 연결선이 불꽃처럼 타들어가며 결승까지 올라가고, 우승팀명에 불꽃 + 강렬한 우승 이펙트.
- `js/bracket-render.js`:
  - 트리 빌드 중 결승이 끝나 우승 확정이면(`isChampionDecided`) `decidedFinalMatch`로 기억해두고, 트리 렌더 끝(`container.appendChild(root)` 직후)에 `!options.editable`(=관객 대시보드만, 관리자 화면 제외)일 때 `setupChampionCeremony()` 호출.
  - `setupChampionCeremony`: `matches`에서 우승팀이 들어있는 경기들을 라운드순 정렬해 "우승 경로"를 구하고, 첫 라운드 카드의 우승팀 이름 엘리먼트에 `.champion-trigger` 클래스 + 클릭 리스너를 건다. 이름 엘리먼트를 찾기 위해 렌더 시점에 `data-team-id` 속성을 심어둠(`renderVsSide`/`renderByeVsRow` 양쪽 다).
  - 연결선(SVG `path.bracket-line`)에도 `data-from`/`data-to`(matchId)를 심어둬서, 우승 경로의 연결선들을 순서대로 찾을 수 있게 함.
  - 클릭 시 `playChampionCeremony`: 경로의 각 연결선마다 같은 `d`를 가진 `.bracket-fire-line` path를 겹쳐 그리고, `stroke-dasharray`/`stroke-dashoffset`을 이용한 "그려지는" 애니메이션으로 아래(1라운드)→위(결승) 순서대로 불꽃이 타오르게 함(연결선 1개당 650ms, `setTimeout`으로 순차 실행). 다 타면 결승 카드의 우승팀 이름에 `.champion-aflame` 클래스 추가, 그 다음 `burstChampionCelebration()`으로 전체 화면 오버레이(금빛 섬광 + 위로 솟는 이모지 불티 34개 + 가운데 "🔥 OOO 우승! 🔥" 배너, 4.6초 후 자동 제거) 실행.
  - 다시 클릭하면 이전 잔여물(`.bracket-fire-line`, `.champion-celebrate-layer`, `.champion-aflame`)을 지우고 재생 — 반복 가능.
- `css/bracket.css`: `.bracket-wrap`에 `position: relative` 추가(오버레이 기준점). 불꽃 그라디언트/글로우는 SVG `<defs>`를 `ensureChampionFireDefs()`로 한 번만 주입.
- **주의**: 이 기능은 관객 대시보드(`dashboard.js`가 `editable:false`로 호출)에서만 동작. 관리자 화면(`admin.js`, `editable:true`)에선 우승 세레모니 트리거가 안 걸림(팀명 드래그 기능과 충돌 방지).

### 10-10. 조별 순위표(대시보드)에 득실차 열 추가 + 가로 스크롤 제거
- `js/dashboard.js` 예선 순위표에 `s.pointDiff`(득실차) 열 추가(세트득실 옆). 8칸이 됨.
- `css/style.css` `.standings-table`: F11 송출 시 좌우 스크롤 없이 한눈에 들어오도록 `table-layout: fixed; width:100%` + 숫자 칸 폭 고정(`width:11%`), 팀 칸이 나머지 차지. 좁으면 줄바꿈으로 접히고 절대 가로 스크롤 안 생김(`.table-scroll{overflow-x:hidden}`).

### 10-11. 예선 대시보드에서도 팀명 시드코드 `(A1)` 숨김
- 10-7에서 만든 `displayTeamName`을 `bracket-render.js`에서 **export**하고 `dashboard.js`가 import해서, 예선 순위표·경기목록·조편성 칩의 팀명에 모두 적용(`teamName()`이 정리된 이름 반환). 저장 데이터는 그대로, 표시만 정리.

### 10-12. ★데이터 백업/복원 기능 (학기 간 이어쓰기 / 사고 대비)
- 배경: 대회 데이터는 Firestore에 영구 저장되지만, 긴 공백(예: 1학기→2학기) 동안 실수로 "초기화" 버튼을 누르면 통째로 날아갈 위험. 그래서 **되돌릴 수 있는** 백업이 필요.
- `js/firestore-service.js`:
  - `exportAllData()`: 대회정보 doc + groups·teams·prelimMatches·finalMatches 전체를 **문서 ID까지 포함**해 한 객체로 반환(ID 보존이 핵심 — teams.groupId, 경기 teamA/teamB·nextMatchId가 ID로 서로 참조). info의 `updatedAt`(serverTimestamp)만 제외.
  - `importAllData(data)`: 기존 4개 컬렉션 전부 삭제 + 백업 문서들을 같은 ID로 재기록, 배치 500 한도 때문에 450개씩 청크로 커밋. 마지막에 info doc을 `setDoc(merge:false)`로 교체. **현재 데이터를 전부 대체**하므로 호출 전 confirm 필수.
  - `getDoc`을 import에 추가함.
- `admin.html` 대회설정 탭에 "💾 데이터 백업/복원" 카드(`#backupBtn`, `#restoreBtn`, 숨김 `#restoreFileInput`).
- `js/admin.js`: `handleBackup`(JSON 파일 다운로드, `downloadJson` 헬퍼), `handleRestoreFile`(파일 읽어 confirm 후 `importAllData`). 복원 후 onSnapshot 구독들이 자동으로 화면 갱신. 복원은 Firestore에 직접 쓰므로 관객 대시보드에도 바로 반영됨(공개 게이트와 무관).
- 사용자 시나리오(이때 확인된 것): 1학기에 6강까지 입력 완료, 2학기에 준결승(4강)부터 이어서 진행 예정 → **같은 대회를 그대로 이어서 쓰면 됨(분리 불필요)**. 백업은 사고 대비 보험용. CSV 내보내기(읽기용)는 기존부터 있었고, 이 JSON 백업은 앱에 그대로 복원 가능한 점이 다름.

### 관련 파일 한눈에
- 대진표 렌더/스케일/우승 세레모니: `js/bracket-render.js` (`redraw` 함수가 스케일 핵심, `setupChampionCeremony`/`playChampionCeremony`/`burstChampionCelebration`이 세레모니, `displayTeamName`이 코드 숨김)
- 대시보드 전체화면 토글: `js/dashboard.js` `initBracketFullscreen()`
- 공개 게이트(대진+점수)·점수 검증·배지: `js/admin.js`
- Firestore 본선 저장/공개·백업/복원: `js/firestore-service.js` (`publishFinalBracket`, `exportAllData`/`importAllData`; `saveFinalMatches`는 이제 안 씀)
- 백업/복원 UI: `admin.html` "데이터 백업/복원" 카드 + `js/admin.js`(`handleBackup`/`handleRestoreFile`/`downloadJson`)
- 점수 규칙 판정: `js/match-logic.js` (`validateSetScore`, `getSetWinner`, `evaluate*Match`)
- 스타일·전체화면·모바일 미디어쿼리·승리/트로피 이펙트·우승 세레모니: `css/bracket.css`

## 11. 코트별 기록관 점수 작성·승인 워크플로

- 기록관 진입점: `recorder.html`. 개인 Google 로그인 후 관리자가 발급한 대회 공용 접근 코드를 입력한다.
- 관리자는 대회설정에서 접근 코드 생성·교체·폐기, 코트와 경기순서 설정, 제출 결과 승인·반려·직접 수정, 잠금 강제 해제, 승인 후 정정을 처리한다.
- 기록관 점수는 `scoreWorkflows`에 비공개로 저장된다. 작성 완료 시 해당 코트는 관리자 검토를 기다리지 않고 다음 경기로 이동하며, 승인된 결과만 기존 경기 문서와 관객 화면에 반영된다.
- 한 경기는 한 기록관만 편집할 수 있다. 자동 만료는 없으며 기록관 취소 또는 사유가 기록되는 관리자 강제 해제로만 잠금이 풀린다.
- 승인 후 정정은 시작 이력이 없는 경기를 기존 순서에서 바로 고치고, 이력이 있는 영향 경기만 우선 재입력 대기열로 보낸다.
- Functions/Rules/에뮬레이터가 추가됐다. 로컬 릴리스 검증은 Node 20과 Java 21 환경에서 `npm ci && npm --prefix functions ci && npm run test:release`로 실행한다.
- 주 보안 규칙은 `firestore.rules`, 장애 시 안전 복구 규칙은 `firestore.safe-rollback.rules`와 `firebase.safe-rollback.json`이다. 기존의 로그인 사용자 전체 쓰기 규칙으로 되돌리면 안 된다.
