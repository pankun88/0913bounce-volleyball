# 바운스발리볼 웹앱 — 역사적 문제 해결 기록

> **범위와 탐색 안내**
> 이 문서는 개발·운영 중 확인된 증상, 기술적 원인, 당시의 수정과 검증을 보존하는 **역사 로그**입니다. 각 항목의 `해결(당시)`은 작성 당시 코드와 데이터에 대한 기록이며 현재의 배포·보안 정책, 경기 규칙, 운영 절차 또는 명령이 아닙니다. 현재 운영 상태는 [`인수인계_노트.md`](./인수인계_노트.md), 개발·검사·배포 방법은 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md), 안정적인 요구사항과 규칙은 [`AGENTS.md`](./AGENTS.md), 진입 안내는 [`README.md`](./README.md)를 기준으로 확인합니다. 이 문서에서는 당시 명령을 재현하지 않습니다.
>
> **배포 증빙의 한계**
> 최신 배포의 커밋·대상·검사 결과와 남은 제한사항은 [인수인계 노트](./인수인계_노트.md)에 기록합니다. 과거 배포나 특정 데이터에서의 성공을 현재 현장 준비 완료의 보증으로 해석하지 않습니다.
>
> **표기**
> `검증·범위`는 당시 확인한 근거와 확인하지 못한 범위를 적습니다. 현재 동작과 직접 관련된 항목에는 현재 문서를 가리키는 짧은 연결만 두며, 세부 운영 지침을 이곳에 복제하지 않습니다.

---

## 1. Firestore 실시간 연결 실패가 화면에 전혀 표시되지 않던 문제

**증상**
대시보드가 `대회 정보를 불러오는 중입니다…`에서 멈추고 콘솔 외에는 오류가 보이지 않았음.

**원인**
`onSnapshot` 실패·무응답과 광고 차단기/보안 확장 프로그램으로 인한 WebChannel 차단을 화면 상태로 전달하지 않았음.

**해결(당시)**
`firestore-timeout`·`firestore-error` 이벤트와 관리자·대시보드의 `#errorBanner`를 추가해 연결 실패와 대처 안내를 표시함.

**검증·범위**
당시 화면 오류 노출을 위한 수정 기록이다. 현재 연결·재시도 상태의 기준은 `인수인계_노트.md`와 소스이며, 이 항목의 안내 문구를 운영 정책으로 사용하지 않는다.

---

## 2. 관리자 페이지 버튼이 전부 먹통이던 문제 (조 추가/팀 추가/저장 등 전혀 반응 없음)

**증상**
`admin.html`의 버튼과 입력, 대시보드 동기화가 모두 멈춘 것처럼 보였음.

**진단 과정·원인**
파일 아이콘으로 HTML을 열어 `file://`가 되면 Chromium 계열이 module script를 CORS 보안 정책으로 실행하지 않았음. 로직 검토만으로는 재현되지 않아 headless Chromium에서 저장·조 추가·팀 추가를 실제 클릭해 확인함.

**해결(당시)**
`로컬서버_시작.command`를 추가해 HTTP로 열도록 하고 README에 파일 직접 열기와 배포 주소를 구분해 적음.

**검증·범위**
`http://localhost`에서 콘솔 오류 0개와 해당 흐름 정상 동작을 확인한 로컬 검증이다. 로컬 실행 방법은 현재 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md)만 따른다.

---

## 3. 관리자 페이지를 수정해도 실제 폴더(Finder)에는 반영이 안 되던 문제

**증상**
안내받은 파일의 수정 시각·내용이 Finder에서 그대로였고, 폴더를 옮겨도 같은 현상이 반복됨.

**원인**
macOS 한글 경로의 NFC/NFD 정규화가 달라 겉보기 이름이 같은 별도 경로에 변경분이 기록된 것으로 진단함.

**해결(당시)**
실제 폴더 목록과 바이트 단위 경로를 다시 확인해 올바른 위치를 구분하고, Finder에서 결과 파일을 재확인하는 단계를 추가함.

**검증·범위**
당시 세션의 경로 혼동 원인 기록이다. 모든 macOS 경로나 도구에서 항상 재현된다는 의미는 아니다.

---

## 4. 링크제 도형의 경기 순서 번호와 실제 경기 목록 순서가 일치하지 않던 문제

**증상**
링크제 도형의 ①②③④와 경기 목록이 달랐고, 예를 들어 도형의 1번과 목록 첫 경기가 서로 달랐음.

**원인**
`subscribePrelimMatches`가 Firestore 문서를 `round` 기준으로 정렬하지 않아 자동 문서 ID 순서가 화면에 노출됨.

**해결(당시)**
관리자·대시보드·CSV의 그룹 경기 배열에 `round` 오름차순 정렬을 적용해 표시·순위 계산·내보내기 순서를 맞춤.

**검증·범위**
라운드로빈에서 잠재적으로 가려져 있던 순서 불일치까지 함께 다룬 수정 기록이다. 현재 표시 순서의 운영 설명은 인수인계 문서에 둔다.

---

## 5. Claude 세션에서 파일 삭제·일부 도구가 막히는 문제

**증상**
오래된 폴더·압축 파일 정리와 한글 경로의 일부 편집 요청이 한 세션에서 권한 또는 마운트 오류로 실패했음.

**원인**
당시 세션의 경로 정규화 불일치와 도구 실행 환경이 겹친 것으로 관찰함. 관찰된 오류만으로 도구의 보편적인 삭제 금지나 특정 편집 우회 정책을 결론 내릴 근거는 없었음.

**해결(당시)**
실제 경로와 작업 결과를 별도로 재확인하고 세션을 정리함. 이전 기록에 있던 도구명, 직접 파일 쓰기 우회, `rm`/`unlink`를 이용하라는 내용은 재현 절차에서 제거하고 **현재 정책으로 폐기**함.

**검증·범위**
이 항목은 특정 세션의 실패 보고일 뿐이며 현재 저장소 작업 규칙·OS 권한·도구 동작을 규정하지 않는다. 파일 삭제나 편집을 이 문서의 지침으로 판단하지 않는다.

---

## 6. 조를 삭제하면 그 조에 속한 팀이 화면에서 사라지고 지울 수도 없던 문제 (참가팀 수가 줄지 않음)

**증상**
조를 모두 삭제하면 팀 목록과 삭제 버튼이 사라지고 참가팀 수가 줄지 않았음.

**원인**
`renderGroupTeamLists()`가 조가 없을 때 즉시 반환했고, `deleteGroup()`은 팀의 `groupId`를 남겨 삭제된 조를 가리키는 고아 팀을 만들었음. 숫자는 고정값이 아니라 실제 Firestore 데이터였음.

**해결(당시)**
조가 없어도 팀을 `미배정`으로 표시하고, 존재하지 않는 조를 가리키는 팀까지 미배정으로 판정함. 조 삭제 시 소속 팀의 `groupId`를 일괄 `null`로 바꿈.

**검증·범위**
관리자와 대시보드가 같은 `teams` 컬렉션을 공유한다는 점도 확인한 당시 결함 기록이다.

---

## 7. js/admin.js 등을 고쳐도 브라우저가 계속 옛 버전을 보여주던 문제 (캐시)

**증상**
파일을 바꿔도 서버 재시작·일반 새로고침 뒤 브라우저가 이전 모듈을 계속 사용했음.

**원인**
당시 기본 Python 정적 서버가 캐시 금지 헤더를 보내지 않아 브라우저가 모듈 스크립트를 디스크 캐시에서 재사용함.

**해결(당시)**
로컬 launcher에 no-store 계열 응답 헤더와 첫 URL의 timestamp를 적용하고, 이미 남은 캐시는 별도 브라우저/개발자 도구로 확인하도록 기록함.

**검증·범위**
이 항목의 launcher와 캐시 우회 설명은 당시 개발 환경의 해결 기록이다. 현재 로컬 서버 선택과 확인 방법은 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md)를 따르며, 이곳의 명령을 복사하지 않는다.

---

## 7-1. 배포된 실제 사이트(Firebase Hosting)에서도 같은 캐시 문제 재발

**증상**
당시 `bbounce-cdb31.web.app`에서 배포 뒤 일반 창만 이전 파일을 보이고 시크릿 창에서는 새 파일이 보였음.

**원인**
당시 `firebase.json`에 정적 파일 재검증 헤더가 없어 Hosting의 기본 캐시 정책이 적용됨.

**해결(당시)**
JavaScript·CSS·HTML에 재검증을 요구하는 Hosting headers를 추가하고, 기존 캐시가 남은 첫 배포 뒤에는 다시 확인함.

**검증·범위**
`bbounce-cdb31.web.app`은 과거 주소이며 현재 프로젝트·Hosting 식별자가 아니다. 현재 주소와 배포 절차는 [`인수인계_노트.md`](./인수인계_노트.md) 및 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md)에서 확인한다.

---

## 8. 관리자 페이지에서 점수를 입력했는데 "경기전"으로 표시되던 문제

**증상**
예선에 일부 점수를 입력해도 관리자 목록에 계속 `경기전`이 표시됐고, `14:13` 같은 점수가 규칙 위반처럼 오해될 수 있었음.

**원인**
관리자 배지가 승·패·무승부 외 상태를 모두 경기 전으로 처리했음. `14:13`은 당시 규칙상 듀스 진행 중일 수 있었고, 운영은 최종 세트 점수를 한 번에 받는 방식이었음.

**해결(당시)**
관리자에 `in_progress` 배지를 추가하고, 최종 승부가 나지 않은 세트는 `0:0` 미입력 예외를 제외하고 저장을 거부하도록 `validateSetScore`를 맞춤.

**검증·범위**
완료·듀스 진행·상한 위반 등 11개 케이스를 직접 확인한 당시 근거다. 경기 규칙 자체를 바꾸는 제안이 아니며 현재 판정 기준은 [`AGENTS.md`](./AGENTS.md)와 구현을 기준으로 한다.

---

## 9. 본선 대진표 트리 화면맞춤 — PC·모바일 2-way 방식 (최종 정리)

**배경/증상**
PC에서는 맞던 본선 트리가 iOS Safari에서 넘쳤고 팀 이름과 세트 표가 잘리거나 세로로 줄바꿈됐음.

**원인**
당시 CSS `zoom`이 iOS Safari·일부 인앱 브라우저에서 동작하지 않아 모바일 축소가 적용되지 않았음.

**해결(당시)**
`bracket-render.js`가 `transform: scale()`과 스케일된 부모 크기를 사용하도록 바꾸고 연결선을 같은 좌표계에서 그렸음. PC 전체화면·일반 화면·모바일의 표시 목적을 분리하고 모바일 전체화면 버튼 겹침도 정리함.

**검증·범위**
이 항목은 당시 viewport 결함과 렌더링 전략의 역사다. 현재 대시보드의 화면맞춤·브래킷 viewport와 모바일 안내는 [`인수인계_노트.md`](./인수인계_노트.md) 및 현재 소스를 기준으로 하며, 이 항목의 `최종` 표현은 영구 보증이 아니다.

---

## 10. 관객 공개 버튼을 한 번 누르면 사라져서, 라운드별 결과 갱신에 쓸 수 없던 문제

**증상**
본선 구조를 한 번 공개하면 공개 막대가 사라져 6강·준결승·결승 결과를 라운드별로 다시 공개할 수 없었음.

**원인**
게이트가 대진 구조 변경에만 걸리고 점수는 `saveFinalMatches`로 즉시 저장되어 구조 초안과 공식 결과의 공개 경계가 달랐음.

**해결(당시)**
점수도 로컬 초안과 `bracketPublishPending`에 모으고 공개 Callable에서 구조·점수·승자 전파를 함께 검증하게 함. 공개 막대는 대진이 있는 동안 유지하고 pending/완료 상태를 구분함.

**검증·범위**
본선 라운드 결과 공개와 공개 전 이탈 경고를 당시 동작 기준으로 정리한 기록이다. 현재 공개 경계의 운영 설명은 인수인계 문서를 우선한다.

---

## 11. 추첨 대상 동률 팀이 자동으로 본선 진출팀에 포함되던 문제

**증상**
승점·세트득실·득실차·승자승이 같은 팀에 추첨 표시가 있어도 서로 다른 순위와 자동 진출 추천이 부여됐음.

**원인**
승자승으로 해소되지 않은 동률에 등록·조회 배열 순서를 사용해 진출선에 걸린 팀을 자동 선택할 수 있었음.

**해결(당시)**
해소되지 않은 팀을 공동 순위로 묶고 `needsLottery`를 표시함. 공동 순위가 진출선을 걸치면 자동 선택하지 않고, 추첨 후 관리자가 직접 선택하게 함. 다자 동률의 승자승은 동률 팀 간 승수라는 기존 해석을 유지함.

**검증·범위**
공동·부분 동률, 진출선 경계와 자동 선택을 단위검사로 확인한 기록이다. 현재 진출 검증과 다자 승자승 해석을 이 문서에서 새 규칙으로 읽지 말고 [`AGENTS.md`](./AGENTS.md)·인수인계 문서를 확인한다.

---

## 12. 예선 대진 재생성·삭제 후 코트 운영 데이터가 남던 문제

**증상**
예선·조·팀을 다시 만들거나 삭제한 뒤 공식 경기와 코트 배정·기록관 작업·대기열이 서로 다른 경기 ID를 가리켰음.

**원인**
과거 브라우저가 Firestore 공식 문서를 직접 삭제·생성했고, 코트 워크플로는 Functions의 별도 문서와 큐로 관리되어 변경 경계가 나뉘었음.

**해결(당시)**
인증된 관리자용 `mutatePrelimStructure` Callable에서 구조·공식 경기·배정·워크플로·큐를 함께 처리함. 기록이 있는 경기는 거부하고, 기록 없는 경기만 원자적으로 제거·재투영하며 본선 의존성과 Rules 우회도 차단함.

**검증·범위**
큐 재연결, 금지 상태 무변경, 라운드로빈·링크제 생성, 참조와 Rules를 에뮬레이터로 확인했다. 이 항목의 `브라우저 직접 쓰기`는 당시 원인 설명이며 현재 허용 경로가 아니다.

---

## 13. 본선 대진표 초기화가 Firestore 권한 오류로 실패하던 문제

**증상**
본선 경기 문서가 있으면 관리자 초기화가 Firestore 권한 오류로 실패하고 문서가 남았음.

**원인**
브라우저가 직접 본선 문서를 삭제하려 했고, 경기만 지우면 코트 배정·워크플로·큐에 삭제된 ID가 남을 수 있었음.

**해결(당시)**
인증된 관리자용 `clearFinalStructure` Callable에서 시작 전이고 기록 없는 경기만 본선·배정·워크플로·큐와 함께 정리함. 본선 직접 쓰기 Rules는 유지하고 다른 부문의 예선 큐만 재연결함.

**검증·범위**
허용·거부·무변경·큐 재연결을 에뮬레이터로 확인한 당시 기록이다. 여기서 말하는 브라우저 삭제는 현재 초기화 절차가 아니다.

---

## 14. 대회 초기화·복원 중에도 관객 화면이 실시간 중계로 표시되던 문제

**증상**
전체 초기화·백업 복원 중 삭제·복원 중간 자료와 `실시간 중계중` 상태가 관객에게 보였음.

**원인**
서버 필드는 `maintenance.enabled`인데 대시보드는 존재하지 않는 `maintenance.active`를 봤고, 자동 송출 타이머가 유지보수 화면과 분리되어 있었음.

**해결(당시)**
대시보드가 `maintenance.enabled`를 확인하고 내용·부문 선택·송출 상태를 숨기며 타이머를 중지하도록 수정함. 종료 시 최신 구독으로 다시 렌더링함.

**검증·범위**
브라우저 모의 Firestore 구독으로 진입·종료를 확인했다. 유지보수 운영 순서는 현재 인수인계 문서에만 둔다.

---

## 15. 확정한 부전승이 관객 대진표에서 일반 경기 전 상태로 바뀌던 문제

**증상**
확정 부전승이 관객 화면에서 상대 없는 일반 경기 전으로 보였고 다음 라운드 진출과 모순됐음.

**원인**
공개 함수가 `bye` 구조 상태를 허용하지 않았고, 대시보드가 공식 승인 번호가 없는 경기를 대부분 `pending`으로 덮어썼음.

**해결(당시)**
서버 공개에서 `bye`를 구조 상태로 허용하고 공개 변환 함수가 `empty`, `waiting`, `pending`, `bye_pending`, `bye`, `in_progress`를 보존하게 함. 승인 전 점수·결과·승자는 계속 숨겼음.

**검증·범위**
단위·에뮬레이터·브라우저 모의 구독에서 부전승 표시와 미승인 점수 비노출을 확인한 기록이다. 실제 경기 규칙이나 진출 해석을 변경한 항목이 아니다.

---

## 16. 일반 관객 대시보드가 경기장 자동 송출 모드로 잠기던 문제

**증상**
일반 대시보드의 부문 선택이 비활성화되고 관리자 경기장 설정에 따라 자동 전환됐음.

**원인**
URL의 `display=venue`뿐 아니라 저장된 `venueDisplay.mode`의 존재만으로 경기장 전용 모드라고 판단했음.

**해결(당시)**
경기장 전용 판정을 URL로 한정해 일반 화면은 선택을 허용하고, 경기장 URL에서만 자동·부문 고정 설정을 적용함.

**검증·범위**
일반 URL과 경기장 URL의 버튼·자동 전환을 브라우저 모의 구독으로 각각 확인했다. 현재 송출 운영 설명은 인수인계 문서를 우선한다.

---

## 17. 본선 점수가 관객 화면 공개 버튼을 거치지 않고 즉시 노출되던 문제

**증상**
관리자가 본선 점수를 저장하자 관객에게 즉시 보였고, 새 대진의 점수 저장은 공식 워크플로가 없어 실패할 수 있었음.

**원인**
본선 점수가 예선 관리자 수정과 같은 `directEditOfficialScore` 경로를 사용해 로컬 초안과 공식 데이터의 경계가 일치하지 않았음.

**해결(당시)**
본선 점수는 브라우저 로컬 초안에만 반영하고, 공개 시 서버가 점수·승자·다음 라운드·제출 버전·동시 편집을 재검증해 한 트랜잭션으로 반영하도록 변경함. 새 경기의 워크플로와 큐도 공개 경계에서 생성·재계산함.

**검증·범위**
새 대진, 미승인 비노출, 오래된 초안 거부, 기록관 제출과 후속 경기 전파를 단위·에뮬레이터로 확인한 당시 기록이다. 이 항목의 `directEditOfficialScore`는 과거 원인 설명이지 본선 공개 우회 방법이 아니다.

---

## 18. 백업 복원 후 백업에 없는 기존 데이터가 남고 대회 설정이 복원되지 않던 문제

**증상**
복원 후 대회명·진출팀 수가 이전 값으로 남고, 백업에 없는 팀·경기·코트 운영 문서가 잔존해 서로 다른 시점의 데이터가 섞일 수 있었음.

**원인**
루트 문서가 복원 대상에서 빠졌고 기존 문서를 덮어쓰기만 했음. 백업 생성도 여러 컬렉션을 독립적으로 읽어 시점이 섞일 가능성이 있었음.

**해결(당시)**
관리자 전용 서버 트랜잭션으로 일관된 export를 만들고, 허용된 루트 필드와 사업 컬렉션만 manifest에 기록함. 청크 적용 후 백업에 없는 문서를 정리하고 체크섬·참조·큐 관계를 검증했으며, 관리자·기록관 권한 자료는 보존함.

**검증·범위**
정확한 루트 복원, 잔존 삭제, 보호 문서 유지, 청크·체크섬·재시도와 오래된 manifest 차단을 에뮬레이터로 확인했다. 현재 v3 복원 운영은 [`인수인계_노트.md`](./인수인계_노트.md)를 기준으로 한다.

---

## 19. 변조된 기록관 잠금으로 코트 경기를 해제할 수 없던 문제

**증상**
빈 문자열·비정상 잠금 토큰으로 경기 가져오기·제출·취소가 실패했고 관리자 강제 해제도 막혀 큐가 멈출 수 있었음. 시도 횟수와 재개 상태도 별도 요청으로 변조될 여지가 있었음.

**원인**
Rules가 토큰 자료형만 확인하고 길이·이전 상태 연결을 검사하지 않았으며, 배정 문서 단독 변경으로 상태·`attemptCount`를 바꿀 수 있었음.

**해결(당시)**
토큰 길이, `resumeDraftState`, 실제 시작 전환 안의 `attemptCount`, 배정과 워크플로의 결합을 서버·Rules에서 검증함. 과거 빈 토큰 잠금은 관리자 권한으로 복구할 수 있게 함.

**검증·범위**
빈·짧은 토큰, 잘못된 재개 상태, 임의 시도 횟수와 배정 단독 변경의 zero-write 거부를 에뮬레이터로 확인한 기록이다.

---

## 20. 기록관이 관리자 감사 문서 ID를 선점해 승인을 막을 수 있던 문제

**증상**
기록관이 미래 관리자 감사 문서 ID를 먼저 만들면 불변 감사 문서 생성이 실패해 공식 처리가 반복해서 막힐 수 있었음.

**원인**
기록관과 서버가 같은 `auditEvents` ID 공간을 사용했고 생성 Rules가 기록관 전용 접두어·grant·연결을 검사하지 않았음.

**해결(당시)**
기록관 ID를 `recorder:`로, 서버 전환·구조 감사 ID를 `server:`로 분리하고 길이·actor·시간·workflow 연결을 Rules에서 검사함. 기존 감사 문서는 수정·삭제하지 않고 신규 이벤트부터 분리함.

**검증·범위**
정상 기록관 작업, 서버 네임스페이스 선점, 독립 감사 문서 선점 거부를 에뮬레이터로 확인한 당시 기록이다.

---

## 21. 관리자 예선 점수 직접 입력 후 코트 대기열이 완료 경기에서 멈추던 문제

**증상**
관리자가 코트 배정 예선을 직접 완료하면 공식 문서만 승인되고 큐가 완료 경기를 계속 가리킬 수 있었으며, 기록관 작업이 덮어써질 여지도 있었음.

**원인**
`directEditOfficialScore`가 공식 점수·워크플로만 갱신하고 큐를 재투영하지 않았음. 공식 revision만 확인해 기록관 잠금·검수·반려 상태와 불완전한 승인을 충분히 구분하지 못했음.

**해결(당시)**
초기 기록 없는 경기만 직접 입력을 허용하고, 코트 경기에는 공식·큐 revision을 함께 CAS함. 완료 후 `projectCourtQueue`로 현재·다음·우선순위를 재계산하고, 기록관 상태나 불완전한 승인에는 zero-write로 거부함.

**검증·범위**
큐 재투영, 오래된 요청, 기록관 상태, 불완전 승인·정정과 잔존 점수 데이터를 에뮬레이터로 확인한 기록이다. 과거의 `직접 입력` 경로는 현재의 인증·서버 검증을 우회하지 않는다.

---

## 22. 정정 대상을 바꿔도 이전 미리보기의 다른 경기가 수정될 수 있던 문제

**증상**
정정 대상을 바꾼 뒤에도 적용 버튼이 활성화되어 이전 경기 ID가 수정될 수 있었음.

**원인**
클라이언트가 선택·팀·조·공식 경기·배정·workflow·큐 변경에 맞춰 미리보기를 무효화하지 않았고, 서버도 경기 ID와 일부 revision만 신뢰했음.

**해결(당시)**
서버가 전체 큐·배정·workflow·공식 경기와 예상 projection의 SHA-256 계획 지문을 만들고 `planToken`만으로 재계산·CAS하도록 함. 선택·상태가 바뀌면 클라이언트 미리보기를 버리고 늦은 응답도 저장하지 않게 함.

**검증·범위**
대상·지문 변조, 각 revision 변경, 토큰 재사용과 정상 계획 일치를 에뮬레이터로 확인했다. 정정 사유·운영 절차는 현재 인수인계 문서에 둔다.

---

## 23. 예선 라운드 번호가 표시 순서와 원형 라운드 의미를 혼용하던 문제

**증상**
4팀 라운드로빈에서 `round`가 `1,1,2,2,3,3`으로 중복되어 문서 ID가 화면 순서를 좌우했고, 드래그 뒤에는 `1..6`으로 달라졌음.

**원인**
원형 알고리즘의 동시 라운드와 관리자 화면의 고유 표시 순서를 한 필드에 함께 저장했음.

**해결(당시)**
브라우저와 서버 생성기가 경기 표시 순서를 고유한 `1..N`으로 저장하도록 통일함.

**검증·범위**
4팀 조합과 순서 테스트를 추가한 데이터 모델 수정 기록이다. `round`를 경기 규칙의 라운드 의미로 해석하지 않는다.

---

## 24. 미완료 예선에서 추첨·진출 추천이 확정되고 잘못된 저장 점수가 순위에 반영되던 문제

**증상**
일정이 없거나 일부만 끝난 조에서도 추첨·자동 진출 추천이 나타났고, 비정상 세트와 위조된 파생 필드가 순위에 반영될 수 있었음.

**원인**
순위 계산이 일정 완료 여부를 구분하지 않았고 일부 경로가 원본 세트 대신 저장된 결과 필드를 신뢰했음. 서버 평가기도 문자열 숫자·초과 세트를 엄격히 거부하지 않았음.

**해결(당시)**
미완료 조를 `provisional`로 표시해 추첨·자동 추천을 중지하고, 브라우저·서버가 원본 정수 세트의 상한·듀스·종료 순서·세트 수를 검증해 파생 결과를 다시 계산하도록 통일함.

**검증·범위**
이 항목은 진출 결과의 준비 상태를 잘못 확정한 결함 기록이다. 현재 다자 동률의 승자승 해석은 변경하지 않았으며 현재 검증 근거는 [`AGENTS.md`](./AGENTS.md)와 인수인계 문서에 둔다.

---

## 25. 관리자 브라우저가 서버 소유 대회 루트 필드를 바꿀 수 있던 문제

**증상**
변조된 관리자 브라우저 요청으로 유지보수·기록관 기능·코트 topology revision 등 서버가 소유해야 할 루트 필드를 Callable 밖에서 바꿀 수 있었음.

**원인**
Rules가 관리자 역할만 확인하고 변경 가능한 루트 필드와 각 스키마를 제한하지 않았음.

**해결(당시)**
브라우저 변경을 `name`, 부문별 `qualifyPerGroup`, `venueDisplay`, 서버 시각 `updatedAt`으로 제한하고 유지보수·기록관·topology·미등록 필드 변경을 거부함.

**검증·범위**
허용 스키마와 거부 경계를 테스트한 당시 기록이다. 이후 진출 보호가 추가되어 **현재 `qualifyPerGroup` 변경은 서버 명령을 사용**한다. 현재 권한은 인수인계 문서와 `firestore.rules`를 함께 확인한다.

---

## 26. 새 예선 경기의 워크플로 누락과 오래된 코트 편집 초안 문제

**증상**
예선 생성 직후 코트 설정을 저장하기 전에는 관리자 점수가 실패했고, 다른 변경 중 오래된 코트 초안이 삭제된 경기를 다시 저장하거나 최신 revision을 빌릴 수 있었음.

**원인**
생성 트랜잭션이 공식 경기만 만들었고 플래너가 dirty 상태에서도 실시간 revision을 기준값으로 교체하며 사라진 키를 유지했음.

**해결(당시)**
예선 생성 시 미배정 `scheduled` assignment와 `idle` workflow를 원자적으로 만들고, 플래너가 편집 시작 revision을 고정하며 서버 CAS로 오래된 저장을 zero-write 거부하게 함.

**검증·범위**
신규 경기와 삭제·동시 변경 시나리오의 저장 경계를 고정한 당시 수정 기록이다.

---

## 27. 승인 결과 정정 후 재경기와 본선 후속 대진이 불일치하던 문제

**증상**
정정 경기가 기록관에게 다시 배정되지 않거나, 본선 상위 경기를 철회한 뒤 관객의 이전 승자·점수와 후속 경기·큐가 서로 달라질 수 있었음.

**원인**
정정 계획과 기록관 Rules의 상태쌍이 달랐고 공식 경기·후속 참가팀·dependency·assignment·priority queue를 하나의 그래프로 재투영하지 않았음.

**해결(당시)**
재경기를 `replay_required + rejected`로 통일하고 공식 결과를 `officialCurrent:false`로 철회함. 후속 이력이 없는 안전한 대진만 참가팀·점수·승자·dependency·영향 큐를 원자적으로 재투영하고, 이력이 있으면 정정을 거부함.

**검증·범위**
정정 ID와 쓰기 수의 상한, 재경기·후속 이력 경계를 테스트한 기록이다. 실제 재경기 판단은 현재 운영 문서를 따른다.

---

## 28. 본선 검수 버전·정정 사유·참가팀 정보가 정확히 보존되지 않던 문제

**증상**
같은 점수의 새 제출이 도착하면 관리자가 잘못된 버전을 승인할 수 있었고, 동일 점수 재제출이 검수를 끝내지 못했음. 정정 사유와 팀 이름도 감사 기록과 달라질 수 있었음.

**원인**
점수 배열만 비교하고 `submissionVersion`을 공개 초안에 넣지 않았으며, 클라이언트 팀 객체와 사유를 서버 경계에서 정규화·보존하지 않았음.

**해결(당시)**
제출 버전을 workflow·snapshot과 CAS하고, 같은 점수 재제출도 revision 증가 없이 승인 완료함. 참가팀은 트랜잭션에서 읽은 팀 문서로 재구성하고 정정 revision·사유·승인 메타데이터를 불변 감사 기록에 보존함.

**검증·범위**
본선 검수의 버전·감사 진실성을 보강한 기록이며 현재 승인 운영 절차를 대신하지 않는다.

---

## 29. 대시보드 연결·송출 시계·렌더링 수명과 모바일 표시 문제

**증상**
일부 구독 실패·캐시 데이터에서도 실시간처럼 보였고, 경기장 화면마다 시계 차이로 부문 전환이 달랐음. 저장 문자열·렌더 타이머·링 크기·모바일 탭과 모달에도 문제가 있었음.

**원인**
구독 상태와 snapshot metadata, 서버 시각 offset, 저장값의 HTML 해석, 렌더 disposer와 동적 레이아웃이 충분히 분리되지 않았음.

**해결(당시)**
여러 feed를 서버 확인·캐시·오류로 집계하고 서버 epoch와 왕복 중간값으로 송출을 동기화함. 실패 시 타이머를 멈추고 재시도하며 `textContent`, 렌더 disposer, 동적 링, 모바일 탭·viewport 제한 모달을 적용함.

**검증·범위**
서버 시계 기반 전환은 이후 변경되어 **현재 자동 전환은 각 화면의 독립 타이머를 사용**한다. `getServerClock`은 연결 진단용으로 남아 있다. 현장 기기·Wi-Fi 리허설 완료를 뜻하지 않는다.

---

## 30. v3 정확 복원에서 Timestamp와 큰 청크·잘못된 envelope를 처리하지 못하던 문제

**증상**
Firestore Timestamp가 일반 JSON 객체가 되고, 문서 수만 기준으로 만든 청크가 manifest 1MiB 한도를 넘거나 출처·대회 정보가 빠진 v3 파일이 복원 단계에 들어갈 수 있었음.

**원인**
Firestore 값 codec이 없었고 원본 문서를 manifest에 중첩 저장했으며 클라이언트 envelope 검증이 느슨했음.

**해결(당시)**
허용 태그로 Timestamp를 인코딩·복호화하고 알 수 없는 태그를 거부함. 청크를 바이트·개수·총량으로 제한하고 manifest에는 descriptor만 저장하며 각 resume 요청에서 checksum을 검증함. `app`, `main`, `info`와 v3 최상위 키를 엄격히 확인함.

**검증·범위**
정확 복원의 형식·크기·타입 경계를 보강한 기록이다. 현재 portable backup은 v3 기준이며 운영 복원 전제는 [`인수인계_노트.md`](./인수인계_노트.md)에 둔다.

---

## 31. 기록관 인증 공급자와 클라이언트 감사 내용이 서버 정책과 달랐던 문제

**증상**
문서에는 Google 기록관만 적혀도 다른 인증 계정이 grant를 사용하거나 받을 수 있었고, 클라이언트가 실제 전환과 다른 `before`·`after` 감사 내용을 만들 수 있었음.

**원인**
공급자 제한과 snapshot 진실성을 UI에만 의존하고 Functions·Rules에서 검증하지 않았음.

**해결(당시)**
Google 공급자 또는 신뢰된 관리자 예외만 grant·Callable·Rules 경계를 통과하게 하고, 감사 키·actor와 claim/draft-save의 실제 workflow·lock·draft를 서버 상태와 대조함.

**검증·범위**
기록관 Google+접근 코드와 관리자 예외의 현재 경계는 인수인계 문서를 기준으로 한다. 과거 문서의 `로그인 없는 운영` 또는 익명 인증 해석은 폐기했다.

---

## 32. 배포 프로젝트·런타임·의존성·운영 문서가 현재 구현과 달랐던 문제

**증상**
과거 문서에 클라이언트가 대회 ID를 바꿀 수 있다는 설명, 로그인 없는/익명 운영, Hosting 또는 Rules만 배포하는 안내, 폐기된 백업·3위전 설명과 다른 Node 런타임이 남아 있었음.

**원인**
정적 클라이언트에서 인증·Functions 중심 구조로 바뀌었는데 운영 문서와 패키지 기준이 함께 갱신되지 않았음.

**해결(당시)**
Firebase 프로젝트와 대회 루트를 고정하고 Node 22·Java 21·lockfile 기준으로 정리함. 인증 초기 구성, 서버를 포함한 배포 범위, v3 복원 기준을 문서에 반영함. 이전 프로젝트·익명 운영·무제한 직접 쓰기 및 서버 변경이 필요한데도 Hosting만 배포하라는 지침을 폐기함.

**검증·범위**
배포 대상은 현재 변경 범위에 따라 고른다. 서버 계약과 무관한 화면 변경은 Hosting만 배포할 수 있으므로, 모든 변경에 전체 배포가 필요하다는 뜻도 아니다. 현재 절차는 [개발 가이드](./개발_워크플로우_가이드.md)를 따른다.

---

## 33. 기록관의 본선 2:0·2:1 점수와 제출 전 확인값이 일치하지 않던 문제

**증상**
2:0 결과에 비활성 3세트 `0:0`이 붙어 서버에서 거부되거나, 2:1이 두 세트로 줄어들 수 있었음. 확인 화면을 연 뒤 입력을 바꾸면 payload도 달라질 수 있었음.

**원인**
종료 세트 계산이 중복 구현됐고 제출 시 확인 snapshot이 아니라 변경 가능한 DOM을 다시 읽었음.

**해결(당시)**
첫 두 세트가 2:0일 때만 두 세트로 정규화하고 1:1이면 3세트를 유지하도록 클라이언트·서비스·서버를 맞춤. 검토 진입 시 불변 payload를 고정해 제출·재시도에 사용함.

**검증·범위**
본선 점수 형식과 확인 경계를 맞춘 역사 기록이며 경기 규칙을 새로 정의한 문서가 아니다.

---

## 34. 실시간 갱신·오프라인·페이지 이탈로 입력 중 점수가 사라지던 문제

**증상**
팀·경기 snapshot 갱신이나 모바일 탭 종료 때 미저장 점수가 사라지고, 오래된 기기 초안이 최신 서버 초안을 덮을 수 있었음. 빈 입력이 0으로 오인되기도 했음.

**원인**
서버 초안·로컬 편집값·dirty·검토값을 분리하지 않고 snapshot마다 입력 DOM을 다시 만들었음.

**해결(당시)**
경기·사용자별 로컬 초안과 revision을 분리하고 dirty DOM을 재생성하지 않게 함. 충돌은 사용자 선택으로 처리하고 빈칸·오류 입력을 명시적으로 거부함. 접근 코드와 잠금 토큰은 저장하지 않음.

**검증·범위**
기기 초안 보존과 revision 충돌을 다룬 기록이다. 기기 저장 실패 시 동작은 현재 기록관 운영 문서를 우선한다.

---

## 35. 방치된 잠금·중복 탭·응답 유실을 안전하게 복구하지 못하던 문제

**증상**
기기 종료 후 잠금이 남고, 복제 탭이 입력권을 번갈아 가져가며, 제출 응답 유실 시 중복 여부를 알 수 없었음.

**원인**
서버 만료 시각·세션 식별자와 요청 내용에 결합된 멱등 키가 없었음.

**해결(당시)**
UID·토큰·탭 세션과 3분 server-time lease, 60초 heartbeat, 감사 가능한 재개·탭 인수를 도입함. 제출·종료·폐기에 operation ID와 payload fingerprint를 저장해 동일 요청은 같은 결과를 반환하고 다른 요청의 ID 재사용은 거부함.

**검증·범위**
기록관 잠금 수명과 멱등성의 당시 설계·수정 기록이다. 실제 행사 운영에서의 장시간·다기기 결과는 별도 확인 대상이다.

---

## 36. 공용 코드 반복 검증과 기록관 권한 수명·개별 폐기가 없던 문제

**증상**
공용 코드 검증을 반복할 수 있었고 grant가 만료되지 않거나 개별 폐기할 수 없었음. 코드 회전 뒤 관리자의 활성 목록이 실제 상태와 달라질 수 있었음.

**원인**
코드 형식·실패 창·cooldown, grant 만료·상태, 서버 기준 effective status가 없었음.

**해결(당시)**
24자 base64url 형식과 UID별 실패 창·cooldown을 트랜잭션으로 강제하고, grant에 12시간 만료·상태를 둠. 개별 폐기와 전체 회전의 영향을 관리자 화면에 표시함.

**검증·범위**
현재 기록관 인증은 Google 로그인과 유효한 대회 접근 코드가 함께 필요한 신뢰 기반 흐름이다. 만료·폐기 상태와 실제 행사 적용 여부는 인수인계 문서와 배포 증빙을 따르며, 이 항목은 새 인증 단계를 제안하지 않는다.

---

## 37. 기록관 브라우저와 서버의 인증·유지보수 정책이 달랐던 문제

**증상**
서버가 유지보수나 비 Google 세션을 거부해도 화면은 작업 가능으로 보였고, 온라인 복귀 뒤 권한 확인이 로딩에 머물 수 있었음.

**원인**
브라우저가 접근 설정을 직접 읽었고 연결·공급자·관리자 예외·grant 만료를 하나의 상태로 재계산하지 않았음.

**해결(당시)**
challenge·유지보수·현재 공급자·관리자 문서·grant를 결합한 상태 머신과 만료 타이머·온라인 복귀 재계산을 적용함. 유지보수 중에는 로컬 점수를 보존하되 입력·제출을 중지함.

**검증·범위**
브라우저 표시와 서버 권한 경계를 맞춘 기록이다. 현재 인증 상태의 운영 처리와 복구 안내는 인수인계 문서를 기준으로 한다.

---

## 38. 관리자가 현재 경기 점수를 직접 확정하면 기록관이 권한 만료로 멈추던 문제

**증상**
관리자가 현재 경기 점수를 확정한 뒤 큐에는 다음 경기가 있어도 기록관 화면이 완료 경기와 `권한이 없거나 권한이 만료되었습니다`에 남았음.

**원인**
관리자 트랜잭션이 이전 workflow를 승인하고 큐를 전진시켰는데, 기록관의 이전 단건 listener가 정상적인 `permission-denied`를 받자 클라이언트가 계정 만료로 오인했음.

**해결(당시)**
유효한 grant가 이미 연 단건 `submitted`·`approved` listener는 전환 중 계속 읽도록 하되 목록 조회는 차단함. 클라이언트는 해당 오류를 다음 경기 로딩으로 처리함.

**검증·범위**
큐 revision 증가·다음 경기 전환·다음 workflow 읽기를 에뮬레이터 회귀로 확인한 기록이다. 현재 제출·승인 워크플로의 단건 읽기는 유효한 권한을 가진 실제 제출자와 관리자로 제한되며, grant만 가진 모든 기록관에게 공개되는 것은 아니다.

---

## 39. 완료 경기가 코트 큐의 현재 경기로 남아 기록관이 다음 경기로 갈 수 없던 문제

**증상**
완료·승인된 경기가 `currentMatchKey`에 남아 기록관에게 입력 버튼이나 다음 경기 이동이 보이지 않았음.

**원인**
코트 배정 전에 관리자 확정이 일어난 과거 데이터에서 stale cursor가 생겼고, 이후 점수 정정은 큐를 되감지 않았음.

**해결(당시)**
기록관 화면이 잠금 없는 완료 경기를 발견하면 `다음 경기로 이동`을 표시하고, 권한·코트·queue revision을 서버 Callable에서 검증해 전체 큐를 한 번 재투영하고 감사 이력을 남김.

**검증·범위**
정상 관리자 확정 전진과 과거 stale queue 복구를 에뮬레이터로 확인한 기록이다. 버튼 사용 조건과 현재 운영 안내는 인수인계 문서를 따른다.

---

## 40. 기록관 권한 목록 Callable이 배포 직후 internal 오류를 반환한 문제

**증상**
관리자 진입 직후 `기록관 권한 목록 실패 (functions/internal): internal` 알림이 반복됨.

**원인**
당시 Functions 배포 timeout 중 새 Cloud Run 서비스는 생성됐지만 Firebase CLI의 invoker IAM 단계가 완료되지 않아 요청이 함수 코드에 도달하기 전에 거부됨.

**해결(당시)**
해당 서비스의 Cloud Run 진입 권한을 복구하고, Callable 내부 Firebase ID token·관리자 문서·grant 검증은 유지함.

**검증·범위**
무인증 요청이 Cloud Run에서 조기 차단되지 않고 함수의 정상 `UNAUTHENTICATED` 경계까지 도달하는지 확인한 당시 배포 장애 기록이다. 이를 임의의 IAM 권한 변경이나 함수 내부 인증 우회 지침으로 사용하지 않는다.

---

## 41. 검수 카드에서 세트별 제출 점수를 한눈에 확인하기 어려웠던 문제

**증상**
기록관 제출 점수가 작은 한 줄 칩으로만 보여 세트별 팀 점수를 승인 전에 대조하기 어려웠음.

**원인**
점수를 기록관·제출 시간과 같은 보조 메타데이터로 취급하고 세트·팀 대응을 표로 제시하지 않았음.

**해결(당시)**
검수 카드에 팀명을 열로 둔 `기록관 제출 점수` 표와 세트별 큰 숫자·승리 강조·전체 스코어를 추가하고 모바일 크기를 조정함.

**검증·범위**
검수 가독성 개선의 역사 기록이며 현재 화면의 운영 절차를 정의하지 않는다.

---

## 42. Hosting 루트에 에뮬레이터 debug 로그가 포함된 문제

**증상**
루트 Hosting에 테스트가 만든 `firestore-debug.log`가 포함되어 공개 URL에서 접근 가능했음.

**원인**
기존 Hosting ignore가 숨김·테스트·Functions 경로는 제외했지만 일반 `*.log`를 제외하지 않았음.

**해결(당시)**
Hosting ignore에 `**/*.log`를 추가하고 배포 후 기존 로그 URL이 404인지 확인함.

**검증·범위**
당시 배포에서 확인한 노출 방지 결함과 404 검증 기록이다. 현재 Hosting 제외 목록·배포 절차는 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md)를 기준으로 한다.

---

## 43. 본선 대진표 관객 공개가 결승 downstream 검증에서 실패한 문제

**증상**
본선 공개 시 `Final championship match cannot have a downstream match` 오류로 거부됐음.

**원인**
생성기가 결승 `nextMatchId`는 `null`로 만들면서 사용하지 않는 `nextSlot`을 남겼고 서버의 구조 무결성 검증이 이를 거부함.

**해결(당시)**
결승의 `nextMatchId`·`nextSlot`을 모두 `null`로 통일하고 공개 직렬화에서도 downstream이 없으면 slot을 제거함. legacy 초안은 서버 경계에서 안전하게 정규화한 뒤 검증함.

**검증·범위**
결승 그래프 구조를 바로잡은 역사 기록이며 현재 본선 공개를 우회하거나 자동으로 승인하는 안내가 아니다.

---

## 44. 본선 대진 생성 후 v3 백업 저장이 잘못된 파일로 거부된 문제

**증상**
본선 생성·공개 뒤 `백업 파일 저장`이 `올바른 백업 파일이 아닙니다`로 실패했음.

**원인**
본선 `courtAssignments`에 예선 문서와 달리 `attemptCount`가 빠져 v3 정규화기가 불완전 문서로 판단함.

**해결(당시)**
새 본선 assignment에 `attemptCount: 0`을 기록하고, 누락만 백업 경계에서 의미가 같은 0으로 정규화함. 문자열·음수는 계속 거부함.

**검증·범위**
당시 운영 Firestore의 116개 사업 문서를 서버 백업 응답 형태로 변환해 검증한 **시점 한정** 증거다. 현재 데이터 규모·백업 성공을 보증하지 않는다.

---

## 45. 일반 관리자 화면에 실효성 없는 migration manifest 기능이 노출된 문제

**증상**
일반 운영 화면에 migration manifest ID·생성·적용 기능이 노출됐지만 사용 시점과 효과가 분명하지 않았음.

**원인**
과거 구조 전환용 내부 점검 기능이 남아 있었고, 실제 데이터 변환 없이 상태만 `applied`로 표시했음.

**해결(당시)**
관리자 UI·이벤트·Functions export·서버 구현·테스트·Rules 항목을 제거함. 당시 운영 Firestore에서 삭제할 기존 manifest는 없다고 확인했고 백업·복원은 별도로 유지함.

**검증·범위**
기능 제거와 당시 데이터 확인의 역사 기록이다. migration manifest를 현재 복원 기능으로 오인하지 않는다.

---

## 46. 초기 버전(v1) 백업 파일을 복원할 수 없던 문제

**증상**
초기 v1 백업을 선택하면 `올바른 백업 파일이 아닙니다`로 거부됐음.

**원인**
당시 정확 복원은 남녀 부문·assignment·queue·workflow를 포함한 v3만 이해했고, v1은 단일 부문·운영 문서 부재로 그대로 병합할 수 없었음.

**해결(당시)**
v1 변환기를 추가해 부문을 선택하고 v3 구조로 보강했으며 `2026_청춘girl_Go!` 백업을 여자부 기준 조 3개·팀 11개·예선 15경기·본선 7경기·assignment/workflow 각 22개로 변환 검증함.

**현재 상태·범위**
현재도 관리자 화면의 `handleRestoreFile`은 v1 파일의 부문을 확인한 뒤 `upgradeLegacyBackup`으로 v3 변환을 수행한다. **서버 복원 형식이 v3라는 사실과 관리자 화면의 v1 변환 지원은 모순되지 않는다.** 부문을 선택해도 해당 부문에 덧붙이는 방식이 아니라 전체 대회 대체 복원이므로 [인수인계 노트](./인수인계_노트.md)의 경고를 따른다. 위 수치는 당시 예제 파일의 검증 결과다.

---

## 47. 대회 전체 초기화가 안전 백업 순서 때문에 항상 취소되던 문제

**증상**
전체 초기화가 데이터를 지우지 않고 `초기화가 완료되지 않았습니다`만 표시했음.

**원인**
클라이언트가 maintenance를 먼저 시작한 뒤 백업을 요청했는데 서버는 일관된 snapshot을 위해 maintenance 중 export를 거부했고, 오류 처리에서 초기화도 취소했음.

**해결(당시)**
안전 백업을 먼저 받고 maintenance 준비와 원자적 초기화를 이어가도록 순서를 바꾸고, 남은 이전 토큰을 취소한 뒤 새 흐름을 시작하게 함.

**검증·범위**
에뮬레이터에서 초기화 전 v3 백업 성공과 reset 순서를 확인한 역사 기록이다. 실제 대회 데이터로 초기화·복원을 리허설했다는 뜻은 아니다.

---

## 48. 대회 진행 중 조 이름을 변경할 수 없던 문제

**증상**
조 생성 후 오타·운영 명칭을 고칠 UI가 없어 이름 변경을 위해 조를 삭제·재구성해야 했음.

**원인**
조 목록에 생성·정렬·삭제만 있고 이름 변경 서비스와 버튼이 없었음.

**해결(당시)**
조 pill에 수정 기능을 추가하고 공백·40자·동일 부문 중복을 검증한 뒤 `name`만 갱신함. `groupId`와 팀·경기 참조는 유지됨.

**검증·범위**
조 이름만 바꾸는 표시 수정의 역사 기록이다. 구조 변경·경기 결과 삭제·규칙 변경 기능으로 해석하지 않는다.

---

## 기록 작성 원칙

- 새 결함은 `증상 / 원인 / 해결(당시) / 검증·범위`로 기록하되, 해결 문장을 현재 지시문처럼 쓰지 않습니다.
- 확인한 테스트·배포·날짜·데이터 수치만 적고, 현장 기기·행사·네트워크를 확인하지 않았다면 그 한계를 명시합니다.
- 현재 운영 상세와 인증·권한·복원 절차는 [`인수인계_노트.md`](./인수인계_노트.md), 명령과 개발·배포 흐름은 [`개발_워크플로우_가이드.md`](./개발_워크플로우_가이드.md)에만 갱신합니다.
- 과거 프로젝트 ID, 익명 운영, 무제한 직접 쓰기·보호 우회와 서버 변경을 누락한 배포를 새 해결책으로 제시하지 않습니다. 지원 여부는 과거 항목만으로 단정하지 말고 현재 UI와 서버 경계를 함께 확인합니다.
