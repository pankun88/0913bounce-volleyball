# 바운스발리볼 웹앱 — 문제해결 기록

개발 중 발생한 오류와 해결 방법을 시간순으로 정리합니다. 새로운 문제가 해결될 때마다 이 파일에 항목이 추가됩니다.

---

## 1. Firestore 실시간 연결 실패가 화면에 전혀 표시되지 않던 문제

**증상**
대시보드가 "대회 정보를 불러오는 중입니다…" 상태에서 멈추고, 콘솔에도 사용자가 볼 수 있는 에러가 없음.

**원인**
`firestore-service.js`의 `onSnapshot` 구독이 실패하거나 응답이 없을 때 `console.error`로만 기록되고 화면에는 아무 표시가 없었음. 광고 차단기·보안 확장 프로그램이 Firestore 실시간 연결(WebChannel)을 막아도 사용자는 원인을 알 수 없었음.

**해결**
- `firestore-service.js`에 `reportSnapshotError()`와 `watchForTimeout()` 추가 — 구독이 8초 안에 응답하지 않으면 `firestore-timeout` 이벤트, 에러가 나면 `firestore-error` 이벤트를 발생시킴.
- `admin.html` / `dashboard.html`에 빨간 `#errorBanner` 영역 추가.
- `admin.js` / `dashboard.js`에 `initConnectionWatch()`를 추가해 위 이벤트를 받아 배너에 원인과 대처 방법(확장 프로그램 끄기, 다른 네트워크 시도 등)을 표시.

---

## 2. 관리자 페이지 버튼이 전부 먹통이던 문제 (조 추가/팀 추가/저장 등 전혀 반응 없음)

**증상**
admin.html에서 모든 버튼·입력칸이 반응하지 않고, 대시보드도 동기화되지 않음. 콘솔 에러도 안 보임.

**진단 과정**
실제 코드 리뷰(admin.js, firestore-service.js)로는 로직 오류를 찾지 못함 → 직접 검증하기 위해 가상 브라우저(headless Chromium)를 띄워 admin.js를 실제로 실행하고, 대회명 저장 → 조 추가 → 팀 추가 과정을 코드로 클릭/입력해 재현 테스트.

**원인**
`admin.html`/`dashboard.html`을 파일 아이콘 더블클릭으로 직접 열면 주소창이 `file://...`로 시작되는데, 크롬 계열 브라우저는 보안 정책상 `<script type="module">`을 file:// 환경에서 통째로 차단함 (CORS 오류). 이 경우 자바스크립트가 단 한 줄도 실행되지 않아 모든 버튼이 죽은 것처럼 보이고, 에러도 화면에 안 보임 — 보고된 증상과 정확히 일치.
(실제 가상 브라우저 테스트에서 `http://localhost`로 열었을 때는 콘솔 에러 0개, 모든 기능 정상 동작을 확인함 → 코드 자체엔 버그 없음을 검증.)

**해결**
- `로컬서버_시작.command` 파일을 새로 추가. 더블클릭하면 로컬 서버가 켜지고 `http://localhost:8765/admin.html`로 자동 열림 — file:// 문제를 원천적으로 회피.
- README에 "0. 로컬에서 열 때는 꼭 이렇게" 항목 추가, 배포된 `https://...web.app` 주소를 쓰는 경우엔 이 문제와 무관하다는 점도 안내.

---

## 3. 관리자 페이지를 수정해도 실제 폴더(Finder)에는 반영이 안 되던 문제

**증상**
admin.html, js/admin.js를 수정했다고 안내받았지만, 실제 Finder에서 열어보면 파일 수정 시각/내용이 그대로(예전 버전)였음. Desktop 폴더에서 Documents 폴더로 옮겨도 동일하게 반영 안 됨.

**원인**
한글 폴더/파일 이름은 유니코드로 표현하는 방식이 두 가지(NFC/NFD)가 있음. macOS는 폴더 이름을 NFD(자모 분해형)로 저장하는데, 작업 중 터미널 명령에 한글 경로를 직접 입력하면 NFC(완성형)로 입력되어, 겉보기엔 이름이 같아 보이는 "가짜" 폴더가 별도로 만들어짐. 그 결과 수정 내용이 진짜 사용자 폴더가 아니라 이 가짜 폴더에만 쌓이고 있었음 (눈으로는 폴더 이름이 똑같아 보여서 알아채기 어려움).

**해결**
- 폴더 목록을 직접 다시 읽어와(글로빙) 바이트 단위로 정확한 진짜 경로를 구분 → 진짜 경로에 수정 파일을 다시 복사.
- 이후 Finder에서 직접 파일을 열어 내용이 실제로 바뀌었는지 확인하는 절차를 검증 단계에 추가.

---

## 4. 링크제 도형의 경기 순서 번호와 실제 경기 목록 순서가 일치하지 않던 문제

**증상**
링크제 도형에는 변마다 경기 순서(①②③④)가 표시되는데, 그 아래 경기 목록은 다른 순서로 나열되어 있었음. 예: 도형에는 1번이 a vs c인데 목록 맨 위에는 b vs d가 표시됨.

**원인**
`firestore-service.js`의 `subscribePrelimMatches`가 정렬 없이 `onSnapshot(prelimCol(), ...)`으로 구독하고 있어서, 화면에 전달되는 경기 배열 순서가 각 경기 문서의 Firestore 자동 생성 ID 순서를 따름 — 의도한 경기 순서(`round` 필드)와 무관한 순서였음. 라운드로빈 방식에서는 우연히 눈에 덜 띄었지만, 링크제 도형에 명시적인 번호를 붙이면서 불일치가 바로 드러남.

**해결**
- `admin.js`(예선 탭 경기 목록 렌더링, 본선 진출팀 선택용 순위 계산), `dashboard.js`(대시보드 경기 목록), `csv-export.js`(CSV 내보내기) 세 곳 모두에서 `groupMatches`를 만들 때 `.sort((a, b) => (a.round || 0) - (b.round || 0))`를 추가해 항상 `round` 오름차순으로 정렬하도록 수정.
- 라운드로빈 방식의 경기 목록 순서도 동일하게 정리됨(기존엔 잠재적으로 같은 문제가 있었음).

---

## 5. Claude 세션에서 파일 삭제·일부 도구가 막히는 문제

**증상**
오래된 버전 파일(`bounce_volleyball_app/` 폴더, `.zip`) 정리 요청 시 bash `rm`/`unlink`이 전부 `Operation not permitted`로 실패. 삭제 권한 요청 도구(`allow_cowork_file_delete`)도 한글 경로를 "마운트를 찾을 수 없음"으로 거부. Edit 도구로 `admin.html`을 직접 수정하는 것도 "protected location" 에러로 거부됨.

**원인**
1) 파일 영구 삭제는 Claude에게 허용되지 않는 동작 — bash 레벨에서도 동일하게 차단되어 있음(의도된 안전장치로 보임).
2) Edit/Write 도구·삭제권한 도구는 한글 경로의 유니코드 정규화(NFC/NFD) 불일치로 마운트 경로 매칭에 실패하는 것으로 보임(3번 문제와 같은 종류).

**해결**
- 파일 **내용 수정**은 bash 도구로 직접 읽고 쓰면 정상 동작함(Read 도구로 재확인 가능) — Edit 도구가 막히면 이 방법으로 우회.
- 파일·폴더 **삭제**는 어떤 도구로도 Claude가 대신 할 수 없음 — 정확한 경로를 안내해서 사용자가 Finder에서 직접 삭제하도록 요청할 것.

---

## 6. 조를 삭제하면 그 조에 속한 팀이 화면에서 사라지고 지울 수도 없던 문제 (참가팀 수가 줄지 않음)

**증상**
관리자 페이지 "조 편성"에서 조를 모두 지웠더니, "참가팀 등록" 섹션에 팀 목록이 전혀 안 보이고 (총 N팀) 숫자도 그대로 남아 삭제가 불가능했음. 대시보드의 참가팀 수도 똑같이 줄지 않음.

**원인**
1) `admin.js`의 `renderGroupTeamLists()`가 등록된 조가 하나도 없으면(`!groups.length`) 무조건 "먼저 조를 등록하세요"만 보여주고 곧장 `return`해서, 팀이 실제로 있어도 목록(✕ 삭제 버튼 포함)을 전혀 그리지 않았음.
2) `firestore-service.js`의 `deleteGroup()`이 조 문서만 지우고, 그 조에 속한 팀들의 `groupId`는 그대로 남겨둬서 — 이미 삭제된 조 id를 계속 가리키는 "고아 팀"이 생김. 화면에서도, 코드에서도 어느 그룹에도 속하지 않은 것으로 처리되지 않아 더더욱 안 보였음.
3) 즉 대시보드의 "17팀"은 코드 버그로 고정된 값이 아니라, 위 두 문제 때문에 화면에서 보이지도 지울 수도 없게 된 실제 Firestore 데이터였음. 관리자 페이지와 대시보드는 원래부터 같은 Firestore `teams` 컬렉션을 실시간 공유하고 있어 연동 자체에는 문제가 없었음.

**해결**
- `renderGroupTeamLists()`: 조가 0개여도 팀이 있으면 항상 "미배정" 섹션에 표시하도록 수정. 미배정 판정 기준을 `!t.groupId`에서 `!t.groupId || !groupIds.has(t.groupId)`로 확장해 "삭제된 조를 계속 가리키는 고아 팀"도 잡아냄.
- `deleteGroup()`: 조를 지울 때 그 조 소속 팀들의 `groupId`를 `null`로 일괄 업데이트(batch)하도록 수정 — 앞으로는 조를 지워도 팀이 고아가 되지 않고 곧바로 "미배정"으로 표시됨.

---

## 7. js/admin.js 등을 고쳐도 브라우저가 계속 옛 버전을 보여주던 문제 (캐시)

**증상**
파일은 분명히 수정됐는데(디스크에서 직접 확인해도 새 코드가 있음), 서버를 껐다 켜고 브라우저를 새로고침해도 화면은 계속 수정 전 상태로 보임. (예: "미배정" 섹션 코드가 분명히 있는데 화면엔 안 보임)

**원인**
`로컬서버_시작.command`가 그냥 `python3 -m http.server`를 띄우는데, 이 기본 서버는 캐시 관련 헤더를 따로 안 보내서 크롬이 `js/admin.js` 같은 모듈 스크립트를 자체 판단으로 디스크 캐시에 계속 보관하고 재사용함. 서버를 재시작해도, 탭을 새로 열어도 크롬의 디스크 캐시 자체는 그대로 남아있어서 계속 옛 파일을 읽어옴 — 단순 새로고침(Cmd+R)이나 서버 재시작으로는 안 풀림.

**해결**
- `로컬서버_시작.command`를 수정해 모든 응답에 `Cache-Control: no-store, no-cache, must-revalidate` 등 캐시 금지 헤더를 강제로 붙이는 파이썬 커스텀 서버로 교체. 이제부터는 서버가 캐시 자체를 못 하게 막아서, 파일을 고치고 새로고침만 하면 항상 최신 버전이 보임.
- 자동으로 열리는 첫 URL에도 `?t=타임스탬프`를 붙여 첫 로드부터 캐시를 우회하도록 함.
- 그래도 안 보이면: 이미 브라우저에 쌓여있던 예전 캐시가 원인일 수 있으니, 시크릿 창으로 열거나 DevTools(Cmd+Option+I) → Network 탭 → "Disable cache" 체크 후 새로고침으로 확인.

---

## 7-1. 배포된 실제 사이트(Firebase Hosting)에서도 같은 캐시 문제 재발

**증상**
로컬이 아니라 실제 배포 주소(`bbounce-cdb31.web.app`)에서, 코드를 고쳐 배포한 뒤에도 평소 쓰던 브라우저 창에서는 계속 이전 버전이 보임. 시크릿 모드로 열면 새 버전이 정상적으로 보임.

**원인**
7번과 같은 종류의 문제이지만 이번엔 로컬 서버가 아니라 Firebase Hosting 쪽 캐시. `firebase.json`에 캐시 관련 `headers` 설정이 없어서 Firebase Hosting 기본 캐시 정책(`.js`/`.css`/`.html` 등 정적 파일에 캐시 허용)을 그대로 따랐고, 이미 한 번 받아간 브라우저는 새로 배포된 파일을 받아가지 않고 디스크 캐시를 계속 재사용함.

**해결**
- `firebase.json`의 `hosting`에 `headers` 항목 추가: `**/*.@(js|css|html)` 패턴에 `Cache-Control: no-cache, max-age=0, must-revalidate`를 강제 설정.
- 이후로는 배포할 때마다 브라우저가 서버에 변경 여부를 항상 재확인(304 또는 새 파일 다운로드)하므로, 일반 새로고침만으로도 최신 버전이 반영됨.
- 단, 이 설정이 적용되기 전에 이미 받아둔 캐시는 이 배포 직후 한 번은 새로고침이 필요할 수 있음.

---

## 8. 관리자 페이지에서 점수를 입력했는데 "경기전"으로 표시되던 문제

**증상**
예선 경기 점수(예: 1세트 14:13)를 입력해 저장했는데도, 관리자 페이지 경기 목록에는 계속 "경기전" 배지가 표시됨. 규칙 위반 점수가 입력되어도 막히지 않는 것처럼 보임.

**원인**
1) `js/admin.js`의 경기 목록 배지 로직이 승/패/무승부 외의 모든 경우를 무조건 "경기전"으로 표시하고 있어서, 점수가 일부 입력된 "진행중" 상태를 구분하지 못함(대시보드 쪽 `js/dashboard.js`에는 이미 있던 분기가 관리자 페이지에는 빠져 있었음).
2) 14:13 자체는 사실 규칙 위반이 아니라 듀스 진행 중인 정상 상태였음 — 진짜 문제는 표시 버그였음.
3) 다만 이 프로그램은 경기가 다 끝난 뒤 최종 점수만 한 번에 입력받는 방식으로 운영되므로(실시간 중계 입력이 아님), 사용자 확인 결과 "아직 승부가 안 난 점수(진행중)" 자체를 애초에 입력 불가하게 막는 것이 운영 방식에 더 맞는다고 판단함.

**해결**
- `js/admin.js`: 배지 로직에 `evald.status === "in_progress"` 분기를 추가해 "경기중"으로 정확히 표시.
- `js/match-logic.js`의 `validateSetScore`: 목표점수 미달, 또는 듀스 중 2점차 미달처럼 "아직 승부가 안 난 점수"는 0:0(미입력) 예외를 빼고 전부 저장을 막고 "아직 끝나지 않은 세트입니다. 세트가 끝난 최종 점수만 입력해주세요." 경고를 띄우도록 변경. 11개 케이스(완료 점수/듀스 진행중/상한 위반 등)로 직접 테스트해 의도대로 동작함을 확인.

---

## 9. 본선 대진표 트리 화면맞춤 — PC·모바일 2-way 방식 (최종 정리)

**배경/증상**
PC에서는 잘 맞던 본선 대진표가 모바일(iOS Safari)에서는 화면을 넘쳐 흐르고, 팀 이름이 "삼화…"로 잘리고 세트 표 헤더가 "1세\n트"로 세로 줄바꿈되는 등 비율이 깨졌음. 이를 고치는 과정에서 PC와 모바일의 사용 목적이 서로 다르다는 점이 드러남:
- **PC**: 화면을 고정해 큰 모니터로 송출 → 전체 대진표가 한 화면에 다 보여야 함.
- **모바일**: 관객이 QR로 각자 휴대폰에서 실시간 확인 → 처음엔 전체 구조가 한눈에 보이고, 자세한 정보는 각자 확대해서 보면 됨.

**핵심 원인**
화면맞춤 축소를 CSS `zoom` 속성으로 했는데, `zoom`은 iOS Safari·인앱 브라우저에서 동작하지 않아 모바일에서 축소가 전혀 안 먹고 자연 크기 그대로 넘쳐 흘렀음.

**최종 해결 (현재 동작)**
- `js/bracket-render.js`: 화면맞춤을 모든 브라우저가 지원하는 `transform: scale()`로 처리. transform은 레이아웃 박스를 안 줄여서 부모가 넘치므로, 트리(`.bracket-track`)를 `position:absolute`로 띄우고 부모 래퍼(`.bracket-root`)의 width/height를 "스케일된 실제 크기"로 직접 지정해 스크롤 없이 딱 맞춤. 연결선(SVG)은 배율 1 상태에서 자연 좌표로 그린 뒤 트리와 함께 스케일되게 해 어긋남 방지. 카드/이름은 PC·모바일 동일한 자연 크기(모바일 전용 카드 축소·이름 말줄임 제거)로 두고 트리 전체만 비율 유지한 채 스케일.
- 배율(factor) 결정 — **세 갈래**:
  1. **전체화면(PC 송출용)**: 가로·세로 모두에 맞춰 전체 대진을 한 화면에(1배 초과 확대도 허용).
  2. **모바일 일반 화면(`window.innerWidth <= 720`)**: 가로 폭에 맞춰 전체 대진표를 한눈에 보이게 축소. 자세한 정보는 사용자가 핀치 줌(두 손가락 확대) + 스크롤로 확인.
  3. **PC 일반 화면**: 자연 크기(1배). 화면보다 넓으면 가로 스크롤, 한눈에 보려면 '전체화면' 버튼.
- `css/bracket.css`: 모바일(≤720px)에서는 사용자 결정에 따라 **전체화면 버튼을 `display:none`으로 숨김**(어차피 핀치 줌으로 보므로, 그리고 제목 배너와 버튼이 겹치던 문제도 함께 해결). 모바일 안내문구는 "👉 두 손가락으로 확대하거나 스크롤해서 자세히 보세요"로 표시(전체화면 모드일 땐 숨김).
- 핀치 줌이 막히지 않도록 `dashboard.html`의 viewport에 `user-scalable=no`/`maximum-scale`이 없는지 확인(이미 허용 상태).

---

## 10. 관객 공개 버튼을 한 번 누르면 사라져서, 라운드별 결과 갱신에 쓸 수 없던 문제

**증상**
본선 대진표를 관객에게 한 번 "공개"하면 공개 버튼/막대가 사라져 비활성화됨. 그래서 6강·준결승·결승 결과를 라운드마다 입력해 공개하려 해도 버튼이 없어 쓸 수 없었음.

**원인**
공개 게이트(§인수인계 §10-3)가 대진표 "구조 변경"(생성·부전승·자리 스왑)에만 걸려 있었음. 경기 점수 입력은 게이트 없이 `saveFinalMatches`로 곧바로 Firestore에 저장(=즉시 공개)됐고, 공개 막대는 `bracketPublishPending`이 true일 때만 보이도록 되어 있어서 한 번 공개하면(pending=false) 막대가 사라졌음. 결과적으로 라운드 진행 중에는 공개 버튼이 안 보였음.

**해결**
- `js/admin.js`: 경기 점수 입력(`openFinalScoreModal`의 onSave)도 즉시 저장하지 않고 로컬에만 반영 + `bracketPublishPending = true`로 바꿔, 구조 변경과 동일하게 "공개 게이트"를 타도록 통일. (더 이상 쓰지 않는 `saveFinalMatches` import 제거.)
- 공개 막대(`#bracketPublishBar`)를 대진표가 있는 한 **항상 표시**되게 변경(`updateBracketPublishBar`). 공개 안 한 변경/기록이 있으면 주황 경고 + 버튼 활성("관객 화면에 공개"), 모두 공개됐으면 초록 + 버튼 비활성("공개 완료"). → 라운드마다 결과 입력 후 같은 버튼으로 그때그때 공개 가능.
- `css/bracket.css`: 막대에 기본(초록=공개완료)/`.is-pending`(주황=미공개) 두 상태 스타일 추가.
- 동작 모델: 점수·대진 변경은 전부 로컬에 모았다가 관리자가 버튼을 눌러야 관객 대시보드에 반영됨(실시간 자동 공개 아님). 공개 전 이탈 시 `beforeunload` 경고는 그대로.

---

## 11. 추첨 대상 동률 팀이 자동으로 본선 진출팀에 포함되던 문제

**증상**
승점·세트득실·득실차·승자승이 모두 같은 팀들에게 "동률·추첨필요"가 표시되면서도 서로 다른 순위가 부여되어, 진출선에 걸린 팀 중 하나가 자동 추천되는 문제가 있었음.

**원인**
`computeGroupStandings()`가 승자승으로 해결되지 않은 동률 팀에도 배열 순서대로 연속 순위를 부여했고, 본선 자동 추천은 해당 순위를 그대로 사용했음. 따라서 실제 추첨 결과가 아니라 팀 등록·조회 순서가 진출팀 선택에 영향을 줄 수 있었음.

**해결**
- 승자승 점수까지 같은 팀은 공동 순위로 처리하고, 해당 동률 묶음에만 `needsLottery`를 표시함. 일부만 동률인 경우에는 경기식 순위(예: 1위, 공동 2위, 공동 2위, 4위)를 적용함.
- 공동 순위 묶음 전체가 진출 인원 안에 들어올 때만 자동 선택하고, 진출선을 걸치면 그 묶음에서는 아무 팀도 자동 선택하지 않도록 변경함.
- 진출선 동률이 있으면 오프라인 추첨 후 승자를 직접 선택하라는 경고를 관리자 본선 진출팀 선택 화면에 표시함.
- 공동 순위, 부분 동률, 진출선 경계, 확정 순위 자동 선택을 단위 테스트로 추가함.

---

## 12. 예선 대진 재생성·삭제 후 코트 운영 데이터가 남던 문제

**증상**
예선 대진을 다시 만들거나 조·팀·예선을 삭제하면 공식 경기 문서는 바뀌지만, 기존 경기 ID를 가리키는 코트 배정·기록관 작성 상태·대기열이 남아 기록 제출과 관리자 승인이 실패할 수 있었음.

**원인**
조·팀·예선 경기의 파괴적 변경은 브라우저가 Firestore 문서를 직접 삭제·생성했고, 새 코트 운영 워크플로는 Cloud Functions에서 별도 문서와 큐로 관리했음. 두 변경 경계가 분리되어 공식 경기와 운영 데이터가 한 작업으로 정리되지 않았음.

**해결**
- 조·팀·예선의 삭제·초기화·대진 생성을 인증된 관리자 전용 `mutatePrelimStructure` Callable로 통합함.
- 점수·제출·승인·반려·잠금·시도 기록이 있는 경기는 변경을 거부하고, 기록이 전혀 없는 경기만 공식 경기·배정·워크플로에서 함께 제거함.
- 제거된 경기가 있던 코트는 남은 경기 순서와 연결을 다시 매기고, 대기열의 현재·다음 경기와 우선순위 항목을 서버 트랜잭션 안에서 재계산함.
- 해당 부문의 본선 대진표가 이미 있으면 예선 구조 변경을 거부함.
- 링크제 배치와 대진 변경을 같은 트랜잭션으로 저장하고, 대진이 있는 조 사이의 팀 이동은 예선을 먼저 초기화하도록 차단함.
- Firestore 규칙에서 브라우저의 조·팀 삭제 및 예선 경기 생성·삭제를 차단해 서버 검사를 우회할 수 없게 함.
- 허용된 초기화의 큐 재연결, 금지된 경기 상태의 무변경, 라운드로빈·링크제 생성, 팀 참조, 본선 의존성과 보안 규칙을 에뮬레이터 테스트로 검증함.

---

## 13. 본선 대진표 초기화가 Firestore 권한 오류로 실패하던 문제

**증상**
관리자 화면에서 본선 대진표 초기화를 실행하면 본선 경기 문서가 존재하는 경우 Firestore 권한 오류가 발생하고 대진표가 삭제되지 않았음.

**원인**
본선 경기 문서는 보안을 위해 브라우저 쓰기를 전부 금지하고 있지만, 초기화 기능은 브라우저에서 해당 문서를 직접 일괄 삭제하려 했음. 또한 경기 문서만 삭제하면 코트 배정·기록관 워크플로·대기열에 삭제된 경기 ID가 남을 수 있었음.

**해결**
- 초기화 버튼을 인증된 관리자 전용 `clearFinalStructure` Callable에 연결함.
- 아직 시작하지 않은 본선 경기만 초기화할 수 있으며, 입력·제출·반려·승인·공식 점수·확정 부전승·다음 경기 전파 기록이 있으면 전체 작업을 거부함.
- 허용된 초기화에서는 본선 경기, 해당 배정과 워크플로, 코트 대기열의 관련 항목을 한 트랜잭션에서 함께 정리함.
- 같은 코트에 남은 예선 경기의 순서와 현재·다음 경기 연결을 다시 계산하고, 다른 부문의 본선은 건드리지 않음.
- 남녀 본선에서 같은 로컬 경기 ID를 사용해도 부문이 다른 배정은 삭제하지 않도록 부문이 포함된 정식 배정 키로 구분함.
- 브라우저의 본선 직접 쓰기 금지 규칙은 그대로 유지하고, 허용·거부·무변경·큐 재연결 동작을 에뮬레이터 테스트로 검증함.

---

## 14. 대회 초기화·복원 중에도 관객 화면이 실시간 중계로 표시되던 문제

**증상**
대회 전체 초기화나 백업 복원 중에도 관객 대시보드가 유지보수 안내로 전환되지 않고, 삭제·복원 중간의 불완전한 경기 자료와 “실시간 중계중” 상태를 계속 표시했음.

**원인**
서버와 보안 규칙은 유지보수 상태를 `maintenance.enabled`로 저장하지만, 대시보드는 존재하지 않는 `maintenance.active`를 확인했음. 또한 경기장 송출 자동 전환 타이머와 상태 영역이 유지보수 콘텐츠와 별도로 동작했음.

**해결**
- 대시보드가 서버 계약과 동일한 `maintenance.enabled`를 확인하도록 수정함.
- 유지보수 시작 시 예선·본선 내용을 숨기고 기존 렌더링을 제거하며, 부문 선택과 경기장 송출 상태를 숨기고 자동 전환 타이머를 중지함.
- 상단 상태를 “실시간 중계중”에서 “점검 중”으로 변경함.
- 유지보수 종료 시 최신 구독 데이터로 화면을 다시 그리고, 일반/경기장 화면의 설정에 따라 송출 상태와 자동 전환을 복구함.
- 브라우저 모의 Firestore 구독으로 유지보수 진입과 종료 상태를 직접 검증함.

---

## 15. 확정한 부전승이 관객 대진표에서 일반 경기 전 상태로 바뀌던 문제

**증상**
관리자가 본선 부전승을 확정하고 관객 화면에 공개해도 대시보드에서는 “부전승”이 아니라 상대가 비어 있는 일반 경기 전 상태로 표시됐음. 다음 라운드에는 해당 팀이 진출해 있어 대진표 앞뒤가 모순됐음.

**원인**
본선 공개 함수가 허용하는 구조 상태에 `bye`가 없어서 확정 부전승 상태를 저장하지 않았고, 대시보드도 공식 점수 승인 번호가 없는 경기의 상태를 대부분 `pending`으로 바꿨음. 실제 경기 점수와 대진 구조인 부전승을 같은 공개 기준으로 처리한 것이 원인이었음.

**해결**
- 서버 본선 공개 처리에서 `bye`를 점수 결과가 아닌 안전한 대진 구조 상태로 허용함.
- 대시보드의 공개 경기 변환을 공용 순수 함수로 분리하고 `empty`, `waiting`, `pending`, `bye_pending`, `bye`, `in_progress` 상태를 보존함.
- 공식 승인 전 세트 점수·결과·승자 필드는 기존처럼 모두 제거해 공개되지 않도록 유지함.
- 확정 부전승과 다음 라운드 팀 배치는 유지하면서 가짜 점수나 공식 승인 번호를 만들지 않음.
- 단위·에뮬레이터 테스트와 브라우저 모의 구독으로 “부전승” 표시 및 미승인 점수 비노출을 검증함.

---

## 16. 일반 관객 대시보드가 경기장 자동 송출 모드로 잠기던 문제

**증상**
일반 관객용 `dashboard.html`에서도 남자부·여자부 선택 버튼이 비활성화되고, 관리자가 저장한 경기장 설정에 따라 부문이 자동 전환됐음.

**원인**
대시보드가 URL의 `display=venue`뿐 아니라 저장된 `venueDisplay.mode`가 존재하는 것만으로도 경기장 전용 모드라고 판단했음. 경기장 설정은 대회 공통 정보이므로 일반 관객 화면까지 모두 잠겼음.

**해결**
- 경기장 전용 여부를 URL의 `display=venue`로만 결정하도록 변경함.
- 일반 대시보드는 저장된 송출 설정과 무관하게 남녀부 선택을 허용하고 자동 전환 타이머와 송출 상태를 표시하지 않음.
- 경기장 전용 URL에서만 관리자의 자동·남자부 고정·여자부 고정 설정을 적용함.
- 브라우저 모의 구독으로 일반 URL의 여자부 선택 유지와 버튼 활성화, 경기장 URL의 버튼 잠금·자동 전환 상태를 각각 검증함.

---

## 17. 본선 점수가 관객 화면 공개 버튼을 거치지 않고 즉시 노출되던 문제

**증상**
관리자가 본선 점수 모달에서 저장하는 순간 점수가 공식 승인되어 관객 대시보드에 바로 표시됐음. 아직 공개하지 않은 새 대진표에서는 공식 경기 워크플로가 없어 같은 점수 저장이 실패할 수 있었음.

**원인**
본선 구조는 로컬에서 편집한 뒤 공개하는 방식이었지만, 본선 점수는 예선 관리자 직접 수정과 같은 `directEditOfficialScore` 경로를 사용했음. 로컬 초안과 공식 데이터의 경계가 구조와 점수에서 서로 달랐고, 공개 함수에는 점수 검증·승자 계산·동시 편집 충돌 검사가 없었음.

**해결**
- 본선 점수 저장은 Firestore나 Callable을 호출하지 않고 관리자 브라우저의 로컬 초안에만 반영함.
- 공개 전 로컬 화면에서 승자와 다음 라운드 진출을 미리 계산하되 공식 revision을 만들지 않음.
- “관객 화면에 공개”에서 구조와 점수 초안을 분리해 전송하고, 서버가 모든 점수·승자·다음 라운드 진출을 다시 계산한 뒤 한 트랜잭션으로 공식 반영함.
- 문서 ID·마지막 전환 ID·공식 revision 전체를 공개 기준으로 비교해 다른 관리자의 변경이 있으면 zero-write로 거부하고 로컬 초안을 자동 덮어쓰지 않음.
- 공개 중에는 대진 생성·초기화·부전승·자리 교환·점수 저장·부문 전환을 잠가 응답 도착 후 새 편집이 사라지지 않도록 함.
- 기록관이 제출한 본선 점수도 즉시 승인하지 않고 관리자 로컬 공개 초안에 반영한 뒤 동일한 공개 경계를 통과하도록 변경함.
- 새 대진표의 경기 워크플로는 공개 시 서버가 생성하고, 배정된 경기의 점수 공개는 코트 대기열과 다음 경기 상태를 함께 재계산함.
- 승인된 경기·확정 부전승의 참가팀과 진행 경로는 임의로 바꿀 수 없으며, 실제 후속 경기 기록이 있는 승자 변경은 거부함.
- 단위·에뮬레이터 테스트로 새 대진 점수 공개, 미승인 정보 비노출, 오래된 초안 거부, 기록관 제출 공개, 연속 준결승 진출 전파와 대기열 활성화를 검증함.

---

## 18. 백업 복원 후 백업에 없는 기존 데이터가 남고 대회 설정이 복원되지 않던 문제

**증상**
백업을 복원해도 대회명·진출팀 수 같은 루트 설정이 이전 값으로 남고, 백업에 없는 기존 팀·경기·코트 운영 문서가 삭제되지 않아 서로 다른 시점의 데이터가 섞일 수 있었음.

**원인**
복원 서버가 `tournaments/main/` 하위 경로만 허용해 루트 문서 `tournaments/main`을 제외했고, 백업 문서는 덮어쓰기만 하면서 백업에 없는 기존 문서를 삭제하지 않았음. 여러 컬렉션을 독립적으로 읽어 백업을 만들었기 때문에 진행 중인 경기 변경 사이에서 서로 다른 시점의 자료가 섞일 가능성도 있었음.

**해결**
- 백업 생성은 관리자 전용 서버 Callable의 단일 Firestore 읽기 트랜잭션으로 이동해 일관된 시점의 자료만 내보냄.
- 복원 가능한 루트 필드를 대회명·부문별 진출팀 수·경기장 송출 설정·코트 토폴로지 revision으로 한정하고 별도로 정확히 복원함.
- 조·팀·예선·남녀 본선·공식 revision·코트·배정·대기열·점수 워크플로·감사 기록의 허용 경로를 명시적으로 제한함.
- 모든 백업 청크 적용 후 허용 컬렉션을 비교해 백업에 없는 기존 문서를 400개 단위로 삭제하고, 경로 집합과 문서 체크섬이 완전히 일치해야만 복원을 승격함.
- 관리자·기록관 접근 설정·grant·복원 manifest 등 보안·운영 문서는 백업 권한에서 제외하고 기존 값을 보존함.
- 배정-워크플로 쌍, 공식 경기 참조, 코트-대기열 연결, 큐 포인터와 투영 상태를 복원 시작 전과 최종 검증에서 모두 검사함.
- 복원 ID를 브라우저 세션에 보존하고, 중단·실패 시 현재 임대를 새 manifest로 안전하게 대체해 다시 진행할 수 있도록 함. 각 단계는 재호출해도 이전 완료 상태를 되돌리지 않음.
- 이전 복원 삭제 작업은 현재 임대를 트랜잭션에서 재확인하므로 새 복원을 대체한 뒤 문서를 삭제할 수 없음.
- 유지보수 중에는 새 백업 생성을 서버 트랜잭션 안에서 거부해 복원 중간 자료가 백업되지 않도록 함.
- 에뮬레이터 테스트로 정확한 루트 복원, 잔존 문서 삭제, 보호 문서 유지, 다중 삭제 청크, 체크섬 실패, 복원 대체·재시도와 오래된 manifest 재사용 차단을 검증함.

---

## 19. 변조된 기록관 잠금으로 코트 경기를 해제할 수 없던 문제

**증상**
유효한 기록관 grant를 가진 사용자가 빈 문자열 또는 비정상 토큰으로 현재 경기를 가져오면 제출·취소가 실패하고, 관리자 강제 해제도 빈 토큰을 거부해 코트 대기열이 해당 경기에서 멈출 수 있었음. 시도 횟수와 재개 상태도 별도 요청으로 변조할 수 있었음.

**원인**
Firestore 규칙이 새 잠금 토큰의 자료형만 확인하고 길이와 이전 상태 연결을 검사하지 않았음. 배정 문서 업데이트 규칙도 실제 경기 시작 전환 없이 `attemptCount`만 단독으로 바꾸는 요청을 허용했음. 관리자 강제 해제 입력 검증은 빈 문자열을 토큰 부재로 취급했음.

**해결**
- 새 기록관 잠금 토큰을 16자 이상 128자 이하 문자열로 제한하고 클라이언트에서도 같은 범위를 사전 검사함.
- `resumeDraftState`가 잠금 직전의 `idle` 또는 `rejected` 상태와 정확히 같아야 경기 시작을 허용함.
- `attemptCount`는 기존 값이 없으면 1, 이미 양수이면 기존 값을 그대로 유지하는 실제 경기 시작 트랜잭션에서만 변경할 수 있도록 함.
- 배정 문서 단독 업데이트로 시도 횟수나 상태를 변조할 수 없도록 배정 규칙도 동일한 워크플로 전환을 독립적으로 검증함.
- 관리자 강제 해제는 저장된 토큰과 요청 토큰을 정확히 비교하되, 과거에 만들어진 빈 토큰 잠금도 관리자 권한으로 복구할 수 있도록 수정함.
- 빈·짧은 토큰, 잘못된 재개 상태, 임의 시도 횟수와 배정 단독 변조의 zero-write 거부 및 기존 빈 토큰 관리자 복구를 에뮬레이터 테스트로 검증함.

---

## 20. 기록관이 관리자 감사 문서 ID를 선점해 승인을 막을 수 있던 문제

**증상**
유효한 기록관 grant를 가진 사용자가 변조된 요청으로 미래의 관리자 승인·반려 감사 문서 ID를 먼저 생성하면, 서버가 같은 ID로 불변 감사 문서를 생성하지 못해 해당 경기의 공식 처리가 반복해서 실패할 수 있었음.

**원인**
기록관과 서버가 같은 `auditEvents` 문서 ID 공간을 사용했고, 기록관 워크플로 규칙과 감사 컬렉션 생성 규칙에서 기록관 전용 ID 접두어를 강제하지 않았음. 감사 문서는 생성 후 수정·삭제가 금지되어 선점된 ID를 정상 승인 트랜잭션이 덮어쓸 수 없었음.

**해결**
- 기록관이 생성하는 모든 전환·감사 ID를 `recorder:` 네임스페이스로 변경하고 최대 길이를 제한함.
- 서버가 생성하는 일반 전환 ID와 대진·코트·예선 구조 감사 ID를 `server:` 네임스페이스로 변경함.
- Firestore 규칙에서 기록관 워크플로 전환뿐 아니라 `auditEvents` 문서 생성 자체에도 현재 grant, `recorder:` 접두어, 길이, actor·시간·워크플로 연결 검사를 적용함.
- 감사 문서 ID, 문서의 `transitionId`, 워크플로와 배정의 `lastTransitionId`가 계속 동일해야 하도록 기존 결합 검사를 유지함.
- 기존 감사 문서는 수정하거나 삭제하지 않고, 신규 이벤트부터 분리된 ID를 사용함.
- 정상 기록관 시작·임시저장과 서버 네임스페이스 선점, 워크플로 변경 없는 독립 감사 문서 선점 거부를 에뮬레이터 테스트로 검증함.

---

## 21. 관리자 예선 점수 직접 입력 후 코트 대기열이 완료 경기에서 멈추던 문제

**증상**
코트에 배정된 예선 경기를 관리자가 직접 점수 입력으로 완료하면 공식 경기·배정·워크플로는 승인 상태가 되지만 코트 대기열은 해당 완료 경기를 현재·다음 경기로 계속 가리킬 수 있었음. 기록관 입력·제출 중인 경기의 작업도 관리자 직접 입력이 조용히 덮어쓸 수 있었음.

**원인**
`directEditOfficialScore`가 공식 점수와 워크플로만 갱신하고 원래 코트 큐를 읽거나 재투영하지 않았음. 직접 입력 가능 상태도 공식 revision 하나만 확인해 기록관 잠금·검수·반려 상태와 불완전한 승인 데이터를 엄격히 구분하지 않았음.

**해결**
- 첫 관리자 직접 입력은 공식 경기·배정·워크플로가 모두 기록 없는 정상 초기 상태일 때만 허용함.
- 코트 배정 경기에는 공식 revision과 큐 revision을 함께 요구하고, 서버 트랜잭션에서 두 값이 현재 상태와 일치하는지 확인함.
- 현재·미래·우선순위 경기를 직접 완료하면 해당 경기를 큐에서 제외하고 남은 경기의 현재·다음 포인터를 `projectCourtQueue`로 다시 계산하며 큐 revision을 한 번 증가시킴.
- 기록관 입력·잠금·제출·검수·반려·재입력 상태는 전체 작업을 zero-write로 거부함.
- 승인된 점수 정정은 공식 경기·배정·워크플로 revision, 마지막 전환 ID, 공식 스냅샷과 계산 결과가 모두 일치하는 완전한 승인 상태에서만 허용하고 큐는 과거로 되돌리지 않음.
- 관리자 화면도 모달을 열고 저장할 때 경기·팀·코트·공식 revision·큐 revision을 다시 비교하며, 검수 중 예선의 직접 “수정·승인” 우회 버튼을 제거함.
- 현재·미래·우선순위 큐 재투영, 오래된 큐 요청, 기록관 상태, 불완전한 승인·잔존 점수 데이터와 승인 정정을 에뮬레이터 테스트로 검증함.

---

## 22. 정정 대상을 바꿔도 이전 미리보기의 다른 경기가 수정될 수 있던 문제

**증상**
관리자가 승인 결과 정정 대상을 선택해 미리보기한 뒤 선택을 바꿔도 적용 버튼이 활성화되어 있었고, 적용 시 현재 선택이 아니라 이전 미리보기의 경기 ID가 정정될 수 있었음.

**원인**
클라이언트가 미리보기 시점의 `matchKeys`와 큐 revision만 저장하고 선택·팀·조·공식 경기·배정·워크플로·잠금·큐 변경에 맞춰 미리보기를 모두 무효화하지 않았음. 서버 적용 요청도 사용자가 다시 보낸 경기 ID와 revision만 신뢰해 미리보기의 전체 상태와 동일한지 증명하지 않았음.

**해결**
- 서버 미리보기에서 코트의 전체 큐 문서, 해당 코트의 모든 배정·워크플로, 대상 공식 경기, 예상 정정 후 큐와 대상 상태를 정규화해 SHA-256 계획 지문을 생성함.
- 적용 요청은 경기 ID를 별도로 받지 않고 서버가 발급한 `planToken`과 사유만 받으며, 같은 트랜잭션에서 전체 계획을 다시 계산해 지문·코트·대상·큐 revision이 모두 같을 때만 실행함.
- 계획 적용 후 큐 revision과 상태가 달라지므로 같은 토큰의 재사용도 자동 거부됨.
- 관리자 화면에서 정정 선택, 팀·조 정보, 공식 경기, 배정, 워크플로·잠금, 코트·큐가 바뀌면 기존 미리보기를 폐기하고 적용 버튼을 비활성화함.
- 미리보기 요청 중 선택이나 상태가 바뀌어도 늦게 도착한 응답을 저장하지 않도록 generation을 비교함.
- 최종 확인창에는 계획에 포함된 불변 경기 키, 코트, 부문, 경기 정보와 팀 이름을 명시함.
- 대상·지문 변조, 공식 revision·배정·워크플로·큐의 일부 필드 변경, 토큰 재사용과 정상 계획 일치를 에뮬레이터 테스트로 검증함.

---

## 23. 예선 라운드 번호가 표시 순서와 원형 라운드 의미를 혼용하던 문제

**증상**
4팀 라운드로빈 생성 시 `round`가 `1,1,2,2,3,3`으로 중복되어 화면 순서가 문서 ID에 좌우되고, 드래그 재정렬 후에는 같은 필드가 `1..6`으로 바뀌었음.

**원인**
하나의 `round` 필드에 원형 알고리즘의 동시 라운드와 관리자 화면의 고유 표시 순서를 함께 저장했음.

**해결**
브라우저와 서버 생성기 모두 실제 생성 경기마다 `round`를 고유하고 연속적인 `1..N` 표시 순서로 저장하도록 통일하고 4팀 조합·순서 테스트를 추가함.

---

## 24. 미완료 예선에서 추첨·진출 추천이 확정되고 잘못된 저장 점수가 순위에 반영되던 문제

**증상**
경기가 없거나 일부만 끝난 조에서도 추첨 표시와 자동 진출 추천이 나타났고, 복원·과거 데이터의 비정상 점수나 위조된 `result`·세트득실 필드가 순위에 반영될 수 있었음.

**원인**
순위 계산이 일정 완료 여부를 구분하지 않았고 일부 경로에서 원본 세트 대신 저장된 파생 필드를 신뢰했음. 서버 평가기도 문자열 숫자와 초과 세트를 잘라서 받아들였음.

**해결**
미완료 조는 `provisional`로 표시해 추첨·자동 진출을 중지하고, 브라우저·서버 모두 원본 정수 세트의 상한·듀스·종료 순서·세트 수를 엄격히 검사해 파생 결과를 다시 계산하도록 통일함.

---

## 25. 관리자 브라우저가 서버 소유 대회 루트 필드를 바꿀 수 있던 문제

**증상**
관리자가 브라우저 요청을 변조하면 유지보수, 기록관 기능, 코트 topology revision 또는 임의 루트 필드를 Callable 절차 없이 변경할 수 있었음.

**원인**
Firestore Rules가 관리자 역할만 확인하고 변경 가능한 루트 필드를 제한하지 않았음.

**해결**
브라우저 변경을 `name`, 부문별 `qualifyPerGroup`, `venueDisplay`, 서버 시각 `updatedAt`으로 제한하고 각 스키마를 검증함. 유지보수·기록관·topology·미등록 필드 변경 거부 테스트를 추가함.

---

## 26. 새 예선 경기의 워크플로 누락과 오래된 코트 편집 초안 문제

**증상**
예선 생성 직후 코트 설정을 한 번 저장하기 전에는 관리자 점수 입력이 실패했고, 다른 관리자가 경기·큐를 바꾼 동안 열어둔 코트 초안이 사라진 경기를 다시 저장하거나 최신 revision을 빌려 덮어쓸 수 있었음.

**원인**
예선 생성 트랜잭션이 공식 경기만 만들었고, 플래너가 dirty 상태에서도 실시간 revision을 기준값으로 교체하며 삭제된 키를 유지했음.

**해결**
예선 생성 시 미배정 `scheduled` assignment와 `idle` workflow를 원자적으로 함께 생성함. 플래너는 편집 시작 revision을 고정하고 서버에서 사라진 경기 키를 제거하며 오래된 저장은 서버 CAS로 zero-write 거부함.

---

## 27. 승인 결과 정정 후 재경기와 본선 후속 대진이 불일치하던 문제

**증상**
정정된 경기가 `replay_required + idle`이 되어 기록관이 가져갈 수 없었고, 본선 상위 경기를 철회해도 관객 화면에 이전 점수·승자가 남거나 후속 경기·큐가 존재하지 않는 참가팀으로 진행될 수 있었음.

**원인**
정정 계획과 기록관 Rules의 상태쌍이 달랐고, 공식 경기·후속 참가팀·dependencyReady·assignment·priority queue를 하나의 그래프로 재투영하지 않았음.

**해결**
재경기를 `replay_required + rejected`로 통일하고 공식 결과를 `officialCurrent:false`로 철회함. 안전한 후속 대진은 참가팀·점수·승자를 제거하고 dependency와 모든 영향 코트 큐를 원자적으로 재투영하며, 후속 이력이 있으면 전체 정정을 거부함. 정정 ID와 쓰기 수에도 고정 길이·499개 제한을 적용함.

---

## 28. 본선 검수 버전·정정 사유·참가팀 정보가 정확히 보존되지 않던 문제

**증상**
관리자가 본선 제출 점수를 미리 본 뒤 같은 점수의 새 제출이 도착하면 잘못된 제출 버전을 승인할 수 있었고, 기존 공식 점수와 같은 재제출은 검수를 끝낼 수 없었음. 정정 사유와 팀 이름도 감사 기록 또는 현재 팀 정보와 달라질 수 있었음.

**원인**
점수 배열만 비교하고 `submissionVersion`을 공개 초안에 포함하지 않았으며, 클라이언트 참가팀 객체와 정정 사유를 공식 서버 경계에서 완전히 정규화·보존하지 않았음.

**해결**
본선 공개 초안에 정확한 제출 버전을 담아 서버의 workflow·submission·snapshot과 CAS하고, 같은 점수 재제출도 revision 증가 없이 승인 완료함. 참가팀은 트랜잭션에서 읽은 팀 문서로 재구성하고 정정 revision·사유와 승인 제출 메타데이터를 불변 감사 기록에 저장함.

---

## 29. 대시보드 연결·송출 시계·렌더링 수명과 모바일 표시 문제

**증상**
일부 Firestore 구독이 실패하거나 캐시 데이터만 남아도 실시간으로 표시될 수 있었고, 경기장 화면마다 로컬 시계 차이로 다른 부문이 보였음. 저장 문자열이 HTML로 해석될 수 있었고 재렌더 후 우승 효과·리스너가 남거나 다팀 링크제와 모바일 모달·탭이 겹쳤음.

**원인**
구독 상태·서버 snapshot metadata를 합산하지 않았고 송출 epoch와 서버 시간 차이를 권위 있게 측정하지 않았으며, 저장값 `innerHTML`과 렌더별 타이머·고정 링 크기를 사용했음.

**해결**
6개 feed를 서버 확인·캐시·오류 상태로 집계하고, 공개 서버 시계의 왕복 중간값 offset과 서버 epoch로 송출을 동기화함. 재검증 실패 시 타이머를 멈추고 재시도함. 저장값은 DOM/textContent로 렌더링하고 렌더 disposer, 동적 스크롤 링, 2/4열 모바일 탭과 viewport 제한 모달을 적용함.

---

## 30. v3 정확 복원에서 Timestamp와 큰 청크·잘못된 envelope를 처리하지 못하던 문제

**증상**
Firestore Timestamp가 JSON 일반 객체로 바뀌고, 문서 수만 기준으로 묶은 청크가 manifest 문서 1MiB 한도를 넘을 수 있었음. 출처·대회 정보가 빠진 v3 모양 파일도 정확 복원과 prune 단계로 들어갈 수 있었음.

**원인**
Firestore 값 codec이 없었고 원본 문서를 manifest 하위 문서에 다시 중첩 저장했으며 클라이언트 envelope 검증이 느슨했음.

**해결**
Timestamp를 허용된 태그로 인코딩·서버 복호화하고 unknown tag를 거부함. 청크는 바이트·개수·총량으로 제한하고 manifest에는 경로·체크섬 descriptor만 저장하며 실제 데이터는 각 resume 요청에서 체크섬 검증 후 기록함. `app`, `main`, `info`, 모든 필수 v3 필드와 정확한 최상위 키 집합을 강제함.

---

## 31. 기록관 인증 공급자와 클라이언트 감사 내용이 서버 정책과 달랐던 문제

**증상**
문서상 Google 기록관만 허용했지만 일반 인증 계정도 grant를 사용하거나 받을 수 있었고, 기록관이 실제 전환과 다른 `before`·`after` 내용을 불변 감사 문서에 기록할 수 있었음.

**원인**
공급자 제한과 감사 snapshot 진실성이 UI에만 의존했고 Functions·Rules 경계에서 검증되지 않았음.

**해결**
Google 공급자 또는 신뢰된 관리자만 기록관 grant·Callable·Rules 경계를 통과하도록 제한함. 클라이언트 감사 문서의 키와 actor를 allowlist하고 claim·draft-save별 실제 이전/이후 workflow·lock·draft와 일치하는지 Rules에서 검증함.

---

## 32. 배포 프로젝트·런타임·의존성·운영 문서가 현재 구현과 달랐던 문제

**증상**
클라이언트만 대회 ID를 바꿀 수 있다는 안내, 로그인 없는 운영 설명, Hosting/Rules만 배포하는 명령, 폐기된 백업·3위전 설명과 Node 런타임 불일치가 남아 있었음.

**원인**
정적 클라이언트 앱에서 인증·Functions 중심 구조로 발전한 뒤 운영 문서와 패키지 기준을 함께 갱신하지 않았음.

**해결**
Firebase 프로젝트 `bounce-0913-edu-cup-2026`와 대회 `main`을 고정하고 Admin SDK 요구사항에 맞춰 Node 22로 통일함. Firebase·Rules 테스트 도구를 호환 최신 버전으로 올리고 High 전이 취약점을 제거함. README·인수인계 문서를 인증 bootstrap, Java 21, lockfile 설치, 전체 Hosting/Functions/Rules/Indexes 배포, v3 복원 기준으로 다시 작성함.

---

## 33. 기록관의 본선 2:0·2:1 점수와 제출 전 확인값이 일치하지 않던 문제

**증상**
본선 2:0 결과에 비활성 3세트 `0:0`이 포함되어 서버에서 거부되거나, 2:1 결과가 두 세트로 잘릴 수 있었음. 확인 화면을 연 뒤 기존 입력을 바꾸면 확인한 값과 제출 payload가 달라질 수도 있었음.

**원인**
경기 종료 세트 계산이 전체 세트의 승리 수로 중복 구현되었고, 제출 시 확인 snapshot이 아니라 변경 가능한 입력 DOM을 다시 읽었음.

**해결**
첫 두 세트가 2:0일 때만 두 세트로 정규화하고 1:1이면 3세트를 유지하도록 클라이언트·서비스·서버 계약을 통일함. 검토 진입 시 경기 정보와 점수를 불변 payload로 고정하고 제출·재시도는 그 payload만 사용하도록 변경함.

---

## 34. 실시간 갱신·오프라인·페이지 이탈로 입력 중 점수가 사라지던 문제

**증상**
팀·경기 snapshot 갱신이나 모바일 탭 종료 시 미저장 점수가 사라지고, 오래된 기기 초안이 더 최신인 서버 초안을 덮어쓸 수 있었음. 빈 입력도 숫자 0으로 오인될 수 있었음.

**원인**
서버 초안, 로컬 편집값, dirty 상태, 검토값이 분리되지 않았고 입력 DOM을 snapshot마다 다시 만들었음.

**해결**
경기·사용자별 로컬 점수 초안과 revision을 저장하고 dirty 입력 DOM을 실시간 갱신으로 재생성하지 않도록 함. revision 충돌은 사용자에게 명시적으로 선택하게 했으며 빈칸을 숫자 변환 전에 거부하고 오류 입력에 접근성 상태와 포커스를 적용함. 접근 코드와 잠금 토큰은 저장하지 않음.

---

## 35. 방치된 잠금·중복 탭·응답 유실을 안전하게 복구하지 못하던 문제

**증상**
기록관 기기가 꺼지면 잠금이 무기한 남고, 복제 탭이 입력권을 번갈아 가져가거나 제출 응답만 유실된 경우 중복 제출 여부를 알 수 없었음.

**원인**
잠금에 서버 만료 시각과 세션 식별자가 없었고, 종료 작업의 멱등 키가 요청 내용과 결합되지 않았음.

**해결**
잠금을 UID·토큰·탭 세션과 3분 server-time lease로 묶고 60초 heartbeat, 만료 후 감사 가능한 재개, 명시적 탭 인수를 추가함. 제출·종료·폐기에 operation ID와 정규 payload fingerprint를 저장하여 동일 요청은 같은 결과를 반환하고 다른 요청의 ID 재사용은 거부함.

---

## 36. 공용 코드 반복 검증과 기록관 권한 수명·개별 폐기가 없던 문제

**증상**
인증 사용자가 비용이 큰 코드 검증을 반복할 수 있었고, 발급된 기록관 권한이 만료되지 않았으며 한 계정만 폐기할 수 없었음. 전체 코드 회전 뒤 관리자 목록에는 무효 권한이 활성으로 보일 수 있었음.

**원인**
코드 형식·실패 횟수 제한, grant 만료·상태, 서버 기준 effective status가 없었음.

**해결**
24자 base64url 형식을 KDF 전에 강제하고 UID별 15분 실패 창·cooldown을 트랜잭션으로 기록함. grant에 12시간 만료와 상태를 추가하고 같은 코드 버전에서 개별 폐기를 되돌릴 수 없게 했음. 관리자에게 활성·만료·폐기·이전 버전·전체 비활성 상태와 개별 폐기, 전체 회전 전 활성 잠금 영향 확인 UI를 제공함.

---

## 37. 기록관 브라우저와 서버의 인증·유지보수 정책이 달랐던 문제

**증상**
서버는 유지보수나 비 Google 세션을 거부하지만 기록관 화면은 작업 가능 상태로 보이거나, 온라인 복귀 후 권한 확인이 로딩에 머물 수 있었음.

**원인**
브라우저가 비공개 접근 설정을 직접 읽으려 했고 연결·활성 로그인 공급자·관리자 예외·grant 만료를 하나의 권한 상태로 재계산하지 않았음.

**해결**
공개 challenge, 대회 유지보수 상태, 현재 로그인 공급자, 본인 관리자 문서, 본인 grant를 결합한 상태 머신을 적용함. 만료 타이머와 온라인 복귀 재계산을 추가하고 유지보수 중에는 로컬 점수를 보존하면서 입력·제출을 중지하도록 함.

---

## 38. 관리자가 현재 경기 점수를 직접 확정하면 기록관이 권한 만료로 멈추던 문제

**증상**
관리자가 코트의 현재 경기 점수를 직접 입력·확정하면 큐에는 다음 경기가 있어도 기록관 화면은 완료된 이전 경기에 남고, 점수 입력 버튼 대신 “권한이 없거나 권한이 만료되었습니다”가 표시되었음.

**원인**
관리자 점수 확정 트랜잭션이 이전 workflow를 `approved`로 바꾸면서 코트 큐를 다음 경기로 전진시켰고, Firestore Rules는 더 이상 현재 경기가 아닌 이전 workflow의 열린 실시간 listener 읽기를 즉시 거부했음. 기록관 화면은 이 정상적인 경기 전환용 `permission-denied`를 전체 계정 권한 만료로 잘못 표시했음.

**해결**
유효한 기록관 grant는 이미 열어 둔 `submitted`·`approved` workflow 단건 listener를 경기 전환 동안 계속 읽을 수 있게 하되 목록 조회는 계속 차단함. 클라이언트도 유효한 인증 상태에서 발생한 해당 오류를 권한 만료가 아니라 “다음 경기 불러오는 중”으로 처리함. 관리자 직접 점수 확정 뒤 큐 revision 증가, 다음 경기 전환, 기록관의 다음 workflow 읽기를 에뮬레이터 회귀 테스트로 고정함.

---

## 39. 완료 경기가 코트 큐의 현재 경기로 남아 기록관이 다음 경기로 갈 수 없던 문제

**증상**
관리자가 점수를 확정한 예선 경기가 `completed`·`approved` 상태인데도 코트 큐의 `currentMatchKey`가 그 경기를 계속 가리켜, 기록관 화면에는 완료 경기 정보만 보이고 점수 입력이나 다음 경기 이동 버튼이 나타나지 않았음.

**원인**
과거에 코트 미배정 상태에서 관리자가 먼저 점수를 확정한 뒤 코트 배정을 저장한 데이터에서는 완료 경기가 큐의 현재 위치에 남을 수 있었음. 이후 관리자 점수 정정은 이미 승인된 결과를 수정할 뿐 큐를 되감거나 다시 진행시키지 않으므로, 현재 데이터의 stale cursor가 자동 복구되지 않았음.

**해결**
기록관 화면이 현재 큐에서 잠금 없는 `submitted`·`approved`·완료 경기를 발견하면 `다음 경기로 이동` 버튼과 명확한 안내를 표시함. 버튼은 기록관 권한·담당 코트명·queue revision을 검증하는 서버 Callable을 호출하고, 트랜잭션에서 전체 코트 큐를 재투영해 다음 입력 가능한 경기로 한 번만 이동하며 감사 이력을 남김. 관리자 확정 후 정상 전진과 과거 stale queue 복구를 모두 에뮬레이터 회귀 테스트로 고정함.

---

## 40. 기록관 권한 목록 Callable이 배포 직후 internal 오류를 반환한 문제

**증상**
관리자 화면에 접속하자마자 `기록관 권한 목록 실패 (functions/internal): internal` 알림이 반복 표시되었음.

**원인**
전체 Functions 배포가 장시간 timeout으로 중단되는 동안 새로 생성된 `claimRecorderDraft`, `listRecorderGrants`, `renewRecorderLease` Cloud Run 서비스는 배포되었지만, Firebase CLI의 후속 공개 invoker IAM 설정 단계가 완료되지 않았음. Firebase 인증은 Callable 내부 인증이며 Cloud Run 진입 권한을 대신하지 않으므로 요청이 함수 코드에 도달하기 전에 거부되었음.

**해결**
세 서비스에 `roles/run.invoker`의 `allUsers` 진입 권한을 복구함. Callable 내부에서는 기존대로 Firebase ID token, 관리자 문서, 기록관 grant를 검증하므로 애플리케이션 권한 경계는 유지됨. 무인증 HTTP 요청이 Cloud Run 403이 아니라 함수의 정상 `UNAUTHENTICATED` 응답까지 도달하는지 확인함.

---

## 41. 검수 카드에서 세트별 제출 점수를 한눈에 확인하기 어려웠던 문제

**증상**
기록관 제출 점수가 `10:8 / 10:8`처럼 작은 정보 칩 한 줄로만 표시되어, 어느 학교가 각 세트에서 몇 점을 기록했는지 승인 전에 빠르게 대조하기 어려웠음.

**원인**
기록관·제출 시간과 핵심 검수 대상인 점수를 같은 크기의 보조 메타데이터로 취급했고, 세트 번호와 팀 이름의 대응 관계를 시각적으로 제시하지 않았음.

**해결**
검수 카드 중앙에 팀 이름을 열 제목으로 둔 전용 `기록관 제출 점수` 표를 추가함. 각 행에 `1세트`, `2세트`, 필요 시 `3세트`와 양 팀 점수를 큰 숫자로 표시하고 세트 승리 점수를 강조하며, 상단에 전체 세트 스코어를 함께 표시함. 모바일에서도 팀명·세트·점수가 한 화면에 유지되도록 반응형 크기를 적용함.

---

## 42. Hosting 루트에 에뮬레이터 debug 로그가 포함된 문제

**증상**
프로젝트 루트를 Hosting 대상으로 사용하는 상태에서 테스트가 생성한 `firestore-debug.log`가 정적 파일 배포 목록에 포함되어 공개 URL로 접근 가능했음.

**원인**
Hosting ignore 목록이 숨김 파일과 테스트·Functions 경로는 제외했지만 일반 `*.log` 파일을 제외하지 않았음.

**해결**
Firebase Hosting ignore 목록에 `**/*.log`를 추가하고 Hosting을 다시 배포함. 배포 뒤 기존 로그 URL이 404를 반환하는지 확인함.

---

## 43. 본선 대진표 관객 공개가 결승 downstream 검증에서 실패한 문제

**증상**
관리자가 본선 대진표를 관객 화면에 공개할 때 `Final championship match cannot have a downstream match` 오류로 공개가 거부되었음.

**원인**
대진 생성기가 결승 경기의 `nextMatchId`는 `null`로 만들면서도 사용하지 않는 `nextSlot`에는 `A` 또는 `B`를 남겼음. 서버는 결승에 downstream 경기나 slot이 없어야 한다는 구조 무결성 규칙에 따라 이를 거부했음.

**해결**
대진 생성 시 결승의 `nextMatchId`와 `nextSlot`을 모두 `null`로 통일하고, 공개 payload 직렬화에서도 downstream이 없으면 slot을 제거함. 서버는 이미 생성된 로컬 초안의 legacy 결승 slot을 안전하게 `null`로 정규화한 뒤 나머지 대진 그래프를 엄격히 검증하도록 변경함.

---

## 44. 본선 대진 생성 후 v3 백업 저장이 잘못된 파일로 거부된 문제

**증상**
본선 대진표를 생성·공개한 뒤 `백업 파일 저장`을 누르면 다운로드 대신 `올바른 백업 파일이 아닙니다` 오류가 발생했음.

**원인**
본선 공개 과정에서 생성한 `courtAssignments` 문서에는 예선 assignment가 항상 갖는 `attemptCount` 필드가 없었음. v3 백업 정규화기는 모든 assignment의 시도 횟수가 0 이상의 정수라고 엄격하게 검증하므로, 서버가 방금 생성한 본선 문서를 legacy 불완전 문서로 판단했음.

**해결**
앞으로 생성되는 본선 assignment에 `attemptCount: 0`을 항상 기록함. 이미 생성된 본선 assignment의 누락값은 백업 경계에서 의미가 같은 0으로 정규화하되 문자열·음수 등 잘못된 값은 계속 거부함. 현재 운영 Firestore의 전체 116개 사업 문서를 실제 서버 백업 응답 형태로 변환해 v3 백업 검증이 통과하는 것을 확인함.

---

## 45. 일반 관리자 화면에 실효성 없는 migration manifest 기능이 노출된 문제

**증상**
백업·복원 영역에 `migration manifest ID`, `migration manifest 생성`, `manifest 적용`이 표시됐지만 일반 대회 운영에서 언제 사용하는지 알 수 없었음.

**원인**
과거 데이터 구조 전환 전에 경기 문서를 분류하기 위한 내부 점검 기능이 관리자 화면에 남아 있었음. 현재 구현은 실제 데이터를 변환하거나 오류를 수정하지 않고 manifest 상태만 `applied`로 표시하므로 운영 기능으로서 가치가 없었음.

**해결**
관리자 UI와 이벤트 처리, Cloud Functions export, 서버 구현 파일, 에뮬레이터 테스트, 전용 Rules 항목을 모두 제거함. 운영 Firestore에는 삭제할 기존 migration manifest 문서가 없음을 확인함. 실제 백업·복원 기능은 그대로 유지함.

---

## 작성 규칙
- 같은 형식(증상 / 원인 / 해결)으로 항목을 계속 추가합니다.
- 코드 버그가 아니라 사용 방법(파일 교체, 캐시 등) 문제로 판명된 경우도 향후 동일 문제 재발 방지를 위해 기록합니다.

